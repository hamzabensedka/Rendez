import MapSearchScreen from '../src/features/search/pages/MapSearchScreen';

/**
 * Map-based search results (black & white UI).
 * Matches stitch_premium_salon_search_results design.
 * Navigate here from search flow (e.g. after address) or from SearchResultsScreen "Map" / "Filtres".
 */
export default function MapSearchRoute() {
  return <MapSearchScreen />;
}
