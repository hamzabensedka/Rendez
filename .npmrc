# Use hoisted node_modules to avoid Windows path length limits (260 chars) and
# CMake object path limits (~250 chars) when building React Native Android.
# pnpm's default nested layout creates very long paths under .pnpm that break
# the native build (react-native-reanimated, react-native-worklets, etc.).
node-linker=hoisted
