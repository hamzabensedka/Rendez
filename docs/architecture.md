{
  "content": "# Architecture Document\n\n## Overview\nThis document outlines the system architecture for the Planity Clone, a mobile-first platform for discovering, booking, and managing appointments with local businesses. The solution follows a clean, modular architecture with clear separation of concerns, leveraging a monorepo managed by Nx, pnpm workspaces, and Docker Compose for local development.\n\n## Tech Stack\n- **Mobile**: Expo, React Native, TypeScript, Expo Router, TanStack React Query, React Native Reanimated\n- **Backend**: NestJS, Prisma ORM, PostgreSQL (with PostGIS extension), Redis, BullMQ (background jobs)\n- **DevOps / CI**: Docker Compose, GitHub Actions, EAS Build (Expo), Supabase (auth & storage optional)\n- **Testing**: Jest (unit & integration)\n- **Monorepo Tooling**: Nx, pnpm\n\n## High-Level Architecture\nThe system is split into three main layers:\n1. **Client Layer** – Expo mobile app (user-facing) and optional web admin/provider portals (could be built with NestJS + React later).\n2. **API Layer** – NestJS microservices (logically separated via modules) exposing REST/GraphQL endpoints.\n3. **Data Layer** – PostgreSQL (core data + PostGIS for geo queries), Redis (caching, rate‑limiting, job queue), BullMQ for background processing.\n\nCommunication:\n- Mobile app ↔ API via HTTPS (REST) with JWT authentication.\n- API ↔ PostgreSQL via Prisma Client.\n- API ↔ Redis for caching and pub/sub.\n- BullMQ workers consume jobs from Redis.\n\n## Service Boundaries (Domain Modules)\n| Module | Responsibility | Key Entities | Notes |\n|--------|----------------|--------------|-------|\n| **auth** | User registration, login, password reset, social auth, JWT issuance | User, Session | Uses Prisma; optional Supabase integration for email/OAuth. |\n| **business** | Business CRUD, categories, geo‑search, hours of operation | Business, Category, BusinessHours, Location (PostGIS) | Spatial queries powered by PostGIS; cached in Redis. |\n| **booking** | Appointment creation, modification, cancellation, slot computation | Appointment, Service, Provider, TimeSlot | Slot algorithm lives here; uses business hours + existing bookings. |\n| **reviews** | Ratings & reviews creation, retrieval, aggregation | Review, Rating | Aggregated scores stored denormatively for fast reads. |\n| **payments** | Payment intent creation, webhook handling, transaction recording | Payment, Invoice | Integrates with a PSP (Stripe/PayPal); webhook processed via BullMQ. |\n| **notifications** | Push, email, in‑app notifications (triggered by events) | Notification, DeviceToken | Uses Expo Push SDK; background jobs send via BullMQ. |\n| **provider** | Business owner portal: manage listings, view bookings, basic analytics | ProviderProfile, Business (owned) | Re‑uses business & booking modules; adds role‑based access. |\n| **admin** | Super‑admin dashboard: user/business management, analytics, settings | AdminUser, AuditLog | Role‑based access; can impersonate users for support. |\n| **shared** | Shared TypeScript types, constants, utilities, design system tokens, reusable UI components | – | Published as an Nx library (`@planity/shared`) consumed by mobile and backend (where applicable). |\n\n## Folder Structure (Nx Monorepo)\n```\nplanity-clone/\n├── apps/\n│   ├── mobile/                 # Expo React Native app\n│   │   ├── src/\n│   │   │   ├── assets/\n│   │   │   ├── components/     # Reanimated animations, shared UI\n│   │   │   ├── navigation/     # Expo Router config (tabs, stacks)\n│   │   │   ├── screens/\n│   │   │   │   ├── auth/\n│   │   │   │   ├── browse/\n│   │   │   │   ├── business/\n│   │   │   │   ├── booking/\n│   │   │   │   ├── profile/\n│   │   │   │   ├── provider/\n│   │   │   │   └── admin/\n│   │   │   ├── hooks/\n│   │   │   ├── lib/\n│   │   │   │   ├── queryClient.ts   # TanStack Query setup\n│   │   │   │   ├── analytics.ts\n│   │   │   │   └── geolocation.ts\n│   │   │   ├── styles/\n│   │   │   │   └── theme.ts       # Design system tokens\n│   │   │   ├── types/\n│   │   │   │   └── index.ts       # Re-exports from @planity/shared\n│   │   │   └── App.tsx\n│   │   ├── app.json\n│   │   ├── eas.json\n│   │   └── expo-router.config.ts\n│   ├─ admin/                  # Optional NestJS + React admin portal (future)\n│   └─ provider/               # Optional NestJS + React provider portal (future)\n├── libs/\n│   ├── shared/                # Shared TS types, utils, design tokens\n│   │   ├── src/\n│   │   │   ├── types/\n│   │   │   │   ├── user.ts\n│   │   │   │   ├── business.ts\n│   │   │   │   ├── booking.ts\n│   │   │   │   └── common.ts\n│   │   │   ├── utils/\n│   │   │   │   ├── geo.ts\n│   │   │   │   ├── auth.ts\n│   │   │   │   └── validation.ts\n│   │   │   ├── constants/\n│   │   │   └── ui/               # Design system primitives (buttons, inputs)\n│   │   └── index.ts\n│   ├─ auth/\n│   │   └── src/\n│   │       ├── dto/\n│   │   │   ├── entities/\n│   │   │   ├── guards/\n│   │   │   ├── strategies/\n│   │   │   ├── auth.controller.ts\n│   │   │   └── auth.module.ts\n│   ├─ business/\n│   │   └── src/\n│   │       ├── dto/\n│   │   │   ├── entities/\n│   │   │   ├── repositories/\n│   │   │   ├── business.service.ts\n│   │   │   ├── business.controller.ts\n│   │   │   └── business.module.ts\n│   ├─ booking/\n│   │   └── src/\n│   │       ├── dto/\n│   │   │   ├── entities/\n│   │   │   ├── slots/               # Slot computation logic\n│   │   │   ├── booking.service.ts\n│   │   │   ├── booking.controller.ts\n│   │   │   └── booking.module.ts\n│   ├─ reviews/\n│   │   └── src/\n│   │       ├── dto/\n│   │   │   ├── entities/\n│   │   │   ├── reviews.service.ts\n│   │   │   ├── reviews.controller.ts\n│   │   │   └── reviews.module.ts\n│   ├─ payments/\n│   │   └── src/\n│   │       ├── dto/\n│   │   │   ├── entities/\n│   │   │   ├── payments.service.ts\n│   │   │   ├── payments.controller.ts\n│   │   │   └── payments.module.ts\n│   ├─ notifications/\n│   │   └── src/\n│   │       ├── dto/\n│   │   │   ├── entities/\n│   │   │   ├── notifications.service.ts\n│   │   │   ├── notifications.controller.ts\n│   │   │   └── notifications.module.ts\n│   ├─ provider/\n│   │   └── src/\n│   │       ├── dto/\n│   │   │   ├── entities/\n│   │   │   ├── provider.service.ts\n│   │   │   ├── provider.controller.ts\n│   │   │   └── provider.module.ts\n│   └─ admin/\n│       └── src/\n│           ├── dto/\n│           │   ├── entities/\n│           │   ├── admin.service.ts\n│           │   ├── admin.controller.ts\n│           │   └── admin.module.ts\n├── prisma/\n│   ├── schema.prisma          # Defines all models, includes PostGIS extensions via raw SQL\n│   └── migrations/\n├── docker-compose.yml         # Services: postgres, redis, nestjs (api), expo (dev), optional worker\n├── .github/\n│   └── workflows/\n│       ├── ci.yml             # Lint, test, build (Nx affected)\n│       ├── cd-mobile.yml      # EAS Build + upload to stores\n│       └── cd-api.yml         # Docker build & push to registry\n├── nx.json\n├── package.json               # pnpm workspace root\n└── tsconfig.base.json\n```\n\n## Data Modeling (Prisma Snippet)\n```prisma\n// prisma/schema.prisma\ndatasource db {\n  provider = \"postgresql\"\n  url      = env(\"DATABASE_URL\")\n}\ngenerator client {\n  provider = \"prisma-client-js\"\n}\n\nmodel User {\n  id            String   @id @default(uuid())\n  email         String   @unique\n  passwordHash  String\n  role          Role     @default(USER)\n  createdAt     DateTime @default(now())\n  updatedAt     DateTime @updatedAt\n  sessions      Session[]\n  bookings      Booking[]\n  reviews       Review[]\n  favorites     Favorite[]\n}\n\nmodel Business {\n  id            String   @id @default(uuid())\n  name          String\n  description   String?\n  address       String\n  phone         String?\n  location      Point?   @db.Point   // PostGIS\n  categoryId    String\n  category      Category @relation(fields: [categoryId], references: [id])\n  hours         BusinessHours[]\n  services      Service[]\n  bookings      Booking[]\n  reviews       Review[]\n}\n// … other models (Service, Appointment, Review, Payment, Notification, Favorite, etc.)\n```\n\n## Caching & Background Jobs\n- **Redis**:\n  - Cache for business listings, search results, and slot computations (TTL 5‑15 min).\n  - Rate‑limiting middleware (express-rate-limit-redis).\n  - Broker for BullMQ (job queue).\n- **BullMQ Workers** (run as separate Node processes via Docker Compose):\n  - `send-notifications` – processes Notification jobs, calls Expo Push API.\n  - `process-payment` – confirms payment with PSP, updates Appointment status.\n  - `update-availability` – recomputes slots after booking/cancellation.\n\n## Authentication & Authorization\n- JWT (access token 15 min, refresh token 7 d) stored securely (Expo SecureStore).\n- NestJS `AuthGuard` validates token; roles (`USER`, `PROVIDER`, `ADMIN`) guard routes.\n- Provider and Admin portals enforce additional role checks.\n\n## API Communication\n- **REST** with JSON payloads; versioned via `/api/v1/`.\n- TanStack React Query handles data fetching, caching, optimistic updates, and background refetch.\n- Reanimated used for gesture‑based UI (e.g., map drag, swipe‑to‑cancel).\n\n## Mobile App Navigation (Expo Router)\n- File‑based routing under `apps/mobile/src/app/`.\n- Auth stack (login, register, reset) → protected tabs (Home, Map, Bookings, Profile).\n- Deep linking support for sharing business URLs.\n\n## DevOps & CI/CD\n- **Docker Compose** defines services: `postgres`, `redis`, `api` (NestJS), `worker` (BullMQ), and optionally `expo` for web preview.\n- **GitHub Actions**:\n  - On PR: lint (`nx lint`), type‑check (`nx type-check`), test (`nx test`).\n  - On merge to `main`: build Docker images, push to registry, trigger EAS Build for iOS/Android, deploy API to staging/prod.\n- **EAS Build**: configured via `eas.json` with profiles for development, preview, and production.\n- **Supabase** (optional): used for email OTP & social login; fallback to custom auth if not needed.\n\n## Testing Strategy\n- **Unit Tests**: Jest + React Native Testing Library for components/services; NestJS Testing Utility for controllers/services.\n- **Integration Tests**: API endpoints with supertest; DB rolled back via transactions.\n- **E2E** (future): Detox for mobile flows.\n- Test coverage target ≥80% for critical domains (auth, booking, payment).\n\n## Security Considerations\n- HTTPS everywhere; HSTS headers.\n- Input validation via class‑validator & DTOs.\n- Prisma protects against SQL injection.\n- Redis accessed only via internal network.\n- Payment data never touches our servers; only webhook verification.\n- Environment variables managed via `.env` (dotenv) and CI secrets.\n\n## Scalability & Performance\n- Horizontal scaling of NestJS API behind a load balancer.\n- Read replicas for PostgreSQL for heavy search queries.\n- Redis clustering for cache and job queue at scale.\n- Business geo‑search leverages PostGIS GiST indexes.\n- Slot computation cached per business/date; invalidated on booking changes.\n\n## Summary\nThe architecture provides a modular, maintainable foundation using a monorepo (Nx/pnpm), clean domain‑driven service boundaries, and proven cloud‑native technologies (Expo, NestJS, PostgreSQL/PostGIS, Redis, BullMQ) to support the Planity Clone’s feature set while enabling independent scaling and deployment of mobile and backend concerns.\n",
  "summary": "A modular monorepo architecture separating mobile (Expo/React Native) and backend (NestJS/Prisma/PostGIS/Redis) into distinct domain services, with shared types, caching, background jobs, and CI/CD pipelines for scalable, maintainable development."
}