{
  "content": "# Planity Clone Architecture\n\n## Overview\nThis document describes the system architecture for the Planity Clone, a mobile‑first appointment‑booking platform built with Expo/React Native on the frontend and NestJS on the backend. The solution follows clean‑architecture principles, separates concerns into distinct layers, and leverages an Nx monorepo for shared code, type safety, and efficient tooling.\n\n## High-Level Architecture\n- **Mobile Client** – Expo managed workflow, React Native, TypeScript, Expo Router, TanStack React Query, React Native Reanimated.\n- **API Gateway** – NestJS (Node.js) REST/GraphQL hybrid (REST for simplicity, GraphQL optional).\n- **Data Store** – PostgreSQL with PostGIS extension for geo‑queries; Prisma ORM for type‑safe DB access.\n- **Caching & Pub/Sub** – Redis (session store, rate‑limiting, background‑job queue via BullMQ).\n- **Background Jobs** – BullMQ workers running in separate NestJS services.\n- **Infrastructure** – Docker Compose for local dev; GitHub Actions for CI; EAS Build for OTA & store builds; Supabase for optional auth & storage fallback.\n- **Monorepo** – Nx workspace with apps/mobile, apps/api, libs/shared, libs/ui, libs/types, libs/utils.\n\n## Mobile App (Expo)\n### Responsibilities\n- UI presentation, navigation, gesture animations.\n- State management via React Query (server‑state) and React Native Reanimated (UI‑state).\n- Offline‑first caching, optimistic updates.\n- Deep linking, push notifications (via Expo Push).\n- Authentication flow (guest vs authenticated).\n\n### Folder Structure (`apps/mobile`)\n```\nsrc/\n  assets/               # images, icons, fonts\n  components/           # dumb UI components (Reanimated, shared)\n  features/\n    auth/               # login, register, reset password screens\n    browse/             # guest & logged‑in business list, search\n    map/                # map view with markers, geo‑search\n    detail/             # business detail, services, reviews\n    booking/            # service selection, slot picker, confirmation\n    appointments/       # upcoming, past, cancel/reschedule\n    favorites/          # toggle & list\n    profile/            # view/edit profile\n    reviews/            # add review, respond\n    payments/           # payment sheet integration\n  navigation/           # Expo Router route definitions\n  hooks/                # custom React Query hooks, geo, auth\n  utils/                # formatters, constants, helpers\n  types/                # re‑exported shared types + UI‑specific\n  theme/                # design system tokens (colors, spacing, typography)\n  app.tsx               # root (Expo Router) entry point\n```\n\n### Key Libraries\n- **Expo Router** – file‑based routing, supports web & native.\n- **TanStack React Query** – data fetching, caching, background refetch.\n- **React Native Reanimated 2** – smooth animations, gesture‑based slot picker.\n- **Zod** – runtime validation of API responses.\n- **expo-notifications** – push & local notifications.\n- **expo-location** – user location for geo‑search.\n- **expo-secure-store** – JWT refresh token storage.\n\n## Backend (NestJS)\n### Responsibilities\n- Business logic, validation, authorization.\n- API contracts (REST) consumed by mobile.\n- Transactional outbox for event‑driven updates (e.g., slot changes).\n- Integration with payment gateway (Stripe/PayPal).\n- Background job processing (BullMQ).\n- Admin & provider portals (same API, role‑based guards).\n\n### Folder Structure (`apps/api`)\n```\nsrc/\n  common/               # guards, interceptors, pipes, exceptions\n  config/               # environment validation (nestjs/config)\n  modules/\n    auth/               # JWT strategies, refresh token rotation\n    users/              # CRUD, profile, role management\n    businesses/         # CRUD, geo‑search (PostGIS), categories\n    services/           # service definitions tied to businesses\n    slots/              # availability computation, booking slots\n    bookings/           # create, cancel, reschedule, payment linking\n    reviews/            # review CRUD, business responses\n    favorites/          # toggle, list\n    payments/           # webhook handling, invoice generation\n    notifications/      # template service, dispatch to BullMQ\n    admin/              # admin‑only endpoints (settings, user mgmt)\n    provider/           # provider‑only endpoints (listings, schedule)\n  jobs/                 # BullMQ processors (email, slot refresh, etc.)\n  prisma/               # Prisma schema, migrations, seed\n  utils/                # geo helpers, pagination, mailers\n  main.ts               # bootstrap\n```\n\n### Key Libraries\n- **NestJS** – modular, DI, built‑in validation pipes.\n- **Prisma ORM** – Type‑safe DB access, migrations.\n- **PostGIS** – spatial indexes for location‑based queries.\n- **BullMQ** – reliable job queues with retries.\n- **Passport JWT** – authentication strategy.\n- **class-validator & class-transformer** – DTO validation.\n- **Stripe Node** – payment processing.\n- **nodemailer / sendgrid** – email notifications (fallback to in‑app).\n\n## Shared Packages (Nx Monorepo)\n- **libs/types** – DTOs, entity shapes, enums shared between mobile and API (generated from Prisma via `prisma-dto-generator` or manual sync).\n- **libs/ui** – reusable native UI components (buttons, inputs, cards) built with React Native Reanimated.\n- **libs/utils** – logging, error formatting, date helpers, geo utilities.\n- **libs/config** – centralized environment schema (used by both apps).\n\n## Data Layer\n- **PostgreSQL** – primary relational store.\n- **PostGIS Extension** – `geography` column on `businesses` for fast radius queries.\n- **Prisma** – schema defines models: User, Business, Category, Service, Slot, Booking, Review, Favorite, Payment, Notification.\n- **Redis** – stores short‑lived sessions, rate‑limit counters, and BullMQ job queues.\n- **Migration Strategy** – Prisma migrate dev/prod; CI runs `prisma migrate deploy`.\n\n## DevOps & CI/CD\n- **Docker Compose** – defines services: api, redis, db, (optional) mailhog, worker.\n- **GitHub Actions** – lint, type‑check, unit tests (Jest), build Docker images, push to registry.\n- **EAS Build** – OTA updates via Expo; store builds for iOS/Android.\n- **Supabase** – optional fallback for auth & file storage (avatars, receipts).\n- **Monitoring** – health endpoints, structured logging (pino), alerting via GitHub Actions workflow.\n\n## Service Boundaries\n| Boundary            | Responsibility                                 | Tech                              |\n|---------------------|------------------------------------------------|-----------------------------------|\n| **Guest Browse**    | Public business list, search, map view         | Mobile (Expo) + API (read‑only)   |\n| **Authentication**  | Sign‑up, login, password reset, JWT handling   | API (auth module) + Mobile        |\n| **Business Mgmt**   | CRUD, geo‑search, categories, service list     | API (businesses, services modules)|\n| **Booking Flow**    | Slot computation, validation, payment, confirmation | API (slots, bookings, payments) + Mobile |\n| **Appointment Mgmt**| View, cancel, reschedule upcoming bookings    | API (bookings) + Mobile           |\n| **Reviews & Ratings**| Create, read, respond to reviews              | API (reviews) + Mobile            |\n| **Favorites**       | Toggle, list                                   | API (favorites) + Mobile          |\n| **User Profile**    | Read/update personal data                      | API (users) + Mobile              |\n| **Notifications**   | Send push/email, manage preferences            | API (notifications) + Jobs + Mobile|\n| **Payments**        | Secure transaction processing, webhook handling| API (payments) + Stripe SDK       |\n| **Provider Portal** | Manage own listing, services, schedule         | API (provider) + Mobile (web view)|\n| **Admin Dashboard** | Global settings, user/business moderation      | API (admin) + Mobile (web view)   |\n| **Background Jobs** | Slot refresh, email/SMS dispatch, retry logic  | BullMQ workers (NestJS)           |\n| **Design System**   | Shared tokens, components, types               | libs/ui, libs/types               |\n\n## Folder Structure (Monorepo Root)\n```\nplanity-clone/\n├─ apps/\n│  ├─ mobile/          # Expo React Native app\n│  └─ api/             # NestJS server\n├─ libs/\n│  ├─ types/           # Shared TypeScript interfaces\n│  ├─ ui/              # Reusable native components\n│  ├─ utils/           # Cross‑platform helpers\n│  └─ config/          # Env schema validation\n├─ prisma/\n│  ├─ schema.prisma\n│  └─ migrations/\n├─ docker-compose.yml\n├─ nx.json\n├─ package.json (pnpm workspace)\n└─ .github/\n   └─ workflows/\n      ├─ ci.yml\n      └─ release.yml\n```\n\n## Coding Conventions\n- **TypeScript** strict mode, `noImplicitAny`.\n- **Barrel exports** avoided; explicit paths via `tsconfig` paths.\n- **Component hierarchy**: smart (feature) → dumb (UI) → atoms (Reanimated).\n- **State**: server state via React Query; UI state via Reanimated/Zustand if needed.\n- **Error handling**: centralized error boundaries (mobile) and exception filters (NestJS).\n- **Testing**: Jest + React Native Testing Library for mobile; Jest + Supertest for API; aim for 80%+ unit coverage.\n- **Linting**: ESLint (plugin:@typescript-eslint, react-native) + Prettier.\n- **Commit messages**: Conventional Commits (feat, fix, docs, style, refactor, test, chore).\n\n## Conclusion\nThis architecture cleanly separates concerns, leverages the strengths of each chosen technology, and provides a scalable foundation for the Planity Clone. The Nx monorepo enables rapid iteration across mobile and backend while maintaining type safety and shared tooling. All features from the product spec map to distinct service boundaries, facilitating independent development, testing, and deployment.\n",
  "summary": "A clean‑architecture, Nx‑monorepo plan for Planity Clone using Expo/React Native, NestJS, Prisma/PostGIS, Redis, and supporting DevOps tools."
}