{
  "content": "# Planity Clone — System Architecture\n\n## 1. High-Level Overview\nPlanity Clone is a mobile-first marketplace for beauty & wellness booking. The system is a pnpm + Nx monorepo containing:\n- **Mobile app** (Expo / React Native / Expo Router) for clients.\n- **Backend API** (NestJS) for all business logic, auth, payments, search.\n- **Provider/Admin web** (optional later; admin via NestJS + simple dashboard) — out of scope for mobile MVP but served by same API.\n- **Shared packages** for types, UI, config.\n\nInfra: PostgreSQL + PostGIS, Redis (cache + BullMQ), Supabase (auth helper / storage), EAS Build (mobile CI/CD), GitHub Actions (monorepo CI), Docker Compose (local dev).\n\n## 2. Monorepo Layout (Nx + pnpm)\n```\nplanity-clone/\n├── apps/\n│   ├── mobile/                # Expo RN app (clients)\n│   ├── api/                   # NestJS backend\n│   └── admin-web/             # (P1) React Web for admin/provider\n├── packages/\n│   ├── types/                 # Shared TS types (DTOs, entities)\n│   ├── ui/                    # RN component library + theme\n│   ├── config/                # ESLint, TS, jest presets\n│   └── utils/                 # Date, geo, format helpers\n├── tools/\n│   ├── docker/                # Dockerfiles, compose\n│   └── scripts/               # Seed, migrate\n├── .github/\n│   └── workflows/             # CI pipelines\n├── nx.json\n├── pnpm-workspace.yaml\n└── README.md\n```\n\n## 3. Mobile App (apps/mobile)\n```\napps/mobile/\n├── app/                       # Expo Router routes\n│   ├── (public)/\n│   │   ├── index.tsx          # Guest home\n│   │   ├── search.tsx\n│   │   └── business/[id].tsx\n│   ├── (auth)/\n│   │   ├── login.tsx\n│   │   └── register.tsx\n│   ├── (tabs)/\n│   │   ├── home.tsx\n│   │   ├── bookings.tsx\n│   │   └── profile.tsx\n│   ├── booking/\n│   │   └── [businessId].tsx   # Multi-step flow\n│   └── _layout.tsx\n├── src/\n│   ├── api/                   # React Query hooks\n│   ├── components/            # Feature components\n│   ├── hooks/\n│   ├── store/                 # Zustand (local UI state)\n│   └── lib/                   # Supabase, stripe, reanimated helpers\n├── assets/\n└── eas.json\n```\n- **Expo Router** for file-based navigation.\n- **TanStack React Query** for server state; **Zustand** for ephemeral flow state (booking wizard).\n- **Reanimated** for map transitions, sheet animations.\n\n## 4. Backend API (apps/api)\n```\napps/api/\n├── src/\n│   ├── main.ts\n│   ├── app.module.ts\n│   ├── modules/\n│   │   ├── auth/              # JWT, OAuth, reset\n│   │   ├── users/\n│   │   ├── businesses/       # CRUD, categories mapping\n│   │   ├── categories/\n│   │   ├── services/\n│   │   ├── staff/\n│   │   ├── availability/     # Slot engine\n│   │   ├── bookings/\n│   │   ├── payments/         # Stripe\n│   │   ├── reviews/\n│   │   ├── notifications/    # FCM/APNs/email/SMS\n│   │   ├── favorites/\n│   │   └── admin/\n│   ├── common/               # Guards, decorators, pipes\n│   ├── jobs/                 # BullMQ processors\n│   ├── prisma/               # PrismaService, migrations\n│   └── redis/                # RedisModule\n├── prisma/\n│   ├── schema.prisma\n│   └── seed.ts\n└── test/\n```\n\n## 5. Service Boundaries\n- **Auth**: issues access/refresh JWT; integrates Supabase OAuth; no business logic.\n- **Businesses / Categories / Services / Staff**: catalog domain; validated by admin.\n- **Availability**: pure function module; computes slots from rules + bookings; timezone aware.\n- **Bookings**: orchestrates availability + payments + notifications.\n- **Payments**: Stripe only; never stores PAN; emits events.\n- **Notifications**: subscribes to domain events; respects prefs; pushes to queues.\n- **Jobs**: BullMQ workers for reminders, emails, recompute.\n- **Admin**: separate guards; user/business moderation.\n\n## 6. Data Layer\n- **PostgreSQL** + **PostGIS**: businesses with `geometry(Point)`; distance queries.\n- **Prisma**: schema, migrations, typed client.\n- **Redis**: cache (search results, availability), BullMQ broker.\n- **Supabase**: auth social providers, file storage for photos.\n\n## 7. Cross-Cutting\n- **packages/types**: DTOs shared by mobile & API (contract-first).\n- **packages/ui**: RN buttons, cards, inputs, theme (Reanimated ready).\n- **GitHub Actions**: lint, typecheck, test per app; EAS build on tag.\n- **Docker Compose**: postgres, redis, api, mobile dev proxy.\n\n## 8. Key Flows\n1. **Booking**: mobile → React Query → API Bookings.create → Availability.check → Payments.intent → Notifications.queue.\n2. **Search**: API uses PostGIS `ST_DWithin` + filters; Redis cache 60s.\n3. **Map**: viewport sent; API returns clustered points; radius filter updates list.\n\n## 9. Testing\n- Jest in all apps/packages; API e2e with test DB; mobile component tests.\n\n## 10. Scalability\n- Stateless API (horizontal scale).\n- Redis for hot paths.\n- BullMQ for async work.\n- PostGIS indexed spatial queries.",
  "summary": "Monorepo architecture for Planity Clone using Expo/NestJS with clear module boundaries, shared types, and Docker/CI setup."
}