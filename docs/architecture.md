{
  "content": "# Planity Clone Architecture\n\n## Overview\nThe Planity Clone is a mobile-first application that connects users with local service businesses for discovery, booking, and management of appointments. The architecture follows a clean, layered approach with clear separation of concerns, leveraging a monorepo (Nx) to share code between frontend and backend, and using Docker Compose for local development.\n\n## Tech Stack\n- **Frontend**: Expo (React Native), TypeScript, Expo Router, TanStack React Query, React Native Reanimated\n- **Backend**: NestJS, TypeScript, Prisma ORM, PostgreSQL + PostGIS, Redis, BullMQ (background jobs)\n- **DevOps**: Nx monorepo, pnpm, Docker Compose, GitHub Actions, EAS Build (Expo), Supabase (optional auth/storage), Jest\n\n## High-Level Architecture\n```\n+-------------------+        +---------------------+        +-------------------+\n|   Mobile App      | <----> |   API Gateway (Nest) | <----> |   Data Stores     |\n| (Expo/React)      |        |   (REST + WS)       |        | (Postgres, Redis) |\n+-------------------+        +---------------------+        +-------------------+\n        ^                           ^                           ^\n        |                           |                           |\n+-------------------+        +---------------------+        +-------------------+\n|   Auth (Supabase) |        |   Worker (BullMQ)   |        |   File Storage    |\n|   (OAuth/JWT)     |        |   (async jobs)      |        |   (Supabase/S3)   |\n+-------------------+        +---------------------+        +-------------------+\n```\nThe mobile app communicates exclusively with the NestJS API over HTTPS (REST) and occasional WebSocket connections for real‑time updates (e.g., booking status). The API handles business logic, validation, transactional operations, and delegates heavy or long‑running tasks to BullMQ workers. Redis is used for caching, rate‑limiting, and as the broker for BullMQ. PostGIS enables location‑based queries for map search.\n\n## Service Boundaries\n1. **Mobile Client (apps/mobile)**\n   - UI layer, navigation (Expo Router), state management (React Query).\n   - Handles offline caching, optimistic updates, and UI animations (Reanimated).\n   - Communicates via generated API clients (typed with `swagger-typescript-api` or manual).\n\n2. **API Gateway (apps/api)**\n   - NestJS modular monolith: `AuthModule`, `BusinessModule`, `BookingModule`, `PaymentModule`, `NotificationModule`, `AdminModule`, `ProviderPortalModule`.\n   - Each module contains controllers, services, DTOs, guards, and interceptors.\n   - Uses Prisma for ORM; transactions ensure consistency across related tables (e.g., booking creation).\n   - Exposes REST endpoints; optional GraphQL or WS via Nest adapters for real‑time.\n\n3. **Background Workers (apps/api/workers)**\n   - BullMQ queues: `email`, `sms`, `payment‑webhook`, `analytics‑aggregation`.\n   - Workers are separate Nest services that process jobs without blocking HTTP requests.\n\n4. **Infrastructure (infra/)**\n   - Docker Compose defines services: `api`, `db`, `redis`, `worker`.\n   - Supabase used for auth (email/social) and optional storage bucket for user‑uploaded media.\n   - EAS Build config for iOS/Android builds; GitHub Actions runs lint, test, build, and pushes Docker images to a registry.\n\n## Folder Structure (Nx Monorepo)\n```\nplanity-clone/\n├── apps/\n│   ├── mobile/                 # Expo React Native app\n│   │   ├── src/\n│   │   │   ├── assets/\n│   │   │   ├── components/\n│   │   │   ├── navigation/\n│   │   │   ├── screens/\n│   │   │   ├── hooks/\n│   │   │   ├── utils/\n│   │   │   ├── types/\n│   │   │   └── app.tsx\n│   │   ├── expo.config.js\n│   │   └── tsconfig.json\n│   └── api/                    # NestJS backend\n│       ├── src/\n│       │   ├── auth/\n│       │   ├── business/\n│       │   ├── booking/\n│       │   ├── payment/\n│       │   ├── notification/\n│       │   ├── provider/\n│       │   ├── admin/\n│       │   ├── common/\n│       │   │   ├── guards/\n│       │   │   ├── interceptors/\n│       │   │   └── dto/\n│       │   ├── workers/\n│       │   │   ├── email.processor.ts\n│       │   │   └── sms.processor.ts\n│       │   ├── prisma/\n│       │   │   └── schema.prisma\n│       │   ├── main.ts\n│       │   └── app.module.ts\n│       ├── test/\n│       └── tsconfig.json\n├── libs/\n│   ├── ui/                     # Shared React Native components (design system)\n│   ├── types/                  # Shared TypeScript interfaces (DTOs, enums)\n│   └── utils/                  # Shared helpers (validation, formatting)\n├── infra/\n│   ├── docker-compose.yml\n│   ├── Dockerfile.api\n│   └── Dockerfile.mob\n├── tools/\n│   └── generators/             # Nx schematic generators\n├── nx.json\n├── package.json\n└── README.md\n```\n\n## Data Model (Prisma Schema)\n```prisma\n// Simplified\nmodel User {\n  id            String   @id @default(uuid())\n  email         String   @unique\n  password      String?\n  name          String\n  role          Role     @default(USER)\n  createdAt     DateTime @default(now())\n  updatedAt     DateTime @updatedAt\n  bookings      Booking[]\n  favorites     Business[] @relation(\"UserFavorites\")\n  reviews       Review[]\n}\n\nmodel Business {\n  id            String   @id @default(uuid())\n  name          String\n  description   String?\n  address       String\n  location      Point    @db.Point\n  category      String\n  services      Service[]\n  bookings      Booking[]\n  reviews       Review[]\n  favoritedBy   User[]   @relation(\"UserFavorites\")\n  createdAt     DateTime @default(now())\n}\n\nmodel Service {\n  id            String   @id @default(uuid())\n  name          String\n  durationMin   Int\n  price         Float\n  businessId    String\n  business      Business @relation(fields: [businessId], references: [id])\n  bookings      Booking[]\n}\n\nmodel Booking {\n  id            String   @id @default(uuid())\n  userId        String\n  businessId    String\n  serviceId     String\n  startTime     DateTime\n  endTime       DateTime\n  status        BookingStatus\n  paymentId     String?\n  createdAt     DateTime @default(now())\n  user          User     @relation(fields: [userId], references: [id])\n  business      Business @relation(fields: [businessId], references: [id])\n  service       Service  @relation(fields: [serviceId], references: [id])\n}\n\nmodel Review {\n  id            String   @id @default(uuid())\n  userId        String\n  businessId    String\n  rating        Int\n  comment       String?\n  createdAt     DateTime @default(now())\n  user          User     @relation(fields: [userId], references: [id])\n  business      Business @relation(fields: [businessId], references: [id])\n}\n\nenum Role { USER ADMIN PROVIDER }\nenum BookingStatus { PENDING CONFIRMED COMPLETED CANCELED }\n```\nPostGIS extension enables `location` field for proximity queries.\n\n## API Contracts\n- **REST** (JSON) with versioning (`/api/v1/`).\n- **DTOs** defined in `libs/types` and used via NestJS `class-validator` and `class-transformer`.\n- **OpenAPI** (Swagger) generated automatically for documentation and client generation.\n- **WebSocket** (NestJS Gateway) for real‑time booking status updates; clients subscribe to `/ws/bookings/:userId`.\n\n## Caching & Pub/Sub\n- **Redis**:\n  - Cache layer for business listings, search results, and user profiles (TTL 5‑15 min).\n  - Rate‑limiting middleware (e.g., 100 req/min/IP).\n  - BullMQ uses Redis as job queue and broker.\n- **Optional**: Redis Pub/Sub for broadcasting events to multiple API instances (horizontal scaling).\n\n## Background Jobs (BullMQ)\n- Queues: `email`, `sms`, `payment-webhook`, `analytics-aggregation`.\n- Workers are Nest services decorated with `@Processor(queueName)`.\n- Failed jobs retry with exponential backoff; dead‑letter queue moved to a separate table for manual inspection.\n- Monitoring via BullMQ UI (mounted under `/admin/queues` in dev).\n\n## DevOps & CI/CD\n- **Local**: `docker compose up --build` spins up API, DB, Redis, and worker.\n- **CI (GitHub Actions)**:\n  1. Install pnpm dependencies.\n  2. Run lint (`nx lint`).\n  3. Run unit & integration tests (`nx test`).\n  4. Build Docker images for API and worker.\n  5. Push images to container registry (GHCR).\n  6. On `main` branch, trigger EAS Build for iOS/Android (via Expo CLI).\n  7. Deploy API to a managed Kubernetes service (or ECS/Fargate) using Helm charts (not shown but implied).\n- **Environment Variables**: managed via `.env` files; secrets injected via GitHub Secrets or cloud provider secret manager.\n- **Database Migrations**: Prisma migrate deployed as part of API startup (`prisma migrate deploy`).\n\n## Testing Strategy\n- **Unit Tests**: Jest for services, utilities, and Nest controllers (mocking Prisma).\n- **Integration Tests**: Supertest against a test PostgreSQL instance (spun up via Docker in CI).\n- **E2E (Mobile)**: Detox or Expo’s built‑in testing library for critical flows (login, search, booking).\n- **Test Coverage Goal**: ≥80% for backend, ≥70% for frontend.\n\n## Security Considerations\n- **Authentication**: JWT (access token 15 min, refresh token 7 days) issued via Supabase Auth; refresh token rotation.\n- **Authorization**: NestJS guards (`RolesGuard`, `OwnershipGuard`) enforce RBAC.\n- **Data Validation**: DTOs with `class-validator`; sanitization to prevent injection.\n- **Rate Limiting & Brute‑Force**: Redis‑based limiter on auth endpoints.\n- **Secrets**: Never stored in repo; accessed via process env.\n- **Transport Security**: TLS enforced; API only reachable via HTTPS.\n- **Audit Logs**: Simple `AuditEntity` table capturing create/update/delete actions for admin review.\n\n## Scalability & Performance\n- **Horizontal Scaling**: API stateless; can run multiple replicas behind a load balancer.\n- **Read Replicas**: PostgreSQL read replicas for heavy search queries; Prisma can route reads.\n- **Caching**: Redis reduces DB load for listing and search endpoints.\n- **Geospatial Index**: PostGIS GIST index on `location` enables fast radius queries.\n- **Background Processing**: Offloads email/SMS, payment webhook handling, and analytics aggregation.\n- **CDN**: Expo serves JS bundle via its CDN; static assets (images) stored in Supabase bucket with CDN edge caching.\n\n## Decision Log\n- **Expo Router** chosen for file‑based navigation and deep linking.\n- **TanStack React Query** for server‑state caching, deduplication, and background refetch.\n- **React Native Reanimated** for smooth UI gestures (e.g., map drag, modal transitions).\n- **NestJS** provides modularity, built‑in validation, and microservice‑ready structure.\n- **Prisma** offers type‑safe DB access and migrations; paired with PostGIS for location.\n- **Redis** selected for caching, rate‑limiting, and BullMQ broker due to maturity and performance.\n- **Supabase** used for auth (social providers) and optional storage to avoid building custom auth.\n- **Nx monorepo** enables code sharing (`libs/types`, `libs/ui`) and consistent tooling.\n- **Docker Compose** simplifies local dev and CI reproducibility.\n- **GitHub Actions + EAS Build** automates mobile build pipeline.\n\n--- End of Document ---",
  "summary": "A clean, layered monorepo architecture for Planity Clone using Expo React Native on the frontend and NestJS/Prisma/PostgreSQL on the backend, with Redis, BullMQ, Docker, and CI/CD pipelines for scalable, maintainable development."
}