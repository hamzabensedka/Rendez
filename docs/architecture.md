{
  "content": "# Planity Clone System Architecture\n\n## Overview\nThis document outlines the complete system architecture for the Planity Clone mobile application. The solution follows a clean architecture approach with clear separation of concerns, leveraging an Nx monorepo for scalable code organization, Expo for cross‑platform mobile development, and NestJS for a robust backend. Key technologies include TypeScript, Expo Router, TanStack React Query, React Native Reanimated, Prisma with PostgreSQL & PostGIS, Redis, Docker Compose, GitHub Actions, EAS Build, and Supabase for authentication/storage.\n\n## Tech Stack\n- **Mobile**: Expo (SDK 49), React Native, TypeScript, Expo Router, TanStack React Query, React Native Reanimated\n- **Backend**: NestJS, TypeScript, Prisma ORM, PostgreSQL (with PostGIS extension), Redis, BullMQ (for background jobs)\n- **DevOps**: Docker Compose, Nx, pnpm, GitHub Actions, EAS Build, Supabase (auth & storage), Jest (testing)\n- **Shared**: Nx workspace, TypeScript path aliases, shared design system, shared types & utilities\n\n## High-Level Architecture\nThe system is split into three main layers:\n1. **Client Layer** – Expo mobile app (guest & authenticated flows).\n2. **API Layer** – NestJS REST/GraphQL gateway exposing domain services.\n3. **Data Layer** – PostgreSQL (core data + spatial indexes via PostGIS), Redis (caching, rate‑limiting, job queue), Supabase (auth & file storage).\n\nCommunication:\n- Mobile ↔ API: HTTPS JSON (REST) with optional GraphQL endpoints; authentication via JWT (issued by Supabase or custom NestJS guard).\n- API ↔ Database: Prisma ORM; raw SQL for PostGIS queries.\n- API ↔ Redis: BullMQ job queue, caching layer.\n- Background Workers: NestJS modules processing BullMQ jobs (notifications, payment webhooks, slot recomputation).\n\n## Service Boundaries\n| Domain | Responsibility | Key Entities | Notes |\n|--------|----------------|--------------|-------|\n| **Auth** | User registration, login, password reset, social login (optional) | User, Session | Handled via Supabase Auth; NestJS guards validate JWT. |\n| **Business** | CRUD for businesses, categories, location data, opening hours | Business, Category, BusinessHours, Location (PostGIS point) | Supports geo‑queries for map search. |\n| **Service** | Services offered by businesses, pricing, duration | Service, Price, Duration | Linked to Business. |\n| **Booking** | Appointment creation, modification, cancellation, slot computation | Booking, TimeSlot, AppointmentStatus | Uses business schedule + existing bookings to compute available slots; updates in real‑time via Redis pub/sub or WebSocket (optional). |\n| **Payment** | Secure payment processing, webhooks, invoices | Payment, Invoice, PaymentMethod | Integrated with Stripe/PayPal via NestJS service; emits payment.success job. |\n| **Review** | Ratings, comments, business responses | Review, ReviewResponse | Linked to Business & Service. |\n| **Notification** | Email, push, in‑app alerts | NotificationTemplate, NotificationLog | Triggered by BullMQ jobs (booking.created, booking.cancelled, payment.success, etc.). |\n| **Provider Portal** | Business owner dashboard: manage profile, services, schedule, view bookings | BusinessOwnerProfile, Staff, Schedule | Separate NestJS module; same API but guarded by business‑owner role. |\n| **Admin Dashboard** | Platform‑wide moderation, user & business management | AdminUser, ModerationLog | Role‑based access (admin). |\n| **Search & Discovery** | Full‑text + geo search, filtering, sorting | SearchQuery, SearchResult | Uses PostgreSQL full‑text + PostGIS; cached in Redis for frequent queries. |\n| **Design System** | Shared UI components, theming, icons | UI Library (React Native) | Published as an Nx lib (`ui`). |\n\n## Folder Structure (Nx Monorepo)\n```\nplanity-clone/\n├── apps/\n│   ├── planity/               # Expo mobile app\n│   │   ├── src/\n│   │   │   ├── app/           # Expo Router routes (tabs, stacks)\n│   │   │   │   ├── (tabs)/\n│   │   │   │   │   ├── home/\n│   │   │   │   │   ├── search/\n│   │   │   │   │   ├── bookings/\n│   │   │   │   │   ├── profile/\n│   │   │   │   │   └── more/\n│   │   │   │   ├── auth/\n│   │   │   │   ├── business-detail/\n│   │   │   │   ├── booking-flow/\n│   │   │   │   ├── map/\n│   │   │   │   ├── favorites/\n│   │   │   │   ├── reviews/\n│   │   │   │   └── settings/\n│   │   │   ├── components/      # Shared app‑specific components (re‑exports from libs/ui)\n│   │   │   ├── hooks/           # Custom React Query hooks per domain\n│   │   │   ├── utils/\n│   │   │   ├── assets/\n│   │   │   └── types/           # App‑specific TS types (re‑exports from libs/shared/types)\n│   │   ├── app.json\n│   │   ├── eas.json\n│   │   └── tsconfig.json\n│   └── api/                   # NestJS backend\n│       ├── src/\n│       │   ├── auth/\n│       │   ├── business/\n│       │   ├── service/\n│       │   ├── booking/\n│       │   ├── payment/\n│       │   ├── review/\n│       │   ├── notification/\n│       │   ├── provider/\n│       │   ├── admin/\n│       │   ├── search/\n│       │   ├── common/          # Guards, DTOs, interceptors, exceptions\n│       │   ├── app.module.ts\n│       │   └── main.ts\n│       ├── test/\n│       ├── dockerfile\n│       └── tsconfig.json\n├── libs/\n│   ├── shared/\n│   │   ├── types/             # Domain‑agnostic TS interfaces (User, Business, Booking, etc.)\n│   │   ├── utils/             # Logging, validation helpers, date helpers\n│   │   └── config/              # Environment schema (zod)\n│   ├── ui/                    # React Native component library (styled with tailwind/rn)\n│   │   ├── atoms/\n│   │   ├── molecules/\n│   │   ├── organisms/\n│   │   └── themes/\n│   ├── design-system/         # Design tokens, spacing, typography, color palette\n│   ├── query/                 # Custom React Query wrappers, mutation helpers\n│   ├── reanimated/            # Shared worklets, animation configs\n│   └── supabase/              # Thin wrapper around supabase-js (auth, storage)\n├── tools/\n│   ├── generators/            # Nx schematics for libs, components\n│   └── lint/\n├── docker-compose.yml         # Services: postgres, redis, supabase (studio), api, mailhog (dev)\n├── nx.json\n├── package.json (pnpm workspace)\n└── README.md\n```\n\n## Mobile App (apps/planity)\n- **Routing**: Expo Router provides file‑based navigation; auth‑guarded routes redirect to login.\n- **State Management**: TanStack React Query handles server data; query keys follow domain pattern (e.g., `['businesses', id]`).\n- **Animations**: React Native Reanimated 2 for smooth transitions (modal booking, map marker animations).\n- **Offline/Fallback**: Query stale‑time & retry; Redis caching reduces API load.\n- **Build**: EAS Build configured for preview and production profiles; GitHub Actions runs `eas build --platform ios --profile preview` on pull‑request.\n\n## Backend API (apps/api)\n- **Architecture**: NestJS modules per domain; each module contains controllers, services, DTOs, entities (Prisma models).\n- **Validation**: class‑validator + class‑transformer; global validation pipe.\n- **Authentication**: Custom `JwtAuthGuard` validates JWT issued by Supabase; role‑based guards (`RolesGuard`) for provider/admin.\n- **Database**: Prisma schema defines models with `@relation`, `@unique`, `@index`, and PostGIS extensions via `db.raw()` for spatial queries.\n- **Caching**: Redis wrapper service; `@Cacheable()` decorator for frequent read‑only queries (business list, categories).\n- **Background Jobs**: BullMQ module; processors live in `jobs/` folder (e.g., `notification.processor.ts`, `slot-recalc.processor.ts`).\n- **Webhooks**: Stripe webhook endpoint verifies signature and enqueues `payment.success` job.\n- **Testing**: Jest + Supertest for controller tests; Prisma mock for unit tests.\n\n## Shared Libraries\n- **libs/shared/types**: Central source of truth for TypeScript interfaces; generated from Prisma schema via `prisma generate --generator=typescript` or manual sync.\n- **libs/ui**: Exported as a local npm package; storybook optional for visual regression.\n- **libs/query**: Provides `useQuery`/`useMutation` helpers with automatic invalidation patterns (e.g., after booking creation, invalidate `['business', id, 'slots']`).\n- **libs/reanimated**: Contains shared worklets (e.g., `usePressAnimation`, `useMapMarkerAnimation`).\n- **libs/supabase**: Wrapper exposing `supabaseClient` with retry logic; used for auth token refresh and file uploads (avatars, business photos).\n\n## Infrastructure & DevOps\n- **Docker Compose**: Defines `postgres` (with PostGIS extension), `redis`, `supabase/studio` (optional for local auth), `api`, `mailhog` (SMTP dev), and `nginx` (reverse proxy).\n- **CI/CD (GitHub Actions)**: \n  - Lint & type-check on PR (`pnpm lint`, `pnpm typecheck`).\n  - Run Jest unit & integration tests.\n  - Build Docker images for API and push to registry.\n  - Trigger EAS Build for mobile preview/production.\n  - Deploy API to a VPS/Kubernetes via `docker-compose pull && docker-compose up -d`.\n- **Environment Variables**: Managed via `.env` files; validated with zod in `libs/shared/config`.\n- **Monitoring**: Suggested integration of Sentry (frontend & backend) and Prometheus/Grafana for API metrics (not required for MVP).\n\n## Data Flow Examples\n1. **Guest Browse Businesses**\n   - Mobile → GET `/businesses` (cached in Redis) → API queries PostgreSQL with geo‑filter (if location provided) → returns list → React Query updates UI.\n2. **User Booking Flow**\n   - User selects service → mobile calls GET `/business/:id/slots?date=…` → API computes available slots using business schedule + existing bookings (read‑through Redis cache) → returns slots.\n   - On slot selection → POST `/bookings` with payload → API validates, creates Booking record, enqueues `booking.created` job → job sends confirmation email/push via Supabase/Redis.\n   - Optimistic UI update via React Query mutation.\n3. **Payment Processing**\n   - User submits payment details → mobile calls POST `/payments` → API creates Payment record, returns Stripe client secret → Stripe SDK confirms → Stripe webhook → API verifies, updates Payment status, enqueues `payment.success` job → job triggers notification and updates Booking status to `confirmed`.\n4. **Map‑Based Search**\n   - Mobile drags map → sends GET `/search?lat=…&lng=…&radius=…` → API uses PostGIS `ST_DWithin` to find businesses within radius, applies text/category filters → returns GeoJSON features → mobile renders markers via react-native-maps.\n\n## Conclusion\nThis architecture delivers a scalable, maintainable foundation for the Planity Clone. By separating concerns into distinct Nx libraries and NestJS modules, leveraging Expo for a unified mobile experience, and relying on proven technologies (Prisma, PostgreSQL+PostGIS, Redis, BullMQ, Supabase), the system can evolve to support additional features (e.g., AI‑based recommendations, multi‑currency payments) with minimal friction.\n\n---\n*Generated by Jordan, Staff Software Engineer – Architecture Focus*\n",
  "summary": "A clean, Nx‑based monorepo architecture separating Expo mobile, NestJS API, PostgreSQL+PostGIS data layer, Redis caching/jobs, and Supabase auth/storage, with clearly defined service boundaries and folder structure."
}