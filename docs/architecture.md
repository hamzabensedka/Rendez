{
  "content": "# Planity Clone System Architecture\n\n## Overview\nThis document describes the complete system architecture for the Planity Clone mobile application. The solution follows a clean, layered architecture with clear separation of concerns, leveraging a monorepo managed by Nx, Expo for the mobile client, NestJS for the backend, Prisma with PostgreSQL/PostGIS for data persistence, Redis for caching, and a suite of DevOps tools for CI/CD, testing, and deployment.\n\n## Tech Stack\n- **Mobile**: Expo (SDK 50), React Native, TypeScript, Expo Router, TanStack React Query, React Native Reanimated\n- **Backend**: NestJS (Node.js), TypeScript, Prisma ORM, PostgreSQL + PostGIS extension, Redis, BullMQ (for background jobs)\n- **Infrastructure**: Docker Compose (local dev), GitHub Actions (CI), EAS Build (Expo builds), Supabase (optional auth/storage fallback), Nx (monorepo tooling), pnpm (package manager)\n- **Testing**: Jest (unit/integration), React Native Testing Library, Supertest (NestJS)\n- **Design System**: Custom token‑based design system with shared TypeScript types across mobile and backend\n\n## High-Level Architecture\nThe system is split into three main layers:\n1. **Presentation Layer** – Expo mobile app (UI, navigation, state).\n2. **Application Layer** – NestJS API server (controllers, services, validation).\n3. **Data Layer** – Prisma models interacting with PostgreSQL/PostGIS; Redis for caching and job queues.\n\nCommunication:\n- Mobile ↔ API: REST/GraphQL (REST chosen for simplicity) over HTTPS.\n- API ↔ Database: Prisma client (SQL).\n- API ↔ Redis: ioredis for caching and BullMQ job processing.\n- Background workers: Separate NestJS processes (same codebase) consuming BullMQ queues.\n\n## Folder Structure (Nx Monorepo)\n```\nplanity-clone/\n├── apps/\n│   ├── mobile/                 # Expo React Native app\n│   │   ├── src/\n│   │   │   ├── assets/\n│   │   │   ├── components/\n│   │   │   │   ├── ui/                 # Reusable UI primitives (Button, Input, etc.)\n│   │   │   │   └── layout/\n│   │   │   ├── screens/\n│   │   │   │   ├── (tabs)/\n│   │   │   │   │   ├── home.tsx\n│   │   │   │   │   ├── search.tsx\n│   │   │   │   │   ├── map.tsx\n│   │   │   │   │   ├── bookings.tsx\n│   │   │   │   │   └── profile.tsx\n│   │   │   │   ├── auth/\n│   │   │   │   │   ├── login.tsx\n│   │   │   │   │   └── register.tsx\n│   │   │   │   ├── business/\n│   │   │   │   │   ├── detail.tsx\n│   │   │   │   │   └── list.tsx\n│   │   │   │   └── provider/\n│   │   │   │       ├── dashboard.tsx\n│   │   │   │       └── appointments.tsx\n│   │   │   ├── hooks/\n│   │   │   │   ├── useAuth.ts\n│   │   │   │   ├── useQuery.ts (wrappers for React Query)\n│   │   │   │   └── useBooking.ts\n│   │   │   ├── navigation/\n│   │   │   │   └── root.tsx (Expo Router config)\n│   │   │   ├── store/\n│   │   │   │   └── index.ts (React Query client setup)\n│   │   │   ├── types/\n│   │   │   │   └── shared.ts (re‑exported from libs/types)\n│   │   │   ├── utils/\n│   │   │   │   ├── geolocation.ts\n│   │   │   │   └── formatting.ts\n│   │   │   ├── theme/\n│   │   │   │   └── tokens.ts (design tokens)\n│   │   │   └── App.tsx\n│   │   ├── app.json\n│   │   ├── eas.json\n│   │   └── tsconfig.json\n│   └── api/                    # NestJS backend\n│       ├── src/\n│       │   ├── auth/\n│       │   │   ├── auth.controller.ts\n│       │   │   ├── auth.service.ts\n│       │   │   └── dto/\n│       │   │       ├── login.dto.ts\n│       │   │       └── register.dto.ts\n│       │   ├── businesses/\n│       │   │   ├── businesses.controller.ts\n│       │   │   ├── businesses.service.ts\n│       │   │   ├── dto/\n│       │   │   │   ├── create-business.dto.ts\n│       │   │   │   └── search-business.dto.ts\n│       │   │   └── entities/\n│       │   │       ├── business.entity.ts\n│       │   │       └── service.entity.ts\n│       │   ├── bookings/\n│       │   │   ├── bookings.controller.ts\n│       │   │   ├── bookings.service.ts\n│       │   │   ├── dto/\n│       │   │   │   ├── create-booking.dto.ts\n│       │   │   │   └── update-booking.dto.ts\n│       │   │   └── entities/\n│       │   │       ├── booking.entity.ts\n│       │   │       └── appointment-slot.entity.ts\n│       │   ├── users/\n│       │   │   ├── users.controller.ts\n│       │   │   ├── users.service.ts\n│       │   │   └── entities/user.entity.ts\n│       │   ├── reviews/\n│       │   │   ├── reviews.controller.ts\n│       │   │   ├── reviews.service.ts\n│       │   │   └── entities/review.entity.ts\n│       │   ├── favorites/\n│       │   │   ├── favorites.controller.ts\n│       │   │   └── favorites.service.ts\n│       │   ├── notifications/\n│       │   │   ├── notifications.controller.ts\n│       │   │   └── notifications.service.ts\n│       │   ├── payments/\n│       │   │   ├── payments.controller.ts\n│       │   │   └── payments.service.ts\n│       │   ├── common/\n│       │   │   ├── guards/\n│       │   │   │   ├── auth.guard.ts\n│       │   │   │   └── roles.guard.ts\n│       │   │   ├── interceptors/\n│       │   │   │   └── logging.interceptor.ts\n│       │   │   ├── pipes/\n│       │   │   │   └── validation.pipe.ts\n│       │   │   └── exceptions/\n│       │   │       └── http-exception.filter.ts\n│       │   ├── app.module.ts\n│       │   └── main.ts\n│       ├── test/\n│       │   ├── auth/\n│       │   └── businesses/\n│       ├── prisma/\n│       │   ├── schema.prisma\n│       │   └── migrations/\n│       ├── Dockerfile\n│       └── tsconfig.json\n├── libs/\n│   ├── design-system/\n│   │   ├── src/\n│   │   │   ├── tokens.ts\n│   │   │   ├── primitives/\n│   │   │   └── index.ts\n│   │   └── tsconfig.json\n│   ├── types/\n│   │   ├── src/\n│   │   │   ├── auth.ts\n│   │   │   ├── business.ts\n│   │   │   ├── booking.ts\n│   │   │   ├── user.ts\n│   │   │   └── index.ts\n│   │   └── tsconfig.json\n│   └── utils/\n│       ├── src/\n│       │   ├── geolocation.ts\n│       │   └── formatting.ts\n│       └── tsconfig.json\n├── prisma/\n│   └── schema.prisma (shared with api)\n├── docker-compose.yml\n├── nx.json\n├── package.json\n└── tsconfig.base.json\n```\n\n## Mobile App (Expo)\n- **Routing**: Expo Router provides file‑based navigation; auth‑guarded routes redirect to login.\n- **State Management**: TanStack React Query handles server state (caching, background updates, optimistic updates).\n- **Animations**: React Native Reanimated 2 for smooth transitions (e.g., map marker drag, modal presentations).\n- **Authentication**: JWT stored securely in Expo’s SecureStore; refresh token rotation via API.\n- **Guest Mode**: Public routes (home, search, map, business detail) accessible without auth; booking flow triggers auth modal.\n- **Map Integration**: Uses `@react-native-maps` with custom markers showing business info; supports user location and radius search.\n- **Design System**: Consumes `libs/design-system` for consistent spacing, typography, color tokens, and theme (light/dark).\n- **Shared Types**: Re‑exports `libs/types` to ensure type safety between mobile and backend (e.g., Business, Booking, User).\n- **Background Fetch**: Not used; real‑time updates rely on React Query refetch intervals and push notifications.\n\n## Backend (NestJS)\n- **Modularity**: Each domain (auth, businesses, bookings, users, reviews, favorites, notifications, payments) is a NestJS module.\n- **Validation**: Class‑validator + custom validation pipes for DTOs.\n- **Authentication**: JWT strategy (access token 15 min, refresh token 7 days); role‑based guards (USER, PROVIDER, ADMIN).\n- **Authorization**: Roles attached to user entity; providers can only manage their own businesses.\n- **Prisma Service**: Centralized PrismaClient with graceful shutdown handling.\n- **PostGIS Integration**: Custom Prisma provider adds spatial indexes; queries use `ST_DistanceSphere` for radius search.\n- **Caching**: Redis wrapper service; caches expensive queries (e.g., business list, popular categories) with TTL.\n- **Background Jobs**: BullMQ queues for:\n  - Sending notifications (email/push).\n  - Recalculating availability slots after booking/cancellation.\n  - Generating daily reports.\n  Workers are separate NestJS processes sharing the same codebase.\n- **Payment Integration**: Stubbed service ready for Stripe/PayPal; creates payment intent, confirms via webhook.\n- **Error Handling**: Global exception filter transforms Prisma errors to appropriate HTTP responses.\n- **API Versioning**: Prefix `/api/v1/`; supports future versioning.\n- **Documentation**: Auto‑generated Swagger UI at `/api/docs`.\n\n## Database & Caching\n- **PostgreSQL** with **PostGIS** extension for geospatial queries.\n- **Prisma Schema** (simplified):\n```prisma\nmodel User {\n  id            String   @id @default(uuid())\n  email         String   @unique\n  password      String\n  role          Role     @default(USER)\n  profile       Profile?\n  businesses    Business[]\n  bookings      Booking[]\n  reviews       Review[]\n  favorites     Favorite[]\n  createdAt     DateTime @default(now())\n}\nmodel Business {\n  id            String   @id @default(uuid())\n  name          String\n  category      String\n  description   String?\n  address       String\n  lat           Float\n  lng           Float\n  services      Service[]\n  bookings      Booking[]\n  reviews       Review[]\n  ownerId       String\n  owner         User     @relation(fields: [ownerId], references: [id])\n  createdAt     DateTime @default(now())\n}\nmodel Service {\n  id            String   @id @default(uuid())\n  name          String\n  price         Float\n  durationInt   Int      // minutes\n  businessId    String\n  business      Business @relation(fields: [businessId], references: [id])\n}\nmodel Booking {\n  id            String   @id @default(uuid())\n  userId        String\n  user          User     @relation(fields: [userId], references: [id])\n  serviceId     String\n  service       Service  @relation(fields: [serviceId], references: [id])\n  startTime     DateTime\n  endTime       DateTime\n  status        BookingStatus\n  paymentId     String?\n  createdAt     DateTime @default(now())\n}\nmodel Favorite {\n  id        String   @id @default(uuid())\n  userId    String\n  user      User     @relation(fields: [userId], references: [id])\n  businessId String\n  business   Business @relation(fields: [businessId], references: [id])\n  @@unique([userId, businessId])\n}\n```\n- **Redis**: Stores:\n  - Cached business search results (key: `search:{hash}`).\n  - Session refresh tokens (optional).\n  - BullMQ job queues.\n\n## DevOps & CI/CD\n- **Local Development**: Docker Compose spins up PostgreSQL (with PostGIS), Redis, and NestJS API; Expo runs via `expo start`.\n- **GitHub Actions**:\n  - Lint (ESLint, Prettier).\n  - Unit tests (Jest) for mobile and backend.\n  - Build Docker image for API.\n  - Publish Expo build to EAS (internal distribution).\n  - Deploy API to a staging environment (e.g., Render, Fly.io) on push to `main`.\n- **EAS Build**: Configured for iOS and Android with OTA updates disabled (store builds).\n- **Supabase** (optional): Used only for auth fallback or file storage if needed; primary auth is custom JWT.\n- **Monitoring**: Health check endpoint (`/health`); logs shipped to a centralized service (e.g., Loki) via Docker logging driver.\n\n## Service Boundaries\n| Boundary | Responsibility | Technologies |\n|----------|----------------|--------------|\n| **Auth** | User registration, login, token issuance, role management | NestJS (Passport JWT), Prisma, Redis (refresh token store) |\n| **Business** | CRUD for businesses, geospatial search, category management | NestJS, Prisma/PostGIS, Redis cache |\n| **Booking** | Slot computation, booking creation, cancellation, rescheduling | NestJS, BullMQ (availability recomputation), Prisma |\n| **User Profile** | Profile read/update, preferences | NestJS, Prisma |\n| **Reviews & Ratings** | Create review, aggregate rating, display | NestJS, Prisma |\n| **Favorites** | Toggle favorite, list favorites | NestJS, Prisma |\n| **Notifications** | Send push/email, manage user preferences | NestJS, BullMQ (email/sms provider), Redis (prefs) |\n| **Payments** | Create payment intent, confirm webhook, receipts | NestJS (Stripe SDK), Prisma |\n| **Provider Portal** | Business owners manage their services, bookings, schedule | NestJS (role‑guarded), Prisma |\n| **Admin Dashboard** | Global oversight: users, businesses, analytics | NestJS (role‑guarded), Prisma |\n| **Mobile UI** | Presentation, navigation, local state, offline‑first caching | Expo, React Query, Reanimated, Design System |\n| **Shared Types** | Domain‑agnostic TypeScript interfaces | Nx lib (`libs/types`) |\n| **Design System** | UI primitives, theming, tokens | Nx lib (`libs/design-system`) |\n\n## Data Models (Key Entities)\n- **User**: id, email, passwordHash, role (USER|PROVIDER|ADMIN), profile (name, phone, avatar), timestamps.\n- **Business**: id, name, category, description, address, lat/lng (PostGIS point), ownerId, timestamps.\n- **Service**: id, name, price, duration, businessId.\n- **Booking**: id, userId, serviceId, startTime, endTime, status (PENDING|CONFIRMED|CANCELLED|COMPLETED), paymentId, timestamps.\n- **Review**: id, userId, businessId, rating (1‑5), comment, timestamps.\n- **Favorite**: id, userId, businessId, unique constraint.\n- **Notification**: id, userId, type, payload, readFlag, timestamps.\n\n## API Contracts (REST)\nAll responses JSON; errors follow `{ message: string, errors?: any[] }`.\nSample endpoints:\n- `POST /auth/register` – body: `{email, password}` → `{accessToken, refreshToken}`.\n- `POST /auth/login` – same.\n- `GET /businesses?lat=&lng=&radius=&category=&search=` → paginated list with distance.\n- `GET /businesses/:id` → business detail with services.\n- `POST /bookings` → `{serviceId, startTime}` → booking object.\n- `GET /users/me/bookings` → user’s bookings.\n- `POST /favorites/:businessId` → toggle.\n- `GET /notifications/me` → list with unread count.\n- `POST /payments/create-intent` → clientSecret.\n- `POST /payments/webhook` → Stripe endpoint.\n\n## State Management (Mobile)\n- **React Query** provides:\n  - Automatic caching and deduplication.\n  - Background refetch (configurable per query).\n  - Optimistic updates for booking creation/favorite toggle.\n  - Query invalidation on mutation success (e.g., after booking, invalidate `businesses` and `userBookings`).\n- **Auth State**: Managed via a custom `useAuth` hook that reads/writes to SecureStore and provides login/logout functions.\n- **UI State**: Local component state (React `useState`) for form inputs, modal visibility, map region.\n\n## Design System & Shared Types\n- **Tokens**: Defined in `libs/design-system/src/tokens.ts` (color palette, spacing scale, font sizes, radius). Exported as a TypeScript object consumed by styled‑components or native‑wind.\n- **Primitives**: Button, Input, Card, Avatar, Badge – all accept `variant` and `size` props based on tokens.\n- **Shared Types**: `libs/types` contains interfaces like `BusinessDTO`, `BookingForm`, `UserProfile`. Both mobile and backend import from this lib, guaranteeing contract alignment.\n\n## Testing Strategy\n- **Unit Tests**: Jest tests for services, utilities, and React components (using React Native Testing Library). Aim for ≥80% coverage.\n- **Integration Tests**: API endpoints tested with Supertest against a temporary PostgreSQL instance (Docker).\n- **E2E (Mobile)**: Detox scripts for critical flows (login → search → book → confirmation).\n- **Contract Tests**: Pact or manual snapshot of API responses to ensure mobile and backend stay in sync.\n- **CI Gate**: All tests must pass before merging to `main`.\n\n## Deployment & Scaling\n- **API**: Docker image deployed to a container platform (e.g., AWS ECS Fargate, Google Cloud Run). Horizontal pod autoscaling based on CPU/RAM and queue length (BullMQ active jobs).\n- **Database**: Managed PostgreSQL (e.g., Amazon RDS) with read replica for heavy search queries; PostGIS extension enabled.\n- **Redis**: Elasticache (Redis) for caching and pub/sub; configured with persistence (AOF) for job queue durability.\n- **Mobile**: Distributed via Apple App Store and Google Play; OTA updates disabled to ensure store compliance.\n- **Monitoring & Alerts**: Prometheus + Grafana for infrastructure; application logs sent to ELK; Sentry for error tracking.\n- **Backup**: Daily snapshots of PostgreSQL and Redis; point‑in‑time recovery tested quarterly.\n\n## Conclusion\nThis architecture provides a scalable, maintainable foundation for the Planity Clone mobile app. By separating concerns into distinct modules, leveraging a monorepo for code sharing, and using proven technologies (Expo, NestJS, Prisma, Redis), the system can evolve to support new features (e.g., video consultations, multi‑currency payments) while keeping development velocity high.\n---\n*Document version: 1.0 – Generated by Jordan (Staff Software Engineer)*\n",
  "summary": "A concise, layered monorepo architecture for Planity Clone using Expo, React Native, NestJS, Prisma, PostgreSQL/PostGIS, Redis, and Nx with clear service boundaries, folder structure, and DevOps practices."
}