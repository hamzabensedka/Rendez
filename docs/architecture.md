{
  "content": "# Planity Clone — System Architecture\n\n## 1. High-Level Overview\nPlanity Clone is a mobile-first beauty & wellness booking platform. The system is composed of:\n- **Mobile App** (Expo + React Native + TypeScript) for customers and providers (basic).\n- **Backend API** (NestJS + Prisma) with PostgreSQL/PostGIS and Redis.\n- **Background Jobs** via BullMQ (Redis) for notifications, payments, availability sync.\n- **Web Portals** (Provider/Admin) served via Expo Web or separate Next/Nest views (basic provider portal in scope via responsive web).\n- **Infra** via Docker Compose, GitHub Actions CI, EAS Build, Supabase (Auth/Storage optional), Jest for tests.\n\nMonorepo managed by **Nx** + **pnpm** with clear app/lib boundaries.\n\n## 2. Monorepo Layout (Nx + pnpm)\n```\nplanity-clone/\n├── apps/\n│   ├── mobile/                # Expo RN app (customers + provider basic)\n│   ├── api/                   # NestJS backend\n│   ├── provider-web/          # Optional Expo Web provider portal\n│   └── admin-web/             # Admin dashboard (React Web)\n├── libs/\n│   ├── types/                 # @planity/types (shared TS interfaces)\n│   ├── ui/                    # @planity/ui (RN components, Reanimated)\n│   ├── design-system/         # tokens, colors, typography\n│   ├── api-client/            # typed fetch wrapper for mobile\n│   ├── prisma/                # Prisma schema + client\n│   ├── redis/                 # Redis/BullMQ config\n│   └── config/                # env, eslint, tsconfig bases\n├── tools/\n│   ├── docker/                # Dockerfiles, compose\n│   └── github/                # workflows\n├── docs/\n│   ├── product.md\n│   └── architecture.md\n├── package.json\n├── pnpm-workspace.yaml\n├── nx.json\n└── docker-compose.yml\n```\n\n## 3. Mobile App (apps/mobile)\n### Stack\nExpo, React Native, TypeScript, Expo Router, TanStack React Query, React Native Reanimated.\n### Folder Structure\n```\napps/mobile/\n├── app/                       # Expo Router routes\n│   ├── (guest)/               # home, explore, search\n│   ├── (auth)/                # login, register, otp\n│   ├── (tabs)/                # main authed tabs\n│   │   ├── browse/\n│   │   ├── bookings/\n│   │   └── profile/\n│   ├── business/[id].tsx\n│   ├── map.tsx\n│   └── booking/[businessId].tsx\n├── components/                # feature components\n├── hooks/                     # useAuth, useBookings (React Query)\n├── lib/                       # query keys, api client\n├── assets/\n└── eas.json\n```\n### Boundaries\n- Only consumes `/api` via `@planity/api-client`.\n- No business logic; uses React Query for caching, Reanimated for transitions.\n\n## 4. Backend API (apps/api)\n### Stack\nNestJS, Prisma, PostgreSQL + PostGIS, Redis, BullMQ.\n### Folder Structure\n```\napps/api/\n├── src/\n│   ├── main.ts\n│   ├── app.module.ts\n│   ├── auth/                  # JWT, OTP, OAuth, guards\n│   ├── users/\n│   ├── businesses/\n│   ├── services/\n│   ├── categories/\n│   ├── appointments/\n│   ├── availability/          # slot computation\n│   ├── reviews/\n│   ├── payments/              # Stripe/PayPal\n│   ├── notifications/         # push/email\n│   ├── favorites/\n│   ├── search/                # PostGIS queries\n│   ├── admin/\n│   ├── jobs/                  # BullMQ processors\n│   └── common/                # pipes, filters, decorators\n└── test/\n```\n### Service Boundaries\n- `auth` issues JWT, integrates Supabase Auth if used.\n- `availability` pure logic from working hours + appointments.\n- `search` uses PostGIS `ST_DWithin` for distance.\n- `payments` atomic with `appointments` via Prisma transaction.\n- `jobs` enqueues to Redis; separate workers.\n\n## 5. Data Model (Prisma + PostGIS)\n- User (id, role, email, phone, avatar)\n- Business (id, geo geometry(Point), address, ownerId)\n- Category (id, parentId nullable)\n- Service (id, businessId, categoryId, duration, price)\n- Appointment (id, userId, serviceId, start, end, status)\n- Review (id, appointmentId unique, rating, comment)\n- Favorite (userId, businessId)\n- Payment (id, appointmentId, stripeId, status)\n- AvailabilityRule (businessId, day, start, end, break)\n\nPostGIS extension for `Business.geo`.\n\n## 6. Background Jobs (BullMQ)\nQueues: `notifications`, `payments-sync`, `availability-sync`.\nWorkers in `apps/api/src/jobs`. Redis holds queue. Retry 3x, DLQ logged.\n\n## 7. Infra & CI\n- `docker-compose.yml`: postgres (postgis), redis, api, worker.\n- GitHub Actions: pnpm install, Nx affected build/test, EAS Build on mobile tag.\n- EAS Build profiles in `eas.json`.\n- Supabase for Auth/Storage optional; fallback to Nest Auth.\n\n## 8. Cross-Cutting\n- `@planity/types` imported by mobile, api, web.\n- `@planity/ui` shared RN components.\n- Jest at lib/app level; Nx runs affected.\n\n## 9. Priority Mapping\n- P0: auth, search, business detail, booking, availability, payment, notifications, jobs, provider basic → implemented first.\n- P1/P2: incremental via feature libs.\n\n## 10. Security\nJWT in secure store (Expo SecureStore). RBAC via Nest guards (Customer/Provider/Admin).",
  "summary": "Monorepo architecture for Planity Clone using Nx/pnpm with Expo mobile, NestJS API, PostGIS, Redis/BullMQ, and clear service boundaries mapped to product features."
}