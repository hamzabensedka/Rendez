{
  "content": "# Planity Clone System Architecture\n\n## Overview\nThis document describes the complete system architecture for the Planity Clone mobile application, covering the mobile client, backend services, data storage, caching, job processing, DevOps, and testing strategies. The architecture follows clean‑architecture principles, separates concerns into well‑defined layers, and leverages an Nx monorepo for scalable development.\n\n## Tech Stack\n- **Mobile**: Expo (React Native), TypeScript, Expo Router, TanStack React Query, React Native Reanimated\n- **Backend**: NestJS (Node.js), Prisma ORM, PostgreSQL with PostGIS extension, Redis, BullMQ (job queue)\n- **Monorepo Tooling**: Nx, pnpm\n- **DevOps**: Docker Compose, GitHub Actions, EAS Build (for mobile), Supabase (optional auth/fallback), Jest (testing)\n- **Design System**: Shared UI component library built with React Native Reanimated & Tailwind‑like styling\n\n## High-Level Architecture\n```\n+-------------------+      +---------------------+      +-------------------+\n|   Mobile App      |<---->|   API Gateway (Nest) |<---->|   PostgreSQL +    |\n| (Expo/React Native)|      |   (REST/WS)         |      |   PostGIS         |\n+-------------------+      +---------------------+      +-------------------+\n                                 ^          ^\n                                 |          |\n                         +-----------------+-----------------+\n                         |                                 |\n               +-------------------+            +-------------------+\n               |   Redis Cache     |            |   BullMQ Workers  |\n               +-------------------+            +-------------------+\n                         |                                 |\n                         +-----------------+-----------------+\n                                           |\n                                   +-------------------+\n                                   |   File Storage (S3/minio) |\n                                   +-------------------+\n```\nThe mobile app communicates exclusively with the NestJS API via REST endpoints and optional WebSocket connections for real‑time updates. The API handles business logic, validation, authorization, and interacts with PostgreSQL (using Prisma) for persistent storage. Redis provides caching for frequently accessed data (e.g., nearby business listings, session tokens) and acts as the broker for BullMQ background jobs. File storage (e.g., profile pictures, business logos) is delegated to an S3‑compatible service.\n\n## Folder Structure (Nx Monorepo)\n```\nplanity-clone/\n├── apps/\n│   ├── mobile/                 # Expo React Native app\n│   │   ├── src/\n│   │   │   ├── assets/\n│   │   │   ├── components/     # Shared UI components (re‑exported from libs/ui)\n│   │   │   ├── hooks/\n│   │   │   ├── navigation/     # Expo Router config\n│   │   │   ├── screens/\n│   │   │   │   ├── auth/\n│   │   │   │   ├── browse/\n│   │   │   │   ├── business/\n│   │   │   │   ├── booking/\n│   │   │   │   ├── profile/\n│   │   │   │   ├── favorites/\n│   │   │   │   └── provider/\n│   │   │   ├── store/            # React Query wrappers & query keys\n│   │   │   ├── utils/\n│   │   │   └── theme/          # Design system tokens\n│   │   ├── app.json\n│   │   └── eas.json\n│   ├── api/                    # NestJS backend\n│   │   ├── src/\n│   │   │   ├── auth/\n│   │   │   ├── users/\n│   │   │   ├── businesses/\n│   │   │   ├── services/\n│   │   │   ├── bookings/\n│   │   │   ├── appointments/\n│   │   │   ├── favorites/\n│   │   │   ├── reviews/\n│   │   │   ├── payments/\n│   │   │   ├── notifications/\n│   │   │   ├── provider-portal/\n│   │   │   ├── admin/\n│   │   │   ├── common/\n│   │   │   │   ├── guards/\n│   │   │   │   ├── interceptors/\n│   │   │   │   ├── dto/\n│   │   │   │   └── pipes/\n│   │   │   ├── prisma/\n│   │   │   │   ├── migrations/\n│   │   │   │   └── schema.prisma\n│   │   │   ├── bullmq/\n│   │   │   │   ├── processors/\n│   │   │   │   └── queue.ts\n│   │   │   ├── websocket/\n│   │   │   ├── app.module.ts\n│   │   │   └── main.ts\n│   │   ├── test/\n│   │   └── dockerfile\n│   └── web/                    # Provider portal & Admin dashboard (React Web, optional)\n│       └── src/                # Similar structure to mobile but using React Router\n├── libs/\n│   ├── shared-types/           # TypeScript interfaces & enums used across mobile/api/web\n│   ├── ui/                     # Design system components (buttons, inputs, cards, etc.)\n│   ├── utils/                  # Logging, date helpers, geo utilities (haversine, PostGIS helpers)\n│   ├── data-access/            # Thin wrapper around Prisma client (used only in API)\n│   ├── test-helpers/           # Jest utilities, mock factories\n│   └── eslint-config/          # Shared ESLint rules\n├── tools/\n│   ├── generators/             # Nx schematics for feature modules\n│   └── scripts/\n├── docker-compose.yml\n├── nx.json\n├── package.json\n└── README.md\n```\n\n## Service Boundaries\n### Mobile Client (`apps/mobile`)\n- **Presentation Layer**: Screens built with Expo Router; navigation is file‑based.\n- **State Management**: TanStack React Query handles server state (caching, background refetch).\n- **Animations**: React Native Reanimated for gesture‑based interactions (e.g., map drag, swipe actions).\n- **Offline‑First Queries**: Query keys include pagination params; stale‑time configured per endpoint.\n- **Design System**: Consumes `libs/ui` and `libs/shared-types` for consistent styling and typed props.\n- **Authentication**: Stores JWT in SecureStore; refresh token flow handled via React Query.\n- **Map Integration**: Uses Expo‑Google‑Maps (or Mapbox) with clustering; location updates via BackgroundFetch (optional).\n\n### API (`apps/api`)\nThe NestJS application is divided into feature modules, each encapsulating its own controllers, services, DTOs, guards, and entities. Modules communicate through well‑defined service interfaces; shared logic lives in `libs/data-access` and `libs/utils`.\n\n| Module | Responsibility |\n|--------|----------------|\n| **Auth** | Registration, login, social auth, email verification, JWT issuance, password reset |\n| **Users** | CRUD for user profiles, role management (customer/provider/admin) |\n| **Businesses** | Business CRUD, geo‑search (PostGIS), category assignment, media upload |\n| **Services** | Service catalog tied to a business, duration, price, staff assignment |\n| **Bookings** | Booking creation, validation against business schedule, conflict detection |\n| **Appointments** | Confirmed bookings, status tracking (pending, confirmed, completed, cancelled) |\n| **Favorites** | Many‑to‑many relation between users and businesses |\n| **Reviews** | Rating, comment, moderation, business response |\n| **Payments** | Integration with payment gateway (Stripe/PayPal), webhook handling, invoice generation |\n| **Notifications** | Template‑based email/SMS/push notifications; uses BullMQ jobs |\n| **Provider Portal** | Role‑guarded endpoints for providers to manage schedule, services, view bookings |\n| **Admin** | Role‑guarded endpoints for admins to oversee users, businesses, analytics |\n| **Common** | Guards (JwtAuthGuard, RolesGuard), interceptors (logging, transformation), DTO base classes, exception filters |\n| **Prisma** | Single source of truth for DB schema; migrations managed via Nx affected commands |\n| **BullMQ** | Queues: `email`, `sms`, `push`, `availability-update`, `payment-reconciliation`; processors defined in `bullmq/processors/` |\n| **WebSocket Gateway** | Real‑time updates for appointment status changes, new reviews, provider availability (uses Redis adapter for scaling) |\n\n### Shared Libraries\n- **shared-types**: Defines DTOs, entity shapes, enums (e.g., `BookingStatus`, `UserRole`). Imported by both mobile (via TypeScript paths) and API to guarantee contract consistency.\n- **ui**: Reusable, themeable components (Button, Input, Card, Modal, MapMarker). Styled with `tailwindcss-rn` or similar; dark‑mode support.\n- **utils**: Geo helpers (distance calculation, bounding box for PostGIS queries), date‑time utilities (slot generation, timezone handling), logging wrapper (pino).\n- **data-access**: Thin wrapper that exposes typed Prisma client methods; isolates API from ORM changes.\n- **test-helpers**: Factory functions for creating test entities, mock request/response objects.\n\n## Data Layer\n- **Primary Store**: PostgreSQL 15 with PostGIS extension.\n  - Tables: `users`, `businesses`, `services`, `bookings`, `appointments`, `favorites`, `reviews`, `payment_transactions`, `notifications`.\n  - Spatial index on `businesses.location` (GEOGRAPHY(Point, 4326)) for efficient `ST_DWithin` queries.\n- **Caching Layer**: Redis 7.\n  - Session tokens (short‑TTL).\n  - Geo‑query results (nearby businesses) cached per user location + radius (TTL 1‑5 min).\n  - Rate‑limit counters.\n- **File Storage**: S3‑compatible bucket (or MinIO in dev) for user avatars, business logos, service images; URLs stored in DB.\n- **Backup Strategy**: Daily logical dump (pg_dump) + WAL archiving; snapshots stored in object storage.\n\n## Communication Protocols\n- **REST**: JSON over HTTP/2; versioned via URL (`/api/v1/`).\n- **WebSocket**: NestJS Gateway with `@SubscribeMessage` for real‑time events; uses Redis IoAdapter for horizontal scaling.\n- **GraphQL**: Not used; REST keeps tooling simple and aligns with React Query.\n- **Internal Calls**: Service‑to‑service communication within the NestJS monolith uses direct method calls (no extra latency).\n\n## Caching & Real-time\n- **React Query** on mobile: stale‑time 1 min for lists, 5 min for details; background refetch on focus.\n- **Server‑Side**: Redis caches results of expensive geo‑queries and aggregated stats (e.g., average rating). Cache invalidation triggered via BullMQ jobs after relevant mutations (e.g., new review, booking).\n- **WebSocket Events**:\n  - `appointment:updated` (sent to customer & provider)\n  - `review:created` (sent to business page subscribers)\n  - `provider:availability-changed` (sent to map view subscribers)\n\n## Background Jobs (BullMQ)\n- **Queues**:\n  1. `email` – send verification, booking confirmation, password reset.\n  2. `sms` – optional OTP or appointment reminders.\n  3. `push` – Firebase/APNs push notifications.\n  4. `availability-update` – recalculate and cache available slots when a provider modifies schedule or a booking changes.\n  5. `payment-reconciliation` – verify webhook statuses, update payment records.\n- **Processors**: Located in `apps/api/src/bullmq/processors/`; each processor is a simple class injecting relevant services (e.g., `MailService`, `PrismaService`).\n- **Rate Limiting & Retries**: Configured with exponential backoff, max 3 attempts.\n- **Monitoring**: BullMQ dashboard exposed behind admin auth; metrics forwarded to Prometheus via `@nestjs/terminus`.\n\n## DevOps & CI/CD\n### Docker Compose (local dev)\n```yaml\nservices:\n  postgres:\n    image: postgis/postgis:15\n    environment:\n      POSTGRES_USER: ${POSTGRES_USER}\n      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}\n      POSTGRES_DB: ${POSTGRES_DB}\n    volumes:\n      - pgdata:/var/lib/postgresql/data\n    ports:\n      - \"5432:5432\"\n  redis:\n    image: redis:7-alpine\n    ports:\n      - \"6379:6379\"\n  api:\n    build: ./apps/api\n    env_file: .env\n    ports:\n      - \"3000:3000\"\n    depends_on: [postgres, redis]\n  mobile:\n    # Expo dev server; run via `npx expo start` locally\n    # In CI, EAS Build handles the build\n```\n\n### GitHub Actions Workflow\n- **lint**: `nx run-many --target=lint --all`\n- **test**: `nx run-many --target=test --all` (includes unit & integration tests)\n- **build**:\n  - Mobile: `eas build --platform all --auto-submit` (triggered on release tag)\n  - API: Docker image built and pushed to registry (e.g., GHCR)\n- **deploy**:\n  - API: Deploy to AWS ECS Fargate or Render via `docker-compose.prod.yml` (blue/green).\n  - Mobile: Distribute via TestFlight/Google Play Internal Test after EAS Build.\n- **notify**: Slack/webhook on success/failure.\n\n### Environment Variables (example)\n```\nPOSTGRES_USER=postgres\nPOSTGRES_PASSWORD=secret\nPOSTGRES_DB=planity\nJWT_SECRET=... \nREDIS_URL=redis://redis:6379\nMAIL_HOST=smtp.mailtrap.io\nMAIL_PORT=2525\nMAIL_USER=... \nMAIL_PASS=... \nPAYMENT_GATEWAY_STRIPE_KEY=... \n```\n\n## Testing Strategy\n- **Unit Tests**: Jest with TS‑Jest; 80%+ coverage target for services, guards, pipes.\n- **Integration Tests**: Supertest for API endpoints; uses an in‑memory SQLite (or Docker Postgres) instance spun up per test suite.\n- **E2E Mobile**: Detox (or Expo’s built‑in testing) for critical flows (auth, booking, map).\n- **Contract Tests**: Pact or manual schema verification ensuring mobile request DTOs match API DTOs (leveraging `shared-types`).\n- **Performance**: Locust/k6 scripts for load testing geo‑search and booking creation.\n- **Test Data**: Factories in `libs/test-helpers` using `@nestjs/testing` and `prisma-mock`.\n\n## Security Considerations\n- **Authentication**: JWT (access token 15 min, refresh token 7 days) stored in SecureStore; refresh token rotation.\n- **Authorization**: Role‑based guards (`RolesGuard`) protecting provider/admin routes.\n- **Data Validation**: DTOs with class‑validator & class‑transformer; sanitization via `mongoose-sanitize`‑like custom pipes.\n- **SQL Injection**: Prisma ORM eliminates raw strings; any raw queries use parameterized bindings.\n- **Rate Limiting**: NestJS `@Throttle` per‑route; Redis‑based distributed throttle for API.\n- **CORS**: Restricted to allowed origins (expo dev, production domains).\n- **Headers**: Helmet, CSP, HSTS (via middleware).\n- **Secrets Management**: Docker secrets / AWS Parameter Store / GitHub Actions encrypted variables.\n- **Data Encryption**: TLS everywhere; sensitive fields (e.g., payment token) encrypted at rest using `pgcrypto`.\n- **Audit Logging**: Interceptor logs mutating requests (user ID, IP, endpoint) to an immutable table or external SIEM.\n\n## Conclusion\nThis architecture cleanly separates the mobile client, API, and shared concerns while leveraging the chosen stack to deliver a scalable, maintainable, and performant system. The Nx monorepo enables rapid feature development with bounded contexts, and the combination of React Query, Prisma, PostGIS, Redis, and BullMQ provides robust data handling, real‑time updates, and background processing. CI/CD pipelines via GitHub Actions, EAS Build, and Docker Compose ensure reliable delivery from code to production.\n",
  "summary": "A clean‑architecture, Nx‑monorepo plan for Planity Clone using Expo/React Native, NestJS, Prisma/PostGIS, Redis, BullMQ, and Docker‑based DevOps."
}