{
  "content": "# Planity Clone Architecture\n\n## Overview\nThis document describes the system architecture for the Planity Clone mobile application. The solution follows a clean‑architecture approach with clear separation of concerns between the mobile client, the NestJS API, background workers, and shared infrastructure. The monorepo is managed with Nx and pnpm, enabling incremental builds, type‑safe code sharing, and consistent tooling.\n\n## Tech Stack\n- **Mobile**: Expo, React Native, TypeScript, Expo Router, TanStack React Query, React Native Reanimated\n- **Backend**: NestJS (Node.js), Prisma ORM, PostgreSQL (with PostGIS extension), Redis, BullMQ (job queue)\n- **DevOps / CI**: Nx (monorepo), pnpm (workspace manager), Docker Compose (local dev), GitHub Actions (CI/CD), EAS Build (Expo builds), Supabase (auth & storage optional)\n- **Testing**: Jest (unit/integration), React Native Testing Library\n\n## High-Level Architecture\nThe system is split into three logical layers:\n\n1. **Presentation Layer** – Expo/React Native app consuming the API via React Query.\n2. **Application Layer** – NestJS controllers, services, and DTOs defining use‑cases (auth, booking, search, etc.).\n3. **Infrastructure Layer** – Prisma models, PostgreSQL schema, Redis caching, BullMQ workers, and external services (payment gateway, email/SMS).\n\nCommunication flows:\n- Mobile → API (REST/GraphQL) over HTTPS.\n- API ↔ PostgreSQL (primary data store) and Redis (caching, rate‑limiting, session store).\n- API → BullMQ → Worker processes (emails, notifications, payment webhooks, slot recomputation).\n- Supabase (optional) handles social login and file uploads; can be replaced by custom NestJS providers.\n\n## Service Boundaries\n\n### Frontend (Mobile)\n- **Responsibility**: UI rendering, navigation, user interaction, offline optimisms, token management.\n- **Boundaries**:\n  - Uses Expo Router for file‑based navigation.\n  - State managed by React Query (server state) and React Context / Zustand (client state if needed).\n  - Animations via React Native Reanimated 2.\n  - Shared UI components and design tokens live in `libs/ui`.\n  - Shared TypeScript interfaces and API clients live in `libs/api`.\n\n### Backend (NestJS API)\n- **Responsibility**: Business logic, validation, authorization, data persistence, real‑time updates via Redis pub/sub.\n- **Boundaries**:\n  - Modules: `Auth`, `Users`, `Businesses`, `Services`, `Bookings`, `Payments`, `Reviews`, `Notifications`, `Admin`, `ProviderPortal`.\n  - Each module contains controllers, services, DTOs, guards, and Prisma repositories.\n  - Business rules encapsulated in services; controllers thin.\n  - Validation via class‑validator and pipes.\n  - Authentication via JWT (access token) + refresh token strategy; social login delegated to Supabase or custom OAuth.\n  - Authorization via roles (user, provider, admin) and abilities (Casbin or custom).\n  - Geo‑search leverages PostGIS (`ST_DWithin`) for radius queries.\n  - Caching layer: Redis stores frequently accessed data (business lists, user profiles) with TTL.\n  - Background jobs: BullMQ queues (`email`, `notification`, `payment-webhook`, `slot-recalc`). Workers are separate NestJS processes started via `nx run worker:start`.\n\n### Infrastructure\n- **Databases**:\n  - Primary: PostgreSQL + PostGIS (spatial indexes for location‑based search).\n  - Cache: Redis (LRU, pub/sub).\n- **Messaging**: BullMQ backed by Redis.\n- **File Storage**: Supabase Storage or AWS S3 (abstracted via upload service).\n- **Observability**: Structured logging (pino), metrics (Prometheus via NestJS middleware), health checks.\n- **Secrets**: Managed via Docker secrets / GitHub Actions vault; local dev uses `.env` files.\n\n## Folder Structure (Nx Monorepo)\n```\nplanity-clone/\n├── apps/\n│   ├── mobile/                 # Expo React Native app\n│   │   ├── src/\n│   │   │   ├── assets/\n│   │   │   ├── components/     # shared UI (reused via libs/ui)\n│   │   │   ├── navigation/     # Expo Router file‑based routes (auto)\n│   │   │   ├── screens/\n│   │   │   ├── hooks/\n│   │   │   ├── lib/            # react-query clients, utils\n│   │   │   └── types/          # re-exported from libs/api\n│   │   ├── app.json\n│   │   ├── eas.json\n│   │   └── tsconfig.json\n│   └── api/                    # NestJS server\n│       ├── src/\n│       │   ├── auth/\n│       │   ├── users/\n│       │   ├── businesses/\n│       │   ├── services/\n│       │   ├── bookings/\n│       │   ├── payments/\n│       │   ├── reviews/\n│       │   ├── notifications/\n│       │   ├── admin/\n│       │   ├── provider-portal/\n│       │   ├── common/         # guards, interceptors, pipes, utils\n│       │   ├── app.module.ts\n│       │   └── main.ts\n│       ├── test/\n│       ├── dockerfile\n│       └── tsconfig.json\n│   └── worker/                 # BullMQ workers (separate process)\n│       ├── src/\n│       │   ├── queues/         # queue definitions\n│       │   ├── processors/     # job handlers\n│       │   └── main.ts\n│       ├── test/\n│       ├── dockerfile\n│       └── tsconfig.json\n├── libs/\n│   ├── api/                    # generated SDK / typed fetch wrappers\n│   │   └── src/\n│   ├── ui/                     # shared design system (buttons, inputs, theme)\│   │   └── src/\n│   ├── types/                  # domain‑level TypeScript interfaces (used by mobile & api)\n│   │   └── src/\n│   ├── utils/                  # logging, date helpers, geo utils\n│   │   └── src/\n│   └── test/                   # shared testing helpers\n├── tools/\n│   ├── generators/             # Nx schematics for module scaffolding\n│   └── lint/\n│       └── rules/\n├── docker-compose.yml          # defines postgres, redis, api, worker, (optional) supabase stack\n├── nx.json\n├── package.json                # root pnpm workspace\n└── README.md\n```\n\n## Data Model Highlights\n- **User**: id, email, passwordHash, role, profile (name, phone, avatar), createdAt, updatedAt.\n- **Business**: id, name, description, address, location (PostGIS Point), phone, ownerId, categoryIds, schedule (JSON), createdAt, updatedAt.\n- **Service**: id, businessId, name, description, duration, price, bufferTime, isActive.\n- **Booking**: id, userId, serviceId, startTime, endTime, status, paymentId, metadata, createdAt, updatedAt.\n- **Review**: id, businessId, userId, rating, comment, response (by owner), createdAt.\n- **Favorite**: id, userId, businessId.\n- **ProviderSchedule**: id, providerId, weekday, startTime, endTime, breakStart, breakEnd.\nIndexes: spatial index on `business.location`, composite indexes on (`serviceId, startTime`) for slot lookup, (`userId, status`) for user bookings.\n\n## API Design\n- RESTful endpoints with versioning (`/api/v1/`).\n- Resource‑oriented URLs: `/businesses`, `/services`, `/bookings`, `/reviews`.\n- Query parameters for filtering, pagination, geo‑search (`?lat=&lng=&radius=`).\n- Auth: `Authorization: Bearer <access-token>`.\n- Error responses follow RFC 7807 (problem‑detail JSON).\n- Validation via class‑validator; returns 400 with field errors.\n\n## State Management & Data Fetching\n- React Query handles server state: automatic caching, background refetch, optimistic updates for bookings/favorites.\n- Query keys are structured: `['businesses', {lat, lng, radius, category}]`, `['booking', id]`, etc.\n- Mutations use `react-query`’s `mutateAsync` with rollback on error.\n- Reanimated 2 drives gesture‑based UI (e.g., swipe‑to‑cancel appointment).\n\n## Caching Strategy\n- **API → Redis**: Cache business list results (5 min), user profile (10 min), service catalog (30 min).\n- **Redis Pub/Sub**: Notify API instances of schedule changes → invalidate relevant caches.\n- **Client**: React Query’s stale‑while‑revalidate (staleTime 0, cacheTime 5 min).\n\n## Background Jobs (BullMQ)\n- **email**: Send confirmation, reminders, cancellation notices via SMTP/SendGrid.\n- **notification**: Push notifications (Expo Push Notifications) or in‑app alerts.\n- **payment-webhook**: Handle gateway async events (success, failure, refund).\n- **slot-recalc**: When a provider updates schedule or a booking changes, recompute and cache available slots for affected services.\n- Workers are horizontally scalable; each job defines attempts, backoff, and dead‑letter queue.\n\n## DevOps & CI/CD\n- **Local Dev**: `docker compose up` spins up postgres, redis, api, worker. Expo dev client runs via `npx expo start`.\n- **CI (GitHub Actions)**: \n  1. Install pnpm, restore Nx cache.\n  2. Run lint, type‑check, unit tests (Jest) for mobile, api, worker.\n  3. Build Docker images for api and worker.\n  4. Push images to registry (GHCR).\n  5. Run e2e tests against a preview environment (optional).\n  6. On tag/release: trigger EAS Build for iOS/Android, publish to stores.\n- **Production**: Kubernetes or ECS (not defined here) pulls images; env‑vars configure DB URLs, Redis, secrets.\n- **Database Migrations**: Prisma migrate deployed as part of api startup (`prisma migrate deploy`).\n\n## Security Considerations\n- HTTPS everywhere; HSTS headers.\n- JWT access token short‑lived (15 min); refresh token rotated and stored HTTP‑only cookie (if using cookies) or secure storage.\n- Role‑based access control (RBAC) enforced via NestJS guards.\n- Input validation and sanitization to prevent SQL injection (Prisma parameterized) and XSS (sanitize HTML in reviews).\n- Rate‑limiting via Redis (`express-rate-limit` with Redis store).\n- Sensitive data (payment info) never stored; tokenized via PCI‑compliant gateway (Stripe, PayPal).\n- Audit logs for critical actions (booking changes, admin modifications).\n\n## Scalability & Performance\n- Read‑heavy workloads served via Redis cache; fallback to DB.\n- Geo‑search uses PostGIS index; limit results + pagination.\n- Websocket‑like updates via Redis pub/sub + React Query refetch intervals for live slot updates.\n- Worker queues enable horizontal scaling; monitor queue depth and latency.\n- CDN for static assets (Expo web build) if needed.\n\n## Monitoring & Observability\n- Structured JSON logs (pino) with request ID correlation.\n- Metrics: request latency, error rates, job processing times exposed at `/metrics` (Prometheus).\n- Health checks: `/health` (db, redis, queue connectivity).\n- Error tracking: Sentry integration (optional).\n- Log aggregation: Loki or Elasticsearch via Docker logging drivers.\n\n## Conclusion\nThis architecture provides a clear separation between mobile UI, API business logic, and asynchronous processing, leveraging the strengths of each chosen technology. The Nx monorepo enables code sharing, consistent tooling, and scalable development practices, while Docker Compose, GitHub Actions, and EAS Build streamline local development, CI/CD, and distribution.\n\n---\n*Prepared by the architecture team.*\n",
  "summary": "A clean‑architecture monorepo plan for Planity Clone using Expo/React Native, NestJS, Prisma, PostgreSQL+PostGIS, Redis, BullMQ, Nx, pnpm, Docker, GitHub Actions, and EAS Build."
}