{
  "content": "# System Architecture for Planity Clone\n\n## Overview\nThis document outlines the architecture of the Planity Clone mobile application and its backend services. The system follows a clean, layered approach with clear separation of concerns, leveraging an Nx monorepo, Expo + React Native for the client, NestJS for the API, Prisma + PostgreSQL/PostGIS for data, Redis for caching and job queuing, and Docker Compose for local development. CI/CD is handled via GitHub Actions, EAS Build for mobile, and Supabase for optional auth/storage.\n\n## High-Level Components\n- **Mobile Client** – Expo React Native app (TypeScript) using Expo Router, TanStack React Query, React Native Reanimated.\n- **API Gateway** – NestJS application exposing REST/GraphQL endpoints.\n- **Domain Services** – Auth, Business, Booking, User, Provider, Admin, Notification, Payment (each as a NestJS module).\n- **Data Layer** – PostgreSQL with PostGIS extension, accessed via Prisma ORM.\n- **Caching & Jobs** – Redis (caching, rate limiting, BullMQ job queue).\n- **DevOps** – Docker Compose, Nx, pnpm, GitHub Actions, EAS Build, Supabase (optional).\n- **Shared Libraries** – Design system, TypeScript types, utility helpers.\n\n## Folder Structure (Nx Monorepo)\n```\nplanity-clone/\n├── apps/\n│   ├── mobile/                 # Expo React Native app\n│   │   ├── src/\n│   │   │   ├── assets/\n│   │   │   ├── components/\n│   │   │   │   ├── ui/          # design system components\n│   │   │   │   └── layout/\n│   │   │   ├── screens/         # Expo Router file‑based routes\n│   │   │   ├── hooks/\n│   │   │   ├── services/        # API service layer (React Query)\n│   │   │   ├── store/\n│   │   │   ├── utils/\n│   │   │   └── types/\n│   │   ├── app.json\n│   │   ├── tsconfig.json\n│   │   └── expo.config.js\n│   ├── api/                    # NestJS backend\n│   │   ├── src/\n│   │   │   ├── modules/\n│   │   │   │   ├── auth/\n│   │   │   │   ├── business/\n│   │   │   │   ├── booking/\n│   │   │   │   ├── user/\n│   │   │   │   ├── provider/\n│   │   │   │   ├── admin/\n│   │   │   │   ├── notification/\n│   │   │   │   └── payment/\n│   │   │   ├── prisma/\n│   │   │   │   ├── schema.prisma\n│   │   │   │   └── migrations/\n│   │   │   ├── utils/\n│   │   │   ├── guards/\n│   │   │   ├── interceptors/\n│   │   │   └── main.ts\n│   │   ├── test/\n│   │   ├── Dockerfile\n│   │   └── tsconfig.json\n│   └── admin/                  # Optional admin dashboard (NestJS or separate Next.js)\n│       ...\n├── libs/\n│   ├── design-system/          # React Native UI components (Reanimated for animations)\n│   │   ├── src/\n│   │   │   ├── components/\n│   │   │   └── tokens/\n│   │   ├── tsconfig.json\n│   │   └── index.ts\n│   ├── types/                  # Shared TypeScript DTOs & entities\n│   │   ├── src/\n│   │   └── index.ts\n│   ├── utils/                  # Shared helpers (logging, date, validation)\n│   │   └── ...\n│   └── eslint-config/          # Shared ESLint configuration\n├── tools/\n│   ├── generators/             # Nx generators for scaffolding modules\n│   └── ...\n├── docker-compose.yml\n├── nx.json\n├── package.json\n├── pnpm-workspace.yaml\n└── .github/\n    └── workflows/\n        ├── ci.yml\n        └── eas-build.yml\n```\n\n## Service Boundaries\n- **Auth Service** – Registration, login, JWT issuance, password reset, role‑based access.\n- **Business Service** – CRUD for businesses, geospatial queries (PostGIS), category management.\n- **Booking Service** – Slot computation, appointment creation, cancellation, rescheduling, conflict detection.\n- **User Service** – Profile, favorites, reviews, password change.\n- **Provider Portal** – Business‑owner specific endpoints (manage services, availability, view bookings).\n- **Admin Service** – Admin‑only endpoints for moderation, user/business management, analytics.\n- **Notification Service** – Workers that process BullMQ jobs to send push (Expo), email, or SMS.\n- **Payment Service** – Wrapper around payment gateway (Stripe/PayPal), creates intents, verifies webhooks.\n- **Gateway/API** – NestJS main app that aggregates modules, provides global validation, logging, error handling.\n\n## Data Model (Prisma)\n```prisma\nmodel User {\n  id            String   @id @default(uuid())\n  email         String   @unique\n  passwordHash  String\n  role          String   @default(\"user\")\n  profile       Profile? @relation(\"userProfile\")\n  bookings      Booking[]\n  reviews       Review[]\n  favorites     Favorite[]\n}\nmodel Profile {\n  id      String   @id @default(uuid())\n  user    User     @relation(\"userProfile\", fields: [userId], references: [id])\n  userId  String   @unique\n  name    String?\n  phone   String?\n}\nmodel Business {\n  id          String   @id @default(uuid())\n  name        String\n  address     String\n  location    Point?   @db.Point   # PostGIS\n  phone       String?\n  website     String?\n  ownerId     String\n  owner       User     @relation(fields: [ownerId], references: [id])\n  categories  BusinessCategory[]\n  services    Service[]\n  availabilities Availability[]\n}\nmodel Category {\n  id   String   @id @default(uuid())\n  name String   @unique\n}\nmodel BusinessCategory {\n  businessId String\n  business   Business @relation(fields: [businessId], references: [id])\n  categoryId String\n  category   Category @relation(fields: [categoryId], references: [id])\n  @@id([businessId, categoryId])\n}\nmodel Service {\n  id        String   @id @default(uuid())\n  business  Business @relation(fields: [businessId], references: [id])\n  businessId String\n  name      String\n  duration  Int      # minutes\n  price     Float\n}\nmodel Availability {\n  id          String   @id @default(uuid())\n  business    Business @relation(fields: [businessId], references: [id])\n  businessId  String\n  dayOfWeek   Int      # 0‑6 (Sun‑Sat)\n  startTime   DateTime\n  endTime     DateTime\n}\nmodel Booking {\n  id          String   @id @default(uuid())\n  user        User     @relation(fields: [userId], references: [id])\n  userId      String\n  service     Service  @relation(fields: [serviceId], references: [id])\n  serviceId   String\n  startTime   DateTime\n  endTime     DateTime\n  status      String   @default(\"pending\") # pending, confirmed, cancelled, completed\n  paymentId   String?\n  payment     PaymentTransaction? @relation(fields: [paymentId], references: [id])\n}\nmodel Review {\n  id        String   @id @default(uuid())\n  booking   Booking  @relation(fields: [bookingId], references: [id])\n  bookingId String\n  user      User     @relation(fields: [userId], references: [id])\n  userId    String\n  rating    Int      @db.Check(\"rating >= 1 AND rating <= 5\")\n  comment   String?\n}\nmodel Favorite {\n  id        String   @id @default(uuid())\n  user      User     @relation(fields: [userId], references: [id])\n  userId    String\n  business  Business @relation(fields: [businessId], references: [id])\n  businessId String\n  @@unique([userId, businessId])\n}\nmodel PaymentTransaction {\n  id          String   @id @default(uuid())\n  booking     Booking? @relation(fields: [bookingId], references: [id])\n  bookingId   String?\n  gateway     String   # e.g., \"stripe\"\n  amount      Float\n  status      String   # pending, succeeded, failed\n  createdAt   DateTime @default(now())\n}\nmodel Notification {\n  id        String   @id @default(uuid())\n  user      User     @relation(fields: [userId], references: [id])\n  userId    String\n  type      String   # booking_confirmation, reminder, review_request\n  payload   Json\n  sentAt    DateTime?\n}\n```\n\n## Caching Layer\n- Redis stores:\n  - Session tokens (optional, JWT stateless preferred).\n  - Rate‑limit counters (IP‑based).\n  - Frequently queried data: business lists, categories, service catalogs.\n  - BullMQ job queue backend.\n\n## Background Jobs (BullMQ)\n- Queues:\n  - `reminders` – send appointment reminders (push/email).\n  - `payment-webhook` – verify and reconcile payment gateway events.\n  - `review-request` – ask for review after completed booking.\n  - `cleanup` – delete expired tokens, temporary data.\n- Workers are NestJS services that process jobs; failures trigger retries with exponential backoff.\n\n## DevOps & CI/CD\n- **Local Development**: Docker Compose spins up `postgres`, `redis`, and the `api` service. Mobile app runs via `expo start`.\n- **Nx Monorepo**: Enables incremental builds, task caching, and `nx affected` commands for lint, test, build.\n- **Package Manager**: pnpm workspace for hoisted dependencies and deterministic installs.\n- **GitHub Actions**:\n  - `ci.yml` – runs lint (`eslint`), typecheck (`tsc`), unit tests (Jest) on every push/PR.\n  - `eas-build.yml` – on merge to `main`, triggers EAS Build for iOS/Android and publishes Docker image for the API to a registry.\n- **EAS Build**: Configured for Expo managed workflow; OTA updates via `expo publish`.\n- **Supabase** (optional): Used for authentication (if preferring Supabase Auth) and storage for user‑uploaded assets (e.g., profile pictures, business photos).\n- **Secrets Management**: GitHub Secrets for DB URLs, JWT secrets, API keys; Docker secrets or .env files for local.\n\n## Mobile App Architecture\n- **Routing**: Expo Router provides file‑based navigation under `src/screens`.\n- **State Management**: TanStack React Query handles server state (caching, background updates, optimistic updates). UI‑refetch). Local UI state uses React Context or Zustand where needed.\n- **Animations**: React Native Reanimated powers gestures, modal transitions, map marker animations within the design‑system components.\n- **API Layer**: A thin service (`src/services/api.ts`) wraps NestJS endpoints; React Query hooks consume these services.\n- **Design System**: Shared `libs/design-system` library provides themed components (Button, Input, Card, Modal, etc.) built with Reanimated for smooth interactions.\n- **Shared Types**: Imported from `@planity-clone/types` ensuring DTO parity between client and server.\n- **Assets**: Images, fonts, icons stored in `apps/mobile/assets`.\n- **Utilities**: Date formatting, geolocation helpers, validation functions in `libs/utils`.\n\n## Quality & Testing\n- **Unit Tests**: Jest for backend services, utilities, and React components (using React Native Testing Library).\n- **Integration Tests**: Jest + Supertest for NestJS endpoints (API contracts).\n- **E2E (Optional)**: Detox for critical user flows on mobile.\n- **Linting**: Shared ESLint config + Prettier; enforced via CI.\n- **TypeScript**: Strict mode (`noImplicitAny`, `strictNullChecks`, etc.) across all packages.\n\n## Security Considerations\n- Transport: HTTPS only (Expo managed, NestJS behind TLS termination).\n- Authentication: JWT (access token + refresh token) stored securely (SecureStore for mobile, HttpOnly cookie for web if applicable).\n- Password: bcrypt hash with salt.\n- Authorization: NestJS guards (`RolesGuard`, `AuthGuard`) checking JWT claims and scopes.\n- Input Validation: Class‑validator + DTOs; sanitization to prevent injection.\n- Rate Limiting: Redis‑based limiter on auth and public endpoints.\n- Webhook Security: Verify signatures (Stripe webhook secret, etc.).\n- Secrets: Never committed; injected via CI/CD environment.\n\n## Scalability & Performance\n- **API Horizontal Scaling**: NestJS app stateless; run multiple instances behind a load balancer (NGINX, ALB).\n- **Database Read Replicas**: For heavy read‑only GIS queries (nearby businesses).\n- **Redis Cluster**: For caching and job queue at scale.\n- **CDN**: Static assets (Expo web build, images) served via CloudFront or similar.\n- **Mobile OTA**: Expo EAS enables over‑the‑air JS bundle updates without store review.\n- **Observability**: Structured logging (winston/pino), metrics (Prometheus), tracing (OpenTelemetry) – can be added later.\n\n## Summary\nThe architecture separates concerns into clearly bounded services, uses a monorepo for shared code, leverages modern mobile (Expo/React Native) and backend (NestJS/Prisma) stacks, and incorporates caching, background jobs, and CI/CD to deliver a scalable, maintainable Planity Clone.",
  "summary": "A clean, layered architecture using Expo React Native, NestJS, Prisma/PostGIS, Redis, BullMQ, Nx, pnpm, Docker, GitHub Actions, and EAS Build to support the Planity Clone’s features with clear service boundaries and shared libraries."
}