{
  "content": "# Planity Clone Architecture\n\n## Overview\nThe Planity Clone is a monorepo built with Nx, separating concerns into frontend (Expo/React Native) and backend (NestJS) applications, sharing a TypeScript library for types and utilities.\n\n## Service Boundaries\n- **Auth Service**: Handles user registration, login, JWT issuance, password reset.\n- **Business Service**: CRUD for businesses, categories, services, location (PostGIS), hours.\n- **Booking Service**: Manages appointment slots, availability computation, booking creation/cancellation.\n- **Payment Service**: Integrates with payment gateway, creates payment intents, webhook handling.\n- **Review Service**: Stores reviews/ratings, aggregates ratings.\n- **Notification Service**: Sends push/email via BullMQ workers.\n- **Admin Service**: Admin dashboard APIs for users, businesses, content moderation.\n- **Provider Portal**: Business owner APIs for managing own listings and bookings.\n\n## Shared Library (`libs/shared`)\n- TypeScript interfaces for DTOs, entities, API responses.\n- Utility functions (date helpers, geo calculations).\n- Design system tokens and reusable UI components (expo).\n\n## Folder Structure\n```\nnx-workspace/\n├── apps/\n│   ├── mobile/               # Expo React Native app\n│   │   ├── src/\n│   │   │   ├── assets/\n│   │   │   ├── components/   # Reusable UI (buttons, cards, etc.)\n│   │   │   ├── screens/      # Expo Router file‑based routes\n│   │   │   ├── hooks/        # Custom React Query hooks\n│   │   │   ├── navigation/   # Expo Router config (if needed)\n│   │   │   ├── store/        # Optional state (e.g., Redux Toolkit)\n│   │   │   ├── themes/       # Design system tokens\n│   │   │   └── utils/\n│   │   ├── app.json\n│   │   ├── tsconfig.json\n│   │   └── Expo specific config\n│   ├── api/                  # NestJS backend\n│   │   ├── src/\n│   │   │   ├── auth/\n│   │   │   ├── business/\n│   │   │   ├── booking/\n│   │   │   ├── payment/\n│   │   │   ├── review/\n│   │   │   ├── notification/\n│   │   │   ├── admin/\n│   │   │   ├── provider/\n│   │   │   ├── shared/       # NestJS shared modules (guards, interceptors)\n│   │   │   ├── prisma/       # Prisma schema and client wrapper\n│   │   │   ├── main.ts\n│   │   │   └── app.module.ts\n│   │   ├── test/\n│   │   ├── tsconfig.json\n│   │   └── Dockerfile\n│   └── admin-portal/         # Optional internal admin web app (could be Next.js, but out of scope)\n├── libs/\n│   ├── shared/               # Shared TS library\n│   │   ├── src/\n│   │   │   ├── types/\n│   │   │   ├── utils/\n│   │   │   └── ui/           # Design system components (React Native)\n│   │   ├── tsconfig.json\n│   │   └── index.ts\n│   └── ... (other libs like ui-kit, data-access)\n├── prisma/\n│   ├── schema.prisma\n│   └── migrations/\n├── docker/\n│   ├── compose.yml\n│   ├── Dockerfile.api\n│   └── Dockerfile.mobile? (not needed)\n├── .github/\n│   └── workflows/\n│       ├── ci.yml            # lint, test, build\n│       └── cd.yml            # EAS build, Docker push\n├── nx.json\n├── package.json\n├── tsconfig.base.json\n└── .eslintrc.json\n```\n\n## Data Layer\n- **Prisma Schema** defines models: User, Business, Category, Service, Appointment, Payment, Review, Notification, etc.\n- **PostGIS** extension used on Business.location (Point) for geo queries.\n- **Redis** caches frequently accessed data: business listings, session tokens, rate limits.\n- **BullMQ** queues: email, push-notification, payment-processing, availability-recalc.\n\n## API Communication\n- Frontend communicates with backend via REST (or GraphQL if preferred) using TanStack React Query for data fetching, mutation, and caching.\n- Auth uses JWT stored in SecureStore; refresh token rotation.\n- Expo Router provides file‑based navigation; screens call React Query hooks that hit `/api/*` endpoints.\n\n## DevOps & CI/CD\n- **Docker Compose** for local development: spins up api, db, redis, mailhog.\n- **GitHub Actions**:\n  - CI: lint (ESLint), type-check (tsc), unit tests (Jest), build Docker images.\n  - CD: on push to main, push Docker images to registry, trigger EAS build for iOS/Android, deploy API to staging/prod (e.g., Fly.io or AWS ECS).\n- **EAS Build** configured via `eas.json` for Expo builds.\n- **Supabase** (optional) can be used for auth and storage; but we keep custom NestJS auth.\n\n## Testing Strategy\n- **Unit Tests**: Jest for services, utilities, React components.\n- **Integration Tests**: Supertest for NestJS endpoints, React Native Testing Library for screens.\n- **E2E**: Detox for critical flows (login, search, booking).\n\n## Quality Attributes\n- **Scalability**: Stateless NestJS services, horizontal scaling via containers; read replicas for PostGIS.\n- **Performance**: Redis caching, React Query pagination, geo-indexed queries.\n- **Maintainability**: Clear domain boundaries, shared types, Nx affected commands.\n- **Security**: JWT, HTTPS, helmet, rate limiting, input validation, Prisma prevents SQL injection.\n\n## Conclusion\nThis architecture separates concerns, leverages the Expo ecosystem for rapid mobile development, and uses NestJS with Prisma for a robust, extensible backend. The Nx monorepo enables code sharing and efficient CI/CD pipelines.\n",
  "summary": "A concise architecture overview for the Planity Clone using Expo, React Native, NestJS, Prisma, PostGIS, Redis, and Nx monorepo with clear service boundaries."
}