{
  "content": "# Planity Clone System Architecture\n\n## Overview\nPlanity Clone is a full‑stack mobile‑first platform that lets users discover, book, and manage appointments with local businesses. The architecture follows a clean‑separation, domain‑driven approach using a monorepo managed by Nx, enabling code sharing between the Expo‑based mobile app and the NestJS backend while keeping concerns isolated.\n\n## Technology Stack\n- **Mobile**: Expo (SDK 49), React Native, TypeScript, Expo Router, TanStack React Query, React Native Reanimated\n- **Backend**: NestJS (Node.js), TypeScript, Prisma ORM, PostgreSQL + PostGIS extension, Redis (caching & pub/sub), BullMQ (background jobs)\n- **DevOps**: Nx monorepo, pnpm workspace, Docker Compose for local dev, GitHub Actions for CI/CD, EAS Build & Submit for mobile, Supabase (optional), Supabase for auth & storage (fallback)\n- **Testing**: Jest (unit/integration), React Native Testing Library, Supertest (API)\n- **Other**: Shared design system (React Native + Tailwind‑like via NativeWind), shared TypeScript types via Nx libs\n\n## Monorepo Layout (Nx)\n```\nplanity-clone/\n├── apps/\n│   ├── mobile/          # Expo React Native app\n│   └── api/             # NestJS backend\n├── libs/\n│   ├── ui/              # Shared UI components (buttons, icons, theme)\n│   ├── design-system/   # Design tokens, styling utils\n│   ├── types/           # Shared TypeScript interfaces (DTOs, enums)\n│   ├── data-access/     # Thin wrappers around Prisma client (used by API)\n│   ├── mobile-query/    # React Query hooks & query keys used in mobile\n│   ├── api-query/       # NestJS query utilities (filters, pagination)\n│   └── utils/           # Logging, error handling, date helpers\n├── docker-compose.yml\n├── nx.json\n├── package.json (pnpm workspace)\n└── .github/\n    └── workflows/\n        ├── ci.yml       # lint, test, build\n        └── cd.yml       # deploy to staging/prod\n```\n\n## Mobile Application (`apps/mobile`)\n- **Routing**: Expo Router (file‑based) provides stack & tab navigation; deep linking supported.\n- **State Management**: TanStack React Query handles server‑state (caching, background refetch, optimistic updates).\n- **Animations**: React Native Reanimated 2 for gesture‑based UI (e.g., swipe‑to‑cancel booking, map interactions).\n- **Authentication**: Supabase Auth (email/password + social) wrapped in a custom `useAuth` hook; tokens stored securely via Expo SecureStore.\n- **Map**: `react-native-maps` with custom markers showing businesses; integrates with Expo Location for user‑position.\n- **Payments**: Stripe SDK via `expo‑payment‑request` (Apple/Google Pay) or custom web‑view fallback.\n- **Notifications**: Expo Notifications (push) + foreground handling; preferences persisted via AsyncStorage.\n- **Offline**: Query persistence (React Query persister) for browse/catalog; critical actions (booking) require online.\n- **Build**: EAS Build for iOS/Android; EAS Submit for store releases.\n\n## Backend API (`apps/api`)\n- **Framework**: NestJS with modular architecture (feature modules: `auth`, `business`, `booking`, `payment`, `notification`, `admin`, `provider`).\n- **Data Layer**: Prisma ORM generates Type‑safe client; PostgreSQL + PostGIS for geo‑queries (nearest businesses, radius search).\n- **Caching**: Redis stores frequently‑accessed lists (business catalog, user favorites) and session tokens; TTL configured per use‑case.\n- **Background Jobs**: BullMQ processes email sending, payment webhook handling, slot recomputation, and analytics aggregation.\n- **Validation**: class‑validator + DTOs shared via `libs/types`.\n- **Authentication**: JWT strategy (access token) + refresh token rotation; Supabase used only for email verification & password reset.\n- **API Contract**: RESTful endpoints with versioning (`/v1/`); OpenAPI spec generated via `@nestjs/swagger`.\n- **Security**: Helmet, CORS, rate limiting, input sanitization, SQL injection prevented by Prisma.\n- **Admin/Provider Portal**: Separate NestJS modules guarded by role‑based guards (`RolesGuard`); UI can be built later as an internal web app or extend mobile with role‑based screens.\n\n## Shared Libraries (`libs`)\n- **`ui` & `design-system`**: Reusable components (Button, Input, Card, Modal) styled with NativeWind (Tailwind‑like) for mobile; same tokens can be consumed by a future web admin panel.\n- **`types`**: Domain models (`User`, `Business`, `Service`, `Appointment`, `Review`, `Payment`) and API DTOs; ensures end‑to‑end type safety.\n- **`data-access`**: Thin wrapper exposing Prisma client with transaction helpers; used only by backend.\n- **`mobile-query`**: Custom React Query hooks (`useBusinesses`, `useBooking`, etc.) that encapsulate query keys, invalidation logic, and optimistic updates.\n- **`api-query`**: NestJS helpers for building filtered, paginated, and geo‑queries (PostGIS `ST_DistanceSphere`).\n- **`utils`**: Logging (pino), error formatting, date‑time utilities, environment validation.\n\n## Infrastructure & DevOps\n- **Local Development**: `docker-compose.yml` spins up PostgreSQL (with PostGIS extension), Redis, and optionally a mock Supabase instance.\n- **CI/CD**: GitHub Actions runs on every push:\n  - Install pnpm deps\n  - Lint (ESLint) & type‑check (tsc)\n  - Run Jest unit/integration tests (mobile + backend)\n  - Build Docker images for API\n  - On main branch: push images to registry, trigger EAS Build for mobile, deploy API to staging/prod (e.g., Fly.io, Render, or Kubernetes).\n- **Environment Variables**: Managed via `.env` files; validated in `libs/utils/env.ts`.\n- **Observability**: API emits structured logs (pino); mobile sends error reports to Sentry via Expo; metrics exposed via Prometheus endpoint (optional).\n\n## Data Modeling & Database\n- **Core Entities**:\n  - `User` (id, email, role, authProvider, createdAt)\n  - `Business` (id, name, description, address, location: PostGIS Point, categoryId, avgRating)\n  - `Service` (id, businessId, name, duration, price)\n  - `Appointment` (id, userId, businessId, serviceId, startTime, endTime, status, paymentId)\n  - `Review` (id, businessId, userId, rating, comment, createdAt)\n  - `Favorite` (userId, businessId)\n  - `Payment` (id, appointmentId, provider, amount, status, transactionId)\n- **Indexes**:\n  - Spatial index on `Business.location` for KNN queries.\n  - Composite indexes on `(businessId, startTime)` for slot lookup.\n  - TTL on Redis keys for cached lists.\n- **Migrations**: Prisma Migrate; seed scripts for default categories and admin user.\n\n## Service Boundaries & Responsibilities\n| Boundary            | Responsibility                                                                                     | Owned By\n|---------------------|----------------------------------------------------------------------------------------------------|----------|\n| **Auth**            | User registration, login, logout, token refresh, password reset, social linking                     | API (`auth` module) + Mobile (`useAuth`)\n| **Business Catalog**| CRUD for businesses, geo‑search, filtering, pagination, caching of lists                           | API (`business`)\n| **Services & Slots**| Service definition, availability calculation (based on business hours, existing appointments), slot generation | API (`service`)\n| **Booking**         | Create/apply appointments, optimistic UI updates, conflict detection, payment initiation, confirmation emails | API (`booking`)\n| **Payments**        | Payment gateway integration (Stripe), webhook handling, refunds, receipts                           | API (`payment`)\n| **Reviews**         | Submit, list, respond to reviews, moderation flags                                                | API (`review`)\n| **Favorites**       | Toggle favorite, list favorites                                                                   | API (`favorite`)\n| **User Profile**    | Read/update user details, change password, manage notification preferences                         | API (`user`)\n| **Notifications**   | Push notification dispatch (via Expo push service), preference storage, background job for batch sends | API (`notification`)\n| **Admin / Provider**| Role‑protected CRUD for businesses, services, appointments, analytics, moderation tools            | API (`admin`, `provider`)\n| **Background Jobs** | Email/SMS dispatch, slot recomputation, analytics aggregation, retry‑failed payments               | BullMQ workers (API)\n| **Mobile UI**       | Presentation, navigation, gesture handling, local state, offline caching, map rendering            | Mobile app (`apps/mobile`)\n| **Shared Types/DS** | Consistent data contracts, UI/theme tokens, utility functions                                      | Nx libs\n\n## Security Considerations\n- Authentication tokens are short‑lived (15 min) with refresh token rotation; stored in SecureStore.\n- Sensitive data (payment info) never touches our servers; handled directly by Stripe SDK.\n- Role‑based access control (RBAC) enforces admin/provider vs regular user.\n- Input validation via class‑validator and Prisma prevents injection.\n- API endpoints are behind HTTPS; CORS restricted to allowed domains.\n- Secrets managed via Docker secrets or platform‑specific env vars (GitHub Actions, EAS).\n- Regular dependency audits (`npm audit`, `pnpm audit`) and Dependabot updates.\n\n## Testing Strategy\n- **Unit Tests**: Jest for utility functions, Prisma query helpers, NestJS services (mocked repositories).\n- **Component Tests**: React Native Testing Library for mobile UI components; mock React Query.\n- **Integration Tests**: Supertest against API endpoints with test database (PostgreSQL in Docker).\n- **End‑to‑End (Optional)**: Detox for critical flows (login → search → book → cancel).\n- **Coverage Goal**: ≥80% for backend, ≥70% for mobile.\n\n## CI/CD Pipeline\n1. **Pull Request** → lint, lint, lint, type‑check, unit tests (mobile + backend), build Docker image (API) → publish to GitHub Packages (if needed).\n2. **Merge to main** →, same steps + push Docker image to registry, trigger EAS Build (iOS/Android) → upload to TestFlight/Google Play Internal Test.\n3. **Deploy API** →, rolling update to staging, run smoke tests, then promote to production after approval.\n4. **Notifications** → on deployment success/failure via Slack/GitHub Actions.\n\n## Deployment & Environment\n- **Development**: Docker Compose + Expo dev client.\n- **Staging**: Identical to prod but with separate DB/Redis instances; feature flags via env.\n- **Production**: Managed PostgreSQL (e.g., Supabase/Neon) with PostGIS, managed Redis (e.g., RedisCloud), NestJS deployed to container platform (Fly.io, Render, AWS ECS). Mobile distributed via App Store/Google Play using EAS Build.\n- **Rollback**: DB migrations are backward‑compatible; Docker image tags allow instant rollback.\n\n## Future Extensions\n- **Web Admin Dashboard**: React + Next.js using same `libs/types` and `libs/ui` (adapted for web).\n- **GraphQL Layer**: Replace REST with Nexus/TypeGraphQL for flexible queries.\n- **Machine Learning**: Recommendation engine for businesses based on user history.\n- **Multi‑currency & Tax**: Extend payment and service models.\n- **Offline‑First**: Enhance React Query persister and add background sync.\n\n---\n*This document captures the high‑level architecture; detailed implementation lives in the respective module READMEs and code comments.*\n",
  "summary": "A concise overview of the Planity Clone architecture covering monorepo structure, mobile and backend services, shared libs, infrastructure, data modeling, service boundaries, security, testing, CI/CD, and deployment."
}