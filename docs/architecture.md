{
  "content": "# Planity Clone — System Architecture\n\n## 1. Introduction\nThis document defines the system architecture for **Planity Clone**, a mobile-first beauty & wellness booking platform. It covers tech stack, folder structure, service boundaries, and key design decisions using Expo, React Native, TypeScript, NestJS, Prisma, PostgreSQL/PostGIS, Redis, and supporting tooling.\n\n## 2. Tech Stack\n- **Mobile**: Expo, React Native, TypeScript, Expo Router, TanStack React Query, React Native Reanimated\n- **Backend**: NestJS, Prisma, PostgreSQL, PostGIS, Redis (BullMQ), Supabase (Auth/Storage)\n- **Monorepo**: Nx, pnpm\n- **Infra**: Docker Compose, GitHub Actions, EAS Build, Jest\n\n## 3. Monorepo Layout (Nx + pnpm)\n```\nplanity-clone/\n├── apps/\n│   ├── mobile/            # Expo React Native app\n│   ├── api/               # NestJS backend\n│   └── admin-web/         # (future) Admin dashboard web\n├── libs/\n│   ├── shared-types/      # TS types shared by mobile + api\n│   ├── ui/                # React Native design system\n│   ├── config/            # ESLint, TS configs\n│   └── utils/             # Common helpers\n├── tools/\n│   ├── docker/            # Docker Compose files\n│   └── github/            # Workflow definitions\n├── docs/\n│   ├── product.md\n│   └── architecture.md\n├── package.json\n├── pnpm-workspace.yaml\n├── nx.json\n└── docker-compose.yml\n```\n\n## 4. Mobile App Structure (apps/mobile)\n```\napps/mobile/\n├── app/                   # Expo Router screens\n│   ├── (auth)/\n│   │   ├── login.tsx\n│   │   └── register.tsx\n│   ├── (tabs)/\n│   │   ├── explore.tsx\n│   │   ├── bookings.tsx\n│   │   └── profile.tsx\n│   ├── business/[id].tsx\n│   ├── booking/flow.tsx\n│   └── _layout.tsx\n├── src/\n│   ├── api/               # React Query hooks\n│   ├── components/        # Feature components\n│   ├── animations/        # Reanimated utils\n│   ├── stores/            # Local state (Zustand)\n│   └── theme/             # Theme tokens\n├── assets/\n├── eas.json\n└── package.json\n```\n\n## 5. API Structure (apps/api)\n```\napps/api/\n├── src/\n│   ├── auth/              # Auth module (Supabase + JWT)\n│   ├── users/             # User profiles\n│   ├── businesses/        # Business CRUD + search\n│   ├── services/          # Service & categories\n│   ├── bookings/          # Appointment logic\n│   ├── availability/      # Slot computation\n│   ├── payments/          # Stripe integration\n│   ├── reviews/           # Ratings\n│   ├── notifications/     # Push/email/SMS\n│   ├── admin/             # Admin operations\n│   ├── jobs/              # BullMQ workers\n│   ├── prisma/            # Prisma service\n│   ├── geo/               # PostGIS helpers\n│   └── main.ts\n├── test/\n├── Dockerfile\n└── package.json\n```\n\n## 6. Service Boundaries\n- **Auth**: Supabase handles identity; NestJS issues app JWT. Mobile uses React Query for session.\n- **Businesses**: Owner CRUD, public search (PostGIS radius), map data.\n- **Booking**: Orchestrates availability, payments, notifications.\n- **Availability**: Pure logic module; computes slots from working hours + bookings.\n- **Payments**: Stripe; webhooks to confirm/cancel.\n- **Jobs**: BullMQ on Redis for reminders, reports.\n- **Admin**: Separate guard; manages moderation.\n\n## 7. Data Model (Prisma + PostGIS)\n- User, Business, Service, Category, Staff, Booking, Review, Address, Payment\n- Business has `location geography(Point)` for PostGIS queries.\n\n## 8. Cross-Cutting\n- **Shared Types**: libs/shared-types synced via Nx.\n- **UI**: libs/ui provides buttons, cards, map components.\n- **CI**: GitHub Actions runs lint, test, EAS build.\n- **Local**: docker-compose for postgres, redis.\n\n## 9. Priority Mapping\n- P0: auth, search, booking, payments, provider portal (api), mobile flows\n- P1: map, admin, notifications, jobs\n- P2: favorites\n\n## 10. Summary\nClean Nx monorepo separating Expo mobile and NestJS API with shared types, PostGIS-powered search, and Redis-backed jobs.",
  "summary": "Nx monorepo architecture with Expo mobile and NestJS API using PostGIS and Redis for a scalable Planity Clone."
}