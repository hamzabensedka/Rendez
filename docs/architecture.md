{
  "content": "# Planity Clone System Architecture\n\n## Overview\nThe Planity Clone is a mobile-first platform that connects users with local businesses for discovering, booking, and managing appointments. The architecture follows a modular monorepo approach using Nx, separating concerns into distinct services and shared libraries.\n\n## Tech Stack\n- Mobile: Expo, React Native, TypeScript, Expo Router, TanStack React Query, React Native Reanimated\n- Backend: NestJS, Prisma, PostgreSQL, PostGIS, Redis, BullMQ\n- Monorepo: Nx, pnpm\n- DevOps: Docker Compose, GitHub Actions, EAS Build, Supabase (auth/storage), Jest\n\n## High-Level Architecture\nThe system consists of three main layers:\n1. **Client Layer** – Expo mobile app communicating with backend via REST/GraphQL (or NestJS controllers).\n2. **API Layer** – NestJS application exposing domain-specific controllers, using Prisma ORM for data access.\n3. **Infrastructure Layer** – PostgreSQL with PostGIS extension for geo‑queries, Redis for caching and BullMQ job queue, Supabase for authentication and file storage.\n\nCommunication between layers is asynchronous where appropriate (e.g., notifications, payment processing) via BullMQ workers.\n\n## Service Boundaries\n| Service | Responsibility |\n|---------|----------------|\n| Auth | User registration, login, password reset, social login, token management |\n| Business | CRUD of business listings, categories, hours, location (PostGIS) |\n| Booking | Slot generation, appointment creation, rescheduling, cancellation |\n| Payment | Integration with payment gateway, transaction recording, refunds |\n| Notification | Sending push/email/SMS via background jobs, handling templates |\n| Review | Creating, reading, updating reviews, business responses |\n| Provider Portal | Business owner dashboard for managing listings, bookings, customer interactions |\n| Admin | Platform‑wide moderation, analytics, user management |\n| Shared Kernel | Shared TypeScript types, design system components, utility functions, API contracts |\n\n## Mobile App Folder Structure\n```\nmobile/\n├─ app/                     # Expo Router file‑based routes\n│   ├─ (tabs)/              # Tab navigator screens\n│   │   ├─ index.tsx        # Home tab (guest browse)\n│   │   ├─ search.tsx       # Search & discovery\n│   │   ├─ map.tsx          # Map‑based search\n│   │   ├─ bookings.tsx     # Appointment management\n│   │   └─ profile.tsx      # User profile\n│   ├─ auth/\n│   │   ├─ sign-in.tsx\n│   │   ├─ sign-up.tsx\n│   │   └─ reset-password.tsx\n│   ├─ business/\n│   │   ├─ [businessId]/    # Dynamic route for business detail\n│   │   │   ├─ index.tsx\n│   │   │   ├─ services.tsx\n│   │   │   └─ reviews.tsx\n│   │   └─ favorites.tsx\n│   ├─ provider/\n│   │   └─ dashboard.tsx    # Provider portal entry\n│   └─ admin/\n│       └─ dashboard.tsx    # Admin dashboard entry\n├─ components/              # Reusable UI components (design system)\n│   ├─ ui/\n│   │   ├─ button.tsx\n│   │   ├─ input.tsx\n│   │   ├─ card.tsx\n│   │   └─ map.tsx\n│   └─ layout/\n│       ├─ header.tsx\n│       └─ footer.tsx\n├─ hooks/                   # Custom React hooks (query, auth, geo)\n├─ lib/                     # API client (React Query wrappers), utils\n├─ assets/                  # Images, icons, fonts\n├─ constants/               # Enums, config values\n├─ types/                   # Shared TypeScript interfaces (imported from libs/types)\n└─ expo.config.js\n```\n\n## Backend (NestJS) Folder Structure\n```\nbackend/\n├─ src/\n│   ├─ app.module.ts\n│   ├─ main.ts\n│   ├─ config/                # Environment configuration (nestjs/config)\n│   ├─ common/                # Guards, interceptors, pipes, DTO base classes\n│   ├─ auth/\n│   │   ├─ auth.controller.ts\n│   │   ├─ auth.service.ts\n│   │   ├─ dto/\n│   │   ├─ guards/\n│   │   └─ strategies/\n│   ├─ business/\n│   │   ├─ business.controller.ts\n│   │   ├─ business.service.ts\n│   │   ├─ dto/\n│   │   ├─ entities/          # Prisma generated entities\n│   │   └─ repositories/\n│   ├─ booking/\n│   │   ├─ booking.controller.ts\n│   │   ├─ booking.service.ts\n│   │   ├─ dto/\n│   │   ├─ entities/\n│   │   └─ slots/             # Slot computation logic\n│   ├─ payment/\n│   │   ├─ payment.controller.ts\n│   │   ├─ payment.service.ts\n│   │   ├─ dto/\n│   │   └─ providers/         # Gateway adapters (Stripe, PayPal)\n│   ├─ notification/\n│   │   ├─ notification.controller.ts\n│   │   ├─ notification.service.ts\n│   │   ├─ dto/\n│   │   └─ templates/\n│   ├─ review/\n│   │   ├─ review.controller.ts\n│   │   ├─ review.service.ts\n│   │   ├─ dto/\n│   │   └─ entities/\n│   ├─ provider/\n│   │   ├─ provider.controller.ts\n│   │   ├─ provider.service.ts\n│   │   ├─ dto/\n│   │   └─ entities/\n│   ├─ admin/\n│   │   ├─ admin.controller.ts\n│   │   ├─ admin.service.ts\n│   │   ├─ dto/\n│   │   └─ entities/\n│   └─ prisma/                # Prisma client wrapper, migrations\n├─ test/                      # Unit & integration tests (Jest)\n├─ prisma/\n│   ├─ schema.prisma\n│   └─ migrations/\n├─ Dockerfile\n└─ nest-cli.json\n```\n\n## Shared Libraries (Nx)\n```\nlibs/\n├─ types/                     # Shared TypeScript interfaces (User, Business, Appointment, etc.)\n├─ ui/                        # Design system components reusable across mobile & web (if any)\n├─ utils/                     # Helper functions (date, geo, validation)\n├─ data-access/               # Thin wrapper around Prisma for use in both backend and scripts\n├─ api-contracts/             # OpenAPI spec or GraphQL schema definitions\n└─ testing/                   # Test utilities, mocks\n```\n\n## Database Schema (Prisma)\n```\nmodel User {\n  id            String   @id @default(uuid())\n  email         String   @unique\n  password      String?\n  name          String\n  role          Role     @default(USER)\n  createdAt     DateTime @default(now())\n  updatedAt     DateTime @updatedAt\n  businesses    Business[]\n  appointments  Appointment[]\n  favorites     Favorite[]\n  reviews       Review[]\n  payments      Payment[]\n  notifications Notification[]\n}\n\nmodel Business {\n  id            String   @id @default(uuid())\n  name          String\n  description   String?\n  address       String\n  latitude      Float\n  longitude     Float\n  location      Point?   @db.Point\n  category      String\n  phone         String?\n  website       String?\n  hours         Json?    # opening hours stored as JSON\n  ownerId       String\n  owner         User     @relation(fields: [ownerId], references: [id])\n  services      Service[]\n  appointments  Appointment[]\n  reviews       Review[]\n  favorites     Favorite[]\n  createdAt     DateTime @default(now())\n  updatedAt     DateTime @updatedAt\n}\n\nmodel Service {\n  id            String   @id @default(uuid())\n  name          String\n  durationMin   Int\n  price         Float\n  businessId    String\n  business      Business @relation(fields: [businessId], references: [id])\n  appointments  Appointment[]\n}\n\nmodel Appointment {\n  id            String   @id @default(uuid())\n  userId        String\n  businessId    String\n  serviceId     String\n  user          User     @relation(fields: [userId], references: [id])\n  business      Business @relation(fields: [businessId], references: [id])\n  service       Service  @relation(fields: [serviceId], references: [id])\n  startTime     DateTime\n  endTime       DateTime\n  status        AppointmentStatus\n  paymentId     String?\n  payment       Payment? @relation(fields: [paymentId], references: [id])\n  createdAt     DateTime @default(now())\n  updatedAt     DateTime @updatedAt\n}\n\nmodel Review {\n  id            String   @id @default(uuid())\n  userId        String\n  businessId    String\n  user          User     @relation(fields: [userId], references: [id])\n  business      Business @relation(fields: [businessId], references: [id])\n  rating        Int\n  comment       String?\n  response      String?  # business response\n  createdAt     DateTime @default(now())\n}\n\nmodel Favorite {\n  id            String   @id @default(uuid())\n  userId        String\n  businessId    String\n  user          User     @relation(fields: [userId], references: [id])\n  business      Business @relation(fields: [businessId], references: [id])\n  createdAt     DateTime @default(now())\n  @@unique([userId, businessId])\n}\n\nmodel Payment {\n  id            String   @id @default(uuid())\n  amount        Float\n  currency      String   @default('USD')\n  status        PaymentStatus\n  provider      String   # e.g., stripe\n  providerId    String\n  appointment   Appointment? @relation(fields: [appointmentId], references: [id])\n  appointmentId String?\n  userId        String\n  user          User     @relation(fields: [userId], references: [id])\n  createdAt     DateTime @default(now())\n}\n\nmodel Notification {\n  id            String   @id @default(uuid())\n  userId        String\n  type          NotificationType\n  title         String\n  body          String\n  data          Json?    # extra payload\n  isRead        Boolean  @default(false)\n  createdAt     DateTime @default(now())\n  user          User     @relation(fields: [userId], references: [id])\n}\n\nenum Role { USER ADMIN PROVIDER }\nenum AppointmentStatus { PENDING CONFIRMED COMPLETED CANCELLED }\nenum PaymentStatus { PENDING SUCCEEDED FAILED REFUNDED }\nenum NotificationType { BOOKING_CONFIRMATION REMINDER PROMO REVIEW_RESPONSE }\n```\n\n## Caching & Message Queue\n- **Redis** stores:\n  - Session tokens (optional)\n  - Geo‑search caches for frequent queries\n  - Rate‑limit counters\n- **BullMQ** workers (defined in NestJS modules) handle:\n  - Sending notifications (push, email, SMS)\n  - Payment reconciliation\n  - Cleanup of expired carts/sessions\n  - Generating periodic reports for admin\n\n## DevOps & CI/CD\n- **Local Development**: Docker Compose spins up PostgreSQL (with PostGIS extension), Redis, and NestJS API. Expo dev client runs locally.\n- **CI Pipeline** (GitHub Actions):\n  - Install pnpm dependencies\n  - Run lint (ESLint) and type checking (tsc)\n  - Run Jest unit tests for mobile and backend\n  - Build Docker images for backend\n  - Run integration tests against ephemeral DB\n  - On push to main: trigger EAS Build for iOS/Android, push Docker image to registry, deploy to staging (e.g., Fly.io or AWS ECS)\n- **Supabase** used for:\n  - Auth (email/password, social providers) via its GoTrue API, accessed via custom NestJS guard or directly from mobile using Supabase JS client.\n  - File storage for business images, user avatars.\n- **Monitoring**: Basic health checks, logs shipped to a logging service (optional).\n\n## Testing Strategy\n- **Unit Tests**: Jest with mocking for services, repositories, and external gateways.\n- **Component Tests**: React Native Testing Library for UI components.\n- **Integration Tests**: Supertest for NestJS endpoints, using a test PostgreSQL database.\n- **E2E** (optional): Detox for critical flows (login → booking → confirmation).\n\n## Conclusion\nThe architecture separates concerns into clearly defined services, leverages a monorepo for code sharing, and uses proven scalable technologies (PostGIS for geo queries, Redis for caching, BullMQ for background jobs). The mobile side uses Expo Router for file‑based navigation and React Query for data synchronization, ensuring a responsive and maintainable codebase. This foundation supports rapid iteration while keeping the system ready for growth.\n",
  "summary": "A modular Nx monorepo architecture separating Expo mobile app, NestJS backend, shared libraries, PostgreSQL/PostGIS/Redis infrastructure, and clearly defined service boundaries for auth, business, booking, payment, notifications, reviews, provider portal, admin, and shared kernel."
}