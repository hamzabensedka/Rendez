{
  "content": "# Planity Clone — System Architecture\n\n**Author:** Jordan (Staff Architect)\n**Stack:** Expo, React Native, TypeScript, Expo Router, TanStack React Query, React Native Reanimated, NestJS, Prisma, PostgreSQL, PostGIS, Redis, Nx, pnpm, Docker Compose, GitHub Actions, Supabase, EAS Build, Jest\n\n## 1. High-Level Overview\nPlanity Clone is a beauty & wellness booking marketplace with four client surfaces:\n- Mobile App (Expo / React Native) for end clients\n- Provider Portal (web, Expo for web or Next.js inside Nx) for business owners\n- Admin Dashboard (web) for internal staff\n- Backend API (NestJS) + Workers (BullMQ) + Database (PostgreSQL + PostGIS)\n\nMonorepo managed by **Nx** with **pnpm**; local dev via **Docker Compose**; CI/CD via **GitHub Actions**; mobile build via **EAS Build**; auth & storage via **Supabase**.\n\n## 2. Monorepo Layout (Nx + pnpm)\n```\nplanity-clone/\n├── apps/\n│   ├── mobile/                # Expo React Native (client)\n│   ├── provider-portal/       # Expo Web / React (provider)\n│   ├── admin-dashboard/       # React Web (admin)\n│   ├── api/                   # NestJS API\n│   └── worker/                # BullMQ job runner\n├── libs/\n│   ├── shared/                # Types, UI kit, i18n, utils\n│   ├── config/                # Env, eslint, tsconfig bases\n│   ├── design-system/         # Cross-platform components\n│   └── api-client/            # Typed fetch wrappers\n├── services/\n│   ├── db/                    # Prisma schema + migrations\n│   └── redis/                 # Redis init scripts\n├── infra/\n│   ├── docker/                # Dockerfiles, compose.yml\n│   ├── github/                # Workflows\n│   └── supabase/              # Supabase config\n├── docs/\n│   ├── product.md\n│   └── architecture.md\n├── package.json\n├── pnpm-workspace.yaml\n├── nx.json\n└── tsconfig.base.json\n```\n\n## 3. Application Boundaries\n### 3.1 Mobile App (apps/mobile)\n- **Expo Router** for file-based navigation\n- **TanStack React Query** for server state\n- **React Native Reanimated** for animations (map pins, sheets)\n- Folders:\n```\napps/mobile/\n├── app/                       # Routes (Expo Router)\n│   ├── (guest)/               # browse, city\n│   ├── (auth)/                # login, signup\n│   ├── (tabs)/                # home, search, map, profile\n│   └── business/[id]/         # detail, booking\n├── components/                # Screen-level components\n├── features/                 # booking, favorites, appointments\n├── hooks/                     # useAuth, useBooking\n├── lib/                       # query clients, storage\n└── assets/\n```\n\n### 3.2 Provider Portal & Admin (apps/provider-portal, apps/admin-dashboard)\n- Shared React + Nx lib `design-system`\n- Provider: manage services, staff, hours, bookings\n- Admin: moderation, categories, metrics export\n\n### 3.3 API (apps/api — NestJS)\n- Modular by domain:\n```\napps/api/src/\n├── auth/                      # Supabase JWT, roles\n├── users/\n├── businesses/\n├── categories/\n├── search/                    # PostGIS queries\n├── bookings/                  # transactional lock\n├── availability/              # slot computation\n├── notifications/\n├── payments/                  # Stripe\n├── reviews/\n├── admin/\n└── common/                    # guards, decorators, prisma\n```\n\n### 3.4 Worker (apps/worker)\n- BullMQ consumers: reminders, slot cache, reports\n- Subscribes to Redis; emits notifications\n\n## 4. Data Layer\n- **PostgreSQL** + **PostGIS** for geo queries\n- **Prisma** schema in `services/db/prisma/schema.prisma`\n- **Redis** for caching, queues, session blacklist\n- Supabase handles auth tokens + file storage\n\n## 5. Cross-Cutting\n- `libs/shared`: TS types (User, Business, Slot), i18n, constants\n- `libs/design-system`: Button, Card, MapView wrappers\n- `libs/api-client`: generated from OpenAPI\n- CI: GitHub Actions runs lint, typecheck, Jest, EAS build\n\n## 6. Service Boundaries & Contracts\n- Mobile → API: HTTPS JSON; React Query caches\n- API → DB: Prisma; geo via `ST_DWithin`\n- API → Worker: BullMQ events (booking.created)\n- Worker → Notifications: FCM / email / SMS\n- Supabase: auth verify, avatar upload\n\n## 7. Priority Mapping\n- P0 in mobile, api, worker core, db schema\n- P1: payments, reviews, favorites, advanced portal\n- P2: analytics, loyalty\n\n## 8. Deployment\n- Docker Compose for local (postgres, redis, api, worker)\n- EAS Build for mobile binaries\n- GitHub Actions for test + build + deploy\n\n## 9. Testing\n- Jest in all libs/apps\n- API: e2e with Prisma test db\n- Mobile: component tests + Maestro flows",
  "summary": "Monorepo architecture for Planity Clone using Nx, NestJS, Expo and PostGIS with clear app, lib and service boundaries mapped to product priorities."
}