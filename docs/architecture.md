{
  "content": "# Planity Clone System Architecture\n\n## 1. Overview\nPlanity Clone is a full‑stack mobile‑first application that lets users discover, browse, and book services from local businesses. The architecture follows a clean, modular approach with clear separation of concerns between the mobile client, API gateway, business services, and data layer. The monorepo is managed with Nx and pnpm, enabling shared types, design system, and reusable libraries across mobile and backend.\n\n## 2. Technology Stack\n- **Mobile**: Expo (SDK 50), React Native, TypeScript, Expo Router, TanStack React Query, React Native Reanimated\n- **Backend**: NestJS (Node.js), TypeORM via Prisma, PostgreSQL + PostGIS, Redis, BullMQ (for background jobs)\n- **DevOps**: Docker Compose (local), GitHub Actions (CI/CD), EAS Build (Expo), Supabase (optional auth/storage fallback), Jest (testing)\n- **Monorepo**: Nx workspace, pnpm workspaces\n\n## 3. High‑Level Architecture\n```\n+-------------------+        +-------------------+        +-------------------+\n|   Mobile App      | <--->  |   API Gateway     | <--->  |   Business Services|\n| (Expo/React)      |        | (NestJS Nest)     |        | (NestJS Modules)   |\n+-------------------+        +-------------------+        +-------------------+\n        ^                           ^                           ^\n        |                           |                           |\n        |           +---------------+---------------+           |\n        |           |                               |           |\n        v           v                               v           v\n+-------------------+   +-------------------+   +-------------------+   +-------------------+\n|   Auth Service    |   |   Business Service|   |   Booking Service |   |   Payment Service   |\n+-------------------+   +-------------------+   +-------------------+   +-------------------+\n        |                           |                           |                           |\n        v                           v                           v                           v\n+-------------------+   +-------------------+   +-------------------+   +-------------------+\n|   PostgreSQL      |   |   Redis Cache     |   |   BullMQ Workers  |   |   File Storage    |\n+-------------------+   +-------------------+   +-------------------+   +-------------------+\n```\n\n- **API Gateway** (NestJS main app) exposes REST/GraphQL endpoints, handles authentication, rate limiting, and request routing to feature‑specific modules.\n- Each **Business Service** is a NestJS module encapsulating domain logic, DTOs, validators, and Prisma repositories.\n- **Background Jobs** (BullMQ) run in separate NestJS workers triggered by the gateway (e.g., sending notifications, slot recomputation).\n- **Redis** caches frequently read data (business listings, map tiles, session tokens) and serves as the broker for BullMQ.\n- **PostGIS** enables geo‑queries for map‑based search and proximity filtering.\n- **Mobile** communicates exclusively with the gateway via HTTPS; TanStack React Query manages server state, caching, and background updates.\n- **Expo Router** provides file‑based navigation, enabling deep linking and web‑compatible routes.\n- **React Native Reanimated** powers smooth animations for map interactions, modal transitions, and gesture‑driven UIs.\n\n## 4. Monorepo Structure (Nx + pnpm)\n```\nplanity-clone/\n├─ apps/\n│  ├─ mobile/                 # Expo React Native app\n│  │   ├─ src/\n│  │   │   ├─ assets/\n│  │   │   ├─ components/     # Shared UI (design system)\n│  │   │   ├─ hooks/\n│  │   │   ├─ navigation/     # Expo Router config (app/\n│  │   │   │   ├─ (tabs)/\n│  │   │   │   │   ├─ home.tsx\n│  │   │   │   │   ├─ search.tsx\n│  │   │   │   │   ├─ map.tsx\n│  │   │   │   │   ├─ bookings.tsx\n│  │   │   │   │   ├─ profile.tsx\n│  │   │   │   │   └─ ...\n│  │   │   │   ├─ (auth)/\n│  │   │   │   │   ├─ login.tsx\n│  │   │   │   │   ├─ register.tsx\n│  │   │   │   │   └─ reset-password.tsx\n│  │   │   │   ├─ business/\n│  │   │   │   │   ├─ [businessId]/\n│  │   │   │   │   │   ├─ details.tsx\n│  │   │   │   │   │   ├─ reviews.tsx\n│  │   │   │   │   │   └─ services.tsx\n│  │   │   │   │   └─ index.tsx   # list/search results\n│  │   │   │   ├─ booking/\n│  │   │   │   │   ├─ [serviceId]/\n│  │   │   │   │   │   ├─ provider-select.tsx\n│  │   │   │   │   │   ├─ date-time.tsx\n│  │   │   │   │   │   └─ confirm.tsx\n│  │   │   │   ├─ provider-portal/\n│  │   │   │   │   └─ ...\n│  │   │   │   ├─ admin/\n│  │   │   │   │   └─ ...\n│  │   │   ├─ lib/\n│  │   │   │   ├─ api/          # Generated RTK‑Query / React Query clients\n│  │   │   │   ├─ constants/\n│  │   │   │   ├─ theme/        # Design system tokens (colors, spacing, typography)\n│  │   │   │   ├─ utils/\n│  │   │   │   └─ types/        # Shared TS interfaces (imported from @planity/types)\n│  │   │   ├─ tests/\n│  │   │   └─ App.tsx\n│  │   ├─ expo.config.js\n│  │   ├─ eas.json\n│  │   └─ ...\n│  └─ api/                    # NestJS backend (Nx library wrapper)\n│      ├─ src/\n│      │   ├─ app.module.ts\n│      │   ├─ main.ts\n│      │   ├─ config/\n│      │   ├─ common/\n│      │   │   ├─ guards/\n│      │   │   ├─ interceptors/\n│      │   │   ├─ pipes/\n│      │   │   └─ dto/\n│      │   ├─ modules/\n│      │   │   ├─ auth/\n│      │   │   │   ├─ auth.controller.ts\n│      │   │   │   ├─ auth.service.ts\n│      │   │   │   ├─ dto/\n│      │   │   │   └─ auth.module.ts\n│      │   │   ├─ business/\n│      │   │   │   ├─ business.controller.ts\n│      │   │   │   ├─ business.service.ts\n│      │   │   │   ├─ dto/\n│      │   │   │   ├─ repositories/\n│      │   │   │   └─ business.module.ts\n│      │   │   ├─ booking/\n│      │   │   │   ├─ booking.controller.ts\n│      │   │   │   ├─ booking.service.ts\n│      │   │   │   ├─ dto/\n│      │   │   │   ├─ slot-computation/   # service for availability logic\n│      │   │   │   └─ booking.module.ts\n│      │   │   ├─ payment/\n│      │   │   │   ├─ payment.controller.ts\n│      │   │   │   ├─ payment.service.ts\n│      │   │   │   └─ payment.module.ts\n│      │   │   ├─ notification/\n│      │   │   │   ├─ notification.gateway.ts   # WebSocket / SSE\n│      │   │   │   ├─ notification.service.ts\n│      │   │   │   └─ notification.module.ts\n│      │   │   ├─ provider-portal/\n│      │   │   │   ├─ provider-portal.controller.ts\n│      │   │   │   ├─ provider-portal.service.ts\n│      │   │   │   └─ provider-portal.module.ts\n│      │   │   ├─ admin/\n│      │   │   │   ├─ admin.controller.ts\n│      │   │   │   ├─ admin.service.ts\n│      │   │   │   └─ admin.module.ts\n│      │   │   └─ shared/\n│      │   │       ├─ types/          # Re‑exported from @planity/types\n│      │   │       ├─ constants/\n│      │   │       └─ utils/\n│      │   ├─ prisma/\n│      │   │   ├─ schema.prisma\n│      │   │   └─ migrations/\n│      │   ├─ bullmq/\n│      │   │   ├─ workers/\n│      │   │   │   ├─ notification.worker.ts\n│      │   │   │   ├─ slot-recompute.worker.ts\n│      │   │   │   └─ email.worker.ts\n│      │   │   └─ queue.ts\n│      │   ├─ tests/\n│      │   └─ ...\n│      ├─ tsconfig.app.json\n│      ├─ Dockerfile\n│      └─ ...\n├─ libs/\n│  ├─ design-system/          # React Native UI library (styled components, tokens)\n│  ├─ ui/                     # Shared web‑compatible components (if web build needed)\n│  ├─ types/                  # Canonical TypeScript interfaces (used by mobile & api)\n│  ├─ utils/                  # Shared helpers (date, geo, validation)\n│  └─ ...\n├─ tools/\n│  ├─ eslint/\n│  ├─ jest/\n│  └─ ...\n├─ nx.json\n├─ package.json\n├─ pnpm-workspace.yaml\n└─ .github/\n    └─ workflows/\n        ├─ ci.yml            # lint, test, build\n        ├─ cd-mobile.yml     # EAS Build + upload to Expo\n        └─ cd-api.yml        # Docker build + push to registry\n```\n\n## 5. Service Boundaries & Responsibilities\n| Service | Domain | Key Responsibilities | Data Owned |
|---------|--------|----------------------|------------|
| **Auth** | User management | Registration, login, password reset, JWT issuance, role‑based access (user/provider/admin) | `users`, `sessions` |
| **Business** | Catalog & discovery | CRUD for businesses, categories, service offerings, geo‑search (PostGIS), rating aggregation | `businesses`, `categories`, `services`, `business_hours`, `reviews` |
| **Booking** | Appointment lifecycle | Slot computation, booking creation, cancellation/rescheduling, conflict detection, provider notification | `appointments`, `appointment_slots`, `providers` |
| **Payment** | Financial transactions | Integration with payment gateway (Stripe/PayPal), webhook handling, invoice generation, refunds | `payments`, `transactions` |
| **Notification** | Real‑time alerts | WebSocket/SSE connections, push notification scheduling (via BullMQ workers), email/SMS dispatch | `notifications` (optional) |
| **Provider Portal** | Business owner UI | Manage business profile, service catalog, staff schedules, view incoming appointments | Reads/writes same tables as Business & Booking (via scoped APIs) |
| **Admin** | Platform oversight | User/provider moderation, analytics, system settings, bulk operations | Reads all tables; writes to `settings`, `audit_logs` |
| **Shared** | Cross‑cutting | Design system, TypeScript types, utility functions, API client generators | None (library) |

Each service is a NestJS module with its own controller, service, DTOs, and Prisma repository. Communication between services occurs via internal method calls (in‑process) or through BullMQ jobs for asynchronous work, ensuring loose coupling and scalability.\n\n## 6. Data Model (Prisma Schema – highlights)\n```prisma\n// simplified\nmodel User {\n  id            String   @id @default(uuid())\n  email         String   @unique\n  passwordHash  String\n  role          Role     @default(USER)\n  createdAt     DateTime @default(now())\n  appointments  Appointment[]\n  favorites     Business[] @relation(\"FavoriteBusiness\")\n}\nmodel Business {\n  id            String   @id @default(uuid())\n  name          String\n  description   String?\n  address       String\n  lat           Float\n  lng           Float\n  location      Point    @db.Point\n  categories    Category[] @relation(\"BusinessCategories\")\n  services      Service[]\n  hours         BusinessHours[]\n  reviews       Review[]\n}\nmodel Category { id String @id @default(uuid()) name String }\nmodel Service { id String @id @default(uuid()) name String businessId String business Business @relation(fields:[businessId],references:[id]) }\nmodel Appointment {\n  id            String   @id @default(uuid())\n  user          User     @relation(fields:[userId],references:[id])\n  provider      User     @relation(fields:[providerId],references:[id])\n  service       Service  @relation(fields:[serviceId],references:[id])\n  startTime     DateTime\n  endTime       DateTime\n  status        AppointmentStatus\n  createdAt     DateTime @default(now())\n}\nmodel Review { id String @id @default(uuid()) rating Int comment String? user User business Business }\nenum Role { USER PROVIDER ADMIN }\nenum AppointmentStatus { PENDING CONFIRMED CANCELLED COMPLETED }\n```\nPostGIS is used via the `Point` type for location‑based queries.\n\n## 7. Caching & Real‑Time Strategy\n- **Redis** caches: business list (by category/location), user sessions, recent reviews, computed slot windows (TTL 5‑15 min).\n- **TanStack React Query** on mobile mirrors server cache with stale‑while‑revalidate, reducing redundant requests.\n- **Notifications**: WebSocket gateway (`NotificationGateway`) pushes real‑time updates (appointment changes, new messages). Fallback to push notifications (Expo Push) via BullMQ workers.\n- **Background Jobs**: Slot recomputation runs every minute for active businesses; email/SMS reminders triggered by cron‑like BullMQ schedules.\n\n## 8. DevOps & Deployment\n### Local Development\n```bash\n# start all services\ndocker compose up --build\n# mobile dev\nexpo start\n```\n`docker-compose.yml` defines: api (NestJS), postgres, redis, mailhog (for email preview), and optionally minio for file storage.\n\n### CI/CD\n- **GitHub Actions** workflows:\n  - `ci.yml`: lint (`eslint`), typecheck (`tsc`), unit tests (`jest`), build both mobile and api.\n  - `cd-mobile.yml`: on tag/release, runs `eas build --platform all --auto-submit`.\n  - `cd-api.yml`: builds Docker image, pushes to registry, triggers rolling update on staging/production.\n- **EAS Build** handles OTA updates for JavaScript bundle; native updates require store submission.\n- **Supabase** can be used as a fallback auth/storage layer if needed, but primary auth is JWT‑based in NestJS.\n\n### Production\n- Managed Kubernetes or ECS/Fargate for the API (auto‑scaling based on CPU/RAM).\n- Redis Elasticache for caching and BullMQ broker.\n- Managed PostgreSQL (RDS/Aurora) with PostGIS extension.\n- CDN (Cloudfront) for static assets (images, design system tokens).\n- Monitoring: Prometheus + Grafana; log aggregation via Loki or Elasticsearch.\n\n## 9. Testing Strategy\n- **Unit**: Jest for services, utilities, and React components.\n- **Integration**: API supertest endpoints; mobile React Native Testing Library with Jest.\n- **E2E**: Detox for critical user flows (login → search → booking).\n- **Load**: k6 scripts simulating peak booking traffic.\n\n## 10. Security Considerations\n- JWT access token (15 min) + refresh token (7 days) stored secure‑HttpOnly cookie (web) or SecureStore (mobile).\n- Role‑based guards (`AuthGuard`, `RolesGuard`).\n- Input validation via class‑validator & DTOs.\n- Rate limiting (nestjs/throttler).\n- CORS restricted to trusted domains.\n- Passwords hashed with bcrypt.\n- Sensitive data (payment details) never stored; tokenized via gateway.\n- HTTPS enforced; Helm/TLS termination at ingress.\n\n## 11. Scalability & Performance\n- Horizontal scaling of API stateless nodes.\n- Read replicas for PostgreSQL for heavy search queries.\n- Geo‑index (`GIST`) on `location` column for fast proximity search.\n- Cache‑aside pattern reduces DB load for catalog reads.\n- Background job workers scale independently based on queue depth.\n- Mobile optimizations: flat lists, memoized components, Reanimated worklet‑driven animations.\n\n## 12. Conclusion\nThis architecture provides a clean separation of concerns, leverages the strengths of the chosen stack (Expo for rapid mobile iteration, NestJS for modular backend, Prisma/PostGIS for robust data, Redis for speed), and supports future growth through modular services, background job processing, and DevOps automation. The Nx monorepo ensures shared code consistency, while Docker Compose, GitHub Actions, and EAS Build streamline local development and continuous delivery.\n",
  "summary": "A modular, Nx‑managed monorepo architecture separating Expo‑based mobile UI from NestJS microservices, backed by PostgreSQL/PostGIS, Redis, and BullMQ, with clear service boundaries, caching, real‑time notifications, and automated CI/CD via GitHub Actions and EAS Build."
}