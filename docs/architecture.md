{
  "content": "# Planity Clone — System Architecture\n\n**Author:** Jordan (Staff Architect)\n**Stack:** Expo, React Native, TypeScript, Expo Router, TanStack React Query, React Native Reanimated, NestJS, Prisma, PostgreSQL, PostGIS, Redis, Nx, pnpm, Docker Compose, GitHub Actions, Supabase, EAS Build, Jest\n\n## 1. Overview\nPlanity Clone is a mobile-first beauty & wellness booking marketplace. It consists of:\n- **Mobile Client** (Expo + RN) for consumers and providers (P0/P1).\n- **Backend API** (NestJS) with Prisma + PostgreSQL/PostGIS.\n- **Realtime/Async** via Redis + BullMQ.\n- **Infra** via Docker Compose, Supabase (Auth/Storage optional), EAS Build, GitHub Actions.\n\nMonorepo managed by **Nx + pnpm** for clear boundaries and shared types.\n\n## 2. Monorepo Layout (Nx + pnpm)\n```\nplanity-clone/\n├── apps/\n│   ├── mobile/            # Expo React Native app (consumer + provider)\n│   ├── api/               # NestJS backend\n│   ├── admin/             # (P1) Admin dashboard (web)\n│   └── jobs/              # BullMQ worker (or part of api)\n├── libs/\n│   ├── shared-types/      # TS interfaces, DTOs, enums\n│   ├── ui/                # Design system (RN components)\n│   ├── config/            # Env, constants\n│   └── utils/             # Helpers (date, geo)\n├── tools/\n│   ├── docker/            # Dockerfiles, compose\n│   └── ci/                # GitHub Actions workflows\n├── docs/\n│   ├── product.md\n│   └── architecture.md\n├── package.json\n├── pnpm-workspace.yaml\n├── nx.json\n└── docker-compose.yml\n```\n\n## 3. Mobile App (apps/mobile)\n```\napps/mobile/\n├── app/                   # Expo Router routes\n│   ├── (public)/\n│   │   ├── index.tsx      # Guest home\n│   │   ├── search.tsx\n│   │   └── business/[id].tsx\n│   ├── (auth)/\n│   │   ├── login.tsx\n│   │   └── register.tsx\n│   ├── (tabs)/\n│   │   ├── home.tsx\n│   │   ├── map.tsx\n│   │   ├── favorites.tsx\n│   │   └── profile.tsx\n│   ├── booking/\n│   │   └── [businessId].tsx\n│   └── _layout.tsx\n├── src/\n│   ├── api/               # React Query hooks\n│   ├── components/        # Feature components\n│   ├── stores/            # Local state (Zustand)\n│   └── lib/               # Client config\n├── assets/\n└── eas.json\n```\n**Boundaries:** Only talks to API via HTTPS; uses TanStack Query for cache; Reanimated for map/transitions.\n\n## 4. Backend API (apps/api)\n```\napps/api/\n├── src/\n│   ├── modules/\n│   │   ├── auth/          # JWT, signup, OAuth\n│   │   ├── user/          # Profile, favorites\n│   │   ├── business/      # CRUD, categories\n│   │   ├── search/        # Text + PostGIS map\n│   │   ├── booking/       # Slots, appointments\n│   │   ├── provider/      # Portal logic\n│   │   ├── review/        # P1\n│   │   ├── payment/       # P1 Stripe\n│   │   └── notification/  # Email/push\n│   ├── common/            # Guards, pipes, decorators\n│   ├── jobs/              # BullMQ producers\n│   ├── prisma/            # Service, migrations\n│   └── main.ts\n├── test/                  # Jest\n└── Dockerfile\n```\n**Service Boundaries:** Modules own entities; Prisma used in data layer; Redis for cache/queues.\n\n## 5. Data Layer\n- **PostgreSQL** + **PostGIS**: businesses, geo queries.\n- **Prisma**: schema in `apps/api/prisma/schema.prisma`.\n- **Redis**: session blacklist, search cache, BullMQ.\n\n## 6. Background Jobs\nBullMQ queues in Redis:\n- `reminders` — appointment reminders\n- `emails` — verification, reset\n- `slot-cleanup` — expired holds\nWorker in `apps/api/src/jobs`.\n\n## 7. Infra & CI\n- `docker-compose.yml`: postgres, redis, api.\n- GitHub Actions: lint, test (Jest), build (EAS), deploy.\n- Supabase: optional auth/storage.\n- EAS Build: mobile builds.\n\n## 8. Cross-Cutting\n- **shared-types**: API DTOs reused in mobile.\n- **ui lib**: buttons, cards.\n- Auth: JWT in header; guard in NestJS.\n\n## 9. P0 Scope Mapping\nAll P0 features map to modules above. Map search uses PostGIS; booking uses availability service.\n\n## 10. Summary\nClean Nx monorepo with Expo mobile, NestJS API, Prisma/PostGIS, Redis jobs; strict module boundaries and shared types.",
  "summary": "Nx monorepo architecture for Planity Clone with Expo mobile, NestJS API, Prisma/PostGIS, Redis/BullMQ, and clear service boundaries."
}