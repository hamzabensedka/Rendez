{
  "content": "# Planity Clone — System Architecture\n\n## 1. Introduction\nPlanity Clone is a mobile-first beauty & wellness booking platform. This document defines the system architecture using Expo (React Native, TypeScript, Expo Router, TanStack React Query, Reanimated) for the client, and NestJS, Prisma, PostgreSQL + PostGIS, Redis, BullMQ for the backend. Monorepo managed via Nx and pnpm; Docker Compose for local infra; GitHub Actions for CI; EAS Build for mobile; Supabase for auth/storage; Jest for tests.\n\n## 2. High-Level Architecture\n- **Mobile App**: Expo RN client (features, UI kit, API client, queries).\n- **API Gateway / BFF**: NestJS app exposing REST/GraphQL; auth, business, booking, payments, notifications.\n- **Data Layer**: PostgreSQL with PostGIS; Prisma ORM; Redis cache/sessions; BullMQ queues.\n- **Jobs**: NestJS worker using BullMQ (reminders, cleanup, reports).\n- **Infra**: Docker Compose (postgres, redis), Supabase (auth+storage), EAS Build, GitHub Actions.\n\n## 3. Monorepo Layout (Nx + pnpm)\n```\nplanity-clone/\n├── apps/\n│   ├── mobile/                # Expo React Native app\n│   ├── api/                   # NestJS API\n│   ├── worker/                # NestJS BullMQ worker\n│   └── admin-web/             # (optional) Admin dashboard (future)\n├── libs/\n│   ├── shared-types/          # TS types (P0: 3.12)\n│   ├── ui/                    # Design system components (P0: 3.12)\n│   ├── config/                # Shared config, env schemas\n│   └── utils/                 # Helpers (date, geo)\n├── services/\n│   ├── auth/                  # Supabase + JWT (P0: 3.1)\n│   ├── business/              # CRUD, search, map (P0/P1: 3.3,3.4,3.5)\n│   ├── booking/               # Flow, slots (P0: 3.7,3.11)\n│   ├── payments/              # Stripe (P0: 3.14)\n│   ├── notifications/         # Push/email/SMS (P1: 3.15)\n│   └── admin/                 # Oversight (P1: 3.17)\n├── infra/\n│   ├── docker-compose.yml\n│   └── supabase/\n├── .github/workflows/         # CI pipelines\n├── package.json\n├── nx.json\n└── pnpm-workspace.yaml\n```\n\n## 4. Mobile App Structure (apps/mobile)\n```\napps/mobile/\n├── app/                       # Expo Router\n│   ├── (guest)/               # Guest browse (P1: 3.2)\n│   ├── (auth)/                # Login, OTP (P0: 3.1)\n│   ├── (tabs)/                # Home, Search, Favs, Profile\n│   ├── business/[id]/         # Detail (P0: 3.5)\n│   ├── booking/               # Flow (P0: 3.7)\n│   └── _layout.tsx\n├── components/                # UI kit usage\n├── features/                  # Feature modules\n│   ├── auth/\n│   ├── search/\n│   ├── map/\n│   ├── booking/\n│   └── profile/\n├── lib/                       # API client, query hooks\n│   ├── api.ts\n│   └── queries/\n├── assets/\n└── eas.json\n```\n\n## 5. Backend Structure (apps/api)\n```\napps/api/\n├── src/\n│   ├── modules/\n│   │   ├── auth/             # Supabase guard, JWT\n│   │   ├── business/         # Controller, Service, Prisma\n│   │   ├── booking/          # Availability engine\n│   │   ├── payments/         # Stripe webhook\n│   │   ├── notifications/    # Expo push\n│   │   └── admin/\n│   ├── prisma/               # Schema, migrations\n│   ├── redis/                # Cache module\n│   ├── bull/                 # Queue defs\n│   └── main.ts\n└── test/\n```\n\n## 6. Service Boundaries\n- **Auth Service**: Supabase email/OTP/OAuth; issues JWT; mobile stores securely (Expo SecureStore). Boundaries: no business logic.\n- **Business Service**: Owned by Business Owner; categories tree (P1: 3.6); search uses PostGIS (ST_Distance). Map pins from viewport query.\n- **Booking Service**: Computes slots from hours+breaks+booked (P0: 3.11); validates no overlap; emits events to notifications.\n- **Payments**: Stripe; webhook updates booking status; receipts via email.\n- **Notifications**: BullMQ jobs send push (Expo), email, SMS; opt-out flag.\n- **Admin**: Separate role; suspends businesses, exports.\n\n## 7. Data Model (Prisma + PostGIS)\n- User (id, email, phone, role)\n- Business (id, geo geometry(Point), categoryId)\n- Category (id, parentId, name) – tree\n- Service (id, businessId, duration, price)\n- Booking (id, userId, serviceId, slotStart, status)\n- Review (id, bookingId, stars, text)\n- Favorite (userId, businessId)\n\n## 8. Cross-Cutting\n- **Shared Types**: libs/shared-types consumed by mobile + api.\n- **UI Kit**: libs/ui with Reanimated animations.\n- **Cache**: Redis for search results, slot caches.\n- **CI**: GitHub Actions runs lint, typecheck, Jest; EAS Build on release.\n\n## 9. Priority Mapping\nP0: auth, search, detail, booking, slots, types, payments, provider portal. P1: guest, map, categories, profile, reviews, notifications, admin, jobs. P2: favorites.\n\n## 10. Deployment\nDocker Compose for local; Supabase cloud; EAS for mobile; API on container orch (future).",
  "summary": "Architecture defines Nx monorepo with Expo mobile and NestJS backend, Prisma/PostGIS/Redis, clear service boundaries, and folder scaffold for Planity Clone."
}