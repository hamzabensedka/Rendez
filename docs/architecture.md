{
  "content": "# Planity Clone System Architecture\n\n## 1. Overview\nPlanity Clone is a full‑stack mobile‑first application that lets users discover, book, and manage appointments with service‑based businesses. The architecture follows a clean, modular approach: a React Native/Expo mobile client, a NestJS API server, and supporting infrastructure (PostgreSQL/PostGIS, Redis, BullMQ, Docker, CI/CD). Code is organized in an Nx monorepo using pnpm for efficient dependency management.\n\n## 2. Tech Stack\n- **Mobile**: Expo (SDK 50), React Native, TypeScript, Expo Router, TanStack React Query, React Native Reanimated\n- **Backend**: NestJS (Node.js), TypeORM via Prisma, PostgreSQL + PostGIS extension, Redis, BullMQ (background jobs)\n- **DevOps**: Docker Compose (local dev), GitHub Actions (CI/CD), EAS Build (Expo builds), Supabase (auth & storage fallback), Nx (monorepo tooling), pnpm (workspace manager), Jest (testing)\n\n## 3. High‑Level Architecture\n```\n+-------------------+        HTTPS/WSS        +-------------------+\n|   Mobile Client   | <--------------------> |   API Gateway     |\n| (Expo RN)         |        (REST/GraphQL)    | (NestJS)          |\n+-------------------+        +----------------> +-------------------+\n                         |                       |\n                         |   Internal RPC/Events |\n                         v                       v\n                +-------------------+   +-------------------+\n                |   Business Service|   |   Booking Service   |\n                +-------------------+   +-------------------+\n                         |                       |\n                         v                       v\n                +-------------------+   +-------------------+\n                |  PostgreSQL/PostGIS|   |  Redis Cache        |\n                +-------------------+   +-------------------+\n                         |                       |\n                         v                       v\n                +-------------------+   +-------------------+\n                |   BullMQ Workers  |   |   Supabase (Auth)   |\n                +-------------------+   +-------------------+\n```\n- **Mobile Client** consumes REST/JSON endpoints (optionally GraphQL via NestJS) and uses React Query for server state, Reanimated for gestures/animations.\n- **API Gateway** is the NestJS monolith exposing domain‑specific controllers; it validates JWTs, handles rate limiting, and forwards commands to services.\n- **Business Service** manages CRUD for businesses, categories, locations (PostGIS), and slot computation.\n- **Booking Service** handles appointment creation, rescheduling, cancellation, payment integration, and notifications.\n- **Redis** caches frequent reads (business listings, slot availability, user sessions).\n- **BullMQ** processes background jobs: email/SMS notifications, payment webhook handling, slot recomputation, analytics aggregation.\n- **Supabase** provides optional OAuth social login and file storage for user‑uploaded assets (profile pics, business photos).\n- **Docker Compose** defines services for local development: api, db, redis, worker.\n- **GitHub Actions** runs lint, test, build, and pushes Docker images; triggers EAS Build for mobile binaries.\n- **EAS Build** creates iOS/Android binaries from the Expo workspace.\n\n## 4. Folder Structure (Nx Monorepo)\n```\nplanity-clone/\n├── apps/\n│   ├── mobile/                 # Expo React Native app\n│   │   ├── src/\n│   │   │   ├── assets/         # Images, fonts, icons\n│   │   │   ├── components/     # Shared UI components (design system)\n│   │   │   ├── navigation/     # Expo Router route definitions\n│   │   │   ├── hooks/          # Custom React Query wrappers, auth hooks\n│   │   │   ├── screens/        # Screen components per feature\n│   │   │   ├── store/          # Optional Zustand/Jotai for UI state\n│   │   │   ├── utils/          # Formatters, constants, API client\n│   │   │   └── types/          # Shared TypeScript interfaces (generated from Prisma)\n│   │   ├── app.json\n│   │   ├── eas.json\n│   │   └── tsconfig.json\n│   └── api/                    # NestJS backend\n│       ├── src/\n│       │   ├── auth/           # JWT strategy, Supabase OAuth guard\n│       │   ├── business/       # Business CRUD, geo search, slot computation\n│       │   ├── booking/        # Appointment lifecycle, payment integration\n│       │   ├── user/           # Profile, favorites, history\n│       │   ├── notification/   # Notification preferences, templates\n│       │   ├── admin/          # Admin dashboard APIs\n│       │   ├── common/         # DTOs, validators, interceptors, guards\n│       │   ├── jobs/           # BullMQ processors (email, payment, analytics)\n│       │   ├── prisma/         # Prisma schema, migrations\n│       │   ├── main.ts\n│       │   └── tsconfig.json\n│       ├── test/\n│       │   ├── unit/\n│       │   └── e2e/\n│       ├── Dockerfile\n│       └── nest-cli.json\n├── libs/\n│   ├── design-system/          # Reusable UI primitives (styled with React Native Reanimated)\n│   ├── types/                  # Generated TypeScript schemas (Prisma → TS)\n│   ├── utils/                  # Shared backend utilities (geo helpers, slot algo)\n│   └── test-helpers/\n├── prisma/\n│   ├── schema.prisma           # DB models incl. PostGIS extensions\n│   └── migrations/\n├── docker/\n│   ├── compose.yml             # api, db, redis, worker services\n│   └── Dockerfile.worker\n├── .github/\n│   └── workflows/\n│       ├── ci.yml              # lint, test, build\n│       └── cd.yml              # Docker push, EAS Build trigger\n├── nx.json\n├── package.json (pnpm workspace root)\n└── tsconfig.base.json\n```\n\n## 5. Service Boundaries\n| Domain          | Responsibility                                                                 | Key Tables (Prisma)                     | External Calls                     |
|-----------------|--------------------------------------------------------------------------------|----------------------------------------|------------------------------------|
| **Auth**        | User registration, login, password reset, social OAuth (Supabase), JWT issuance | `User`, `Session`                      | Supabase Auth, Email provider      |
| **Business**    | Business CRUD, category taxonomy, geo‑search (PostGIS), schedule management    | `Business`, `Category`, `BusinessHours`, `Service` | Mapbox/Google Geocoding (optional) |
| **Booking**     | Appointment creation, rescheduling, cancellation, payment processing, slot generation | `Appointment`, `Payment`, `Invoice`    | Payment gateway (Stripe/PayPal), Webhook listener |
| **User**        | Profile management, favorites, appointment history, notification preferences   | `UserProfile`, `Favorite`, `UserFavorite`, `NotificationPreference` | — |
| **Notification**| Template management, preference storage, dispatch via BullMQ workers            | `NotificationTemplate`, `NotificationLog` | Email/SMS provider (SendGrid, Twilio) |
| **Admin**       | Content moderation, analytics, user/business bulk actions                     | `AuditLog`, `AnalyticsSnapshot`        | — |
| **Worker**      | BullMQ processors: send notifications, reconcile payments, recompute slots, aggregate metrics | — | Same as above (email, payment, geo) |

Each domain is a NestJS module with its own controllers, services, DTOs, and guards. Modules communicate via **internal NestJS event emitter** or **direct service injection** when synchronous; otherwise they publish jobs to BullMQ queues.

## 6. Data Model Highlights (Prisma)\n```prisma\nmodel User {\n  id            String   @id @default(uuid())\n  email         String   @unique\n  phone         String?\n  passwordHash  String\n  role          Role     @default(USER)\n  createdAt     DateTime @default(now())\n  updatedAt     DateTime @updatedAt\n  profile       UserProfile?\n  favorites     BusinessFavorite[]\n  appointments  Appointment[]\n}\nmodel Business {\n  id            String   @id @default(uuid())\n  name          String\n  description   String?\n  address       String\n  location      Point    @db.Point   // PostGIS\n  categoryId    String\n  category      Category @relation(fields: [categoryId], references: [id])\n  services      Service[]\n  hours         BusinessHours[]\n}\nmodel Appointment {\n  id            String   @id @default(uuid())\n  userId        String\n  businessId    String\n  serviceId     String\n  startsAt      DateTime\n  endsAt        DateTime\n  status        AppointmentStatus\n  payment       Payment?\n  createdAt     DateTime @default(now())\n}\n```\n- PostGIS enables radius queries: `ST_DWithin(location, :point, :distance)`.\n- Prisma generates TypeScript types exported to `libs/types` for mobile consumption via a generated OpenAPI spec or custom codegen.\n\n## 7. Caching Strategy (Redis)\n- **Business List**: key `biz:list:{lat}:{lon}:{radius}:{category}` → JSON array, TTL 5 min.\n- **Slot Availability**: key `slot:{businessId}:{date}` → bitmap of 30‑min slots, TTL 15 min.\n- **User Session**: JWT stored as `session:{userId}` with refresh token mapping.\n- Cache‑aside pattern: service attempts Redis first, falls back to DB, then writes back.\n\n## 8. Background Jobs (BullMQ)\nQueues defined in `apps/api/src/jobs/queues.ts`:\n- `email-notification`: sends templated emails/SMS.\n- `payment-processing`: confirms webhook events, updates `Payment` status.\n- `slot-recalc`: triggered on business hour changes or appointment CRUD.\n- `analytics-aggregate`: nightly roll‑up of KPIs.\nWorkers are separate processes (`docker-compose.yml` worker service) to isolate CPU‑heavy tasks.\n\n## 9. DevOps & CI/CD\n- **Local**: `docker compose up --build` spins API, PostgreSQL, Redis, worker.\n- **Testing**: Jest unit tests for services & utilities; E2E tests using Detox for mobile flows; GitHub Actions runs on every PR.\n- **Build**: `nx run mobile:build` (expo export) → EAS Build produces `.apk`/`.ipa`.\n- **Deploy**: Docker images pushed to registry; Kubernetes/ECS (not shown) pulls latest tag; migrations run via `prisma migrate deploy` in entrypoint.\n- **Monitoring**: Health endpoints (`/health`) exposed; logs via sidecar).\n\n## 10. Security Considerations\n- JWTs signed with RS256; private key stored in Vault/secret manager.\n- Passwords hashed via bcrypt.\n- Rate limiting via NestJS `ThrottlerGuard`.\n- Input validation with class‑validator & DTOs.\n- SQL injection prevented by Prisma ORM.\n- File uploads (Supabase) scanned for malware; served via signed URLs with expiry.\n- CORS restricted to allowed origins (expo dev, production domains).\n\n## 11. Shared Types & Design System\n- `libs/types` contains generated TS interfaces (from Prisma) used by both mobile (via codegen) and backend.\n- `libs/design-system` exports primitive components (Button, Input, Card) styled with Reanimated for smooth gestures; themeable via Context.\n- Ensures UI consistency and reduces duplication.\n\n## 12. Scalability & Extensibility\n- Horizontal scaling of API stateless nodes behind a load balancer.\n- Read replicas for PostgreSQL to scale geo‑queries.\n- Redis Cluster for cache scaling.\n- Adding new domain: create a new NestJS module under `apps/api/src`, add Prisma model, expose controller, optionally add a BullMQ queue.\n- Mobile: feature‑based folders under `apps/mobile/src/screens`; navigation via Expo Router automatically picks up file‑based routes.\n\n## 13. Conclusion\nThis architecture cleanly separates concerns, leverages the strengths of the chosen stack, and provides a solid foundation for rapid iteration, testing, and deployment. The Nx monorepo with pnpm ensures fast builds and shared code, while Docker, GitHub Actions, and EAS Build automate the path from commit to production.\n",
  "summary": "A modular, clean‑architecture plan for Planity Clone using Expo/React Native mobile, NestJS/Prisma/PostGIS backend, Redis caching, BullMQ jobs, Docker, GitHub Actions, and EAS Build, organized in an Nx pnpm monorepo with clear folder boundaries and service layers."
}