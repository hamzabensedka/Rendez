{
  "content": "# Planity Clone — System Architecture\n\n**Author:** Jordan (Architect)\n**Stack:** Expo, React Native, TypeScript, Expo Router, TanStack React Query, React Native Reanimated, NestJS, Prisma, PostgreSQL, PostGIS, Redis, Nx, pnpm, Docker Compose, GitHub Actions, Supabase, EAS Build, Jest\n\n## 1. High-Level Overview\nPlanity Clone is a mobile-first beauty & wellness booking marketplace. The system is split into:\n- **Mobile App** (Expo + RN): client-facing iOS/Android.\n- **Backend API** (NestJS): business logic, auth, payments, jobs.\n- **Provider Portal & Admin** (web, optional in M2): served via same NestJS or separate Nx app.\n- **Data Layer**: PostgreSQL + PostGIS, Redis (cache/queue), Supabase (auth辅助/storage).\n- **Infra**: Docker Compose (local), GitHub Actions (CI), EAS Build (mobile).\n\nMonorepo managed by **Nx** with **pnpm** workspaces.\n\n## 2. Monorepo Layout (Nx + pnpm)\n```\nplanity-clone/\n├── apps/\n│   ├── mobile/            # Expo Router RN app\n│   ├── api/               # NestJS backend\n│   ├── provider-portal/   # Web (M2, optional Nx React)\n│   └── admin-dashboard/   # Web (M2)\n├── libs/\n│   ├── shared-types/      # TS types (AC1 no any)\n│   ├── ui/                # RN design system + Storybook\n│   ├── config/            # env, constants\n│   └── utils/             # date, geo helpers\n├── services/\n│   ├── auth/              # NestJS auth module\n│   ├── business/          # biz, categories, search\n│   ├── booking/           # slots, appointments\n│   ├── payments/          # Stripe\n│   ├── notifications/     # FCM/SMS/email\n│   └── jobs/              # BullMQ\n├── prisma/\n│   ├── schema.prisma\n│   └── seed/\n├── docker/\n│   ├── docker-compose.yml\n│   └── Dockerfile.api\n├── .github/workflows/\n└── docs/\n```\n\n## 3. Mobile App (apps/mobile)\n- **Expo Router** file-based routing under `app/`.\n- **TanStack React Query** for server state; **Reanimated** for gallery/sheet.\n- Folder:\n```\napps/mobile/\n├── app/\n│   ├── (guest)/home, explore\n│   ├── (auth)/login, register\n│   ├── business/[id]\n│   ├── search\n│   ├── map (M2)\n│   ├── booking/\n│   ├── profile/\n│   └── _layout.tsx\n├── src/\n│   ├── api/ (query hooks)\n│   ├── components/\n│   └── stores/ (zustand if needed)\n└── eas.json\n```\nService boundaries: UI only; calls API via HTTPS; no direct DB.\n\n## 4. Backend (apps/api + services/*)\nNestJS modular:\n- `AuthModule`: JWT, OTP, OAuth (Supabase), roles.\n- `BusinessModule`: CRUD, categories tree, PostGIS search.\n- `BookingModule`: slot engine (Redis cached), appointments.\n- `PaymentModule`: Stripe webhooks.\n- `NotificationModule`: templates, FCM/SMS/email.\n- `JobsModule`: BullMQ queues (notify, availability, report).\n\n## 5. Data Model (Prisma + PostGIS)\nEntities: User, Business, Service, Slot, Booking, Review, Payment, Notification.\n- PostGIS: Business.location (geometry).\n- Redis: slot cache, session, recent searches.\n\n## 6. Service Boundaries\n- Mobile ↔ API: REST/JSON.\n- API ↔ DB: Prisma.\n- API ↔ Redis: cache + BullMQ.\n- Jobs: decoupled via queue, no sync coupling.\n- Shared-types lib enforces contracts.\n\n## 7. CI/CD & Infra\n- GitHub Actions: lint, typecheck, Jest, build.\n- Docker Compose: postgres, redis, api.\n- EAS Build: mobile releases.\n\n## 8. Milestones Mapping\nM1 uses Must modules; M2 adds map, favorites, admin.\n\n## 9. Testing\nJest unit for libs/services; E2E for API; Storybook for UI.",
  "summary": "Architecture defines an Nx monorepo with Expo mobile, NestJS backend, PostGIS/Redis data layer, and clear service boundaries mapped to Planity Clone's Must/Should features."
}