{
  "content": "# Planity Clone — System Architecture\n\n**Author:** Jordan (Staff Architect)\n**Stack:** Expo, React Native, TypeScript, Expo Router, TanStack React Query, React Native Reanimated, NestJS, Prisma, PostgreSQL, PostGIS, Redis, Nx, pnpm, Docker Compose, GitHub Actions, Supabase, EAS Build, Jest\n\n## 1. High-Level Overview\n\nPlanity Clone is a mobile-first salon/beauty booking platform. The system is split into:\n- **Mobile App** (Expo + RN): client-facing iOS/Android app.\n- **API Backend** (NestJS): business logic, auth, booking, payments.\n- **Data Layer** (PostgreSQL + PostGIS + Prisma + Redis): persistence, geo, cache.\n- **Async Workers** (BullMQ + Redis): notifications, jobs.\n- **Web Portals** (Provider/Admin): served via Expo Web or separate Next/Nest views (P1).\n- **Infra**: Docker Compose, GitHub Actions, Supabase (Auth/Storage), EAS Build.\n\nMonorepo managed by **Nx** with **pnpm** workspaces.\n\n## 2. Monorepo Layout (Nx + pnpm)\n\n```\nplanity-clone/\n├── apps/\n│   ├── mobile/            # Expo RN app (Expo Router)\n│   ├── api/               # NestJS backend\n│   ├── worker/            # BullMQ consumer (NestJS or TS script)\n│   └── web-portal/        # Provider/Admin (P1, Expo Web or React)\n├── libs/\n│   ├── shared-types/      # TS DTOs, domain models\n│   ├── ui/                # RN design system (Button, Card...)\n│   ├── config/            # Env, constants\n│   └── prisma/            # Prisma schema + client\n├── tools/\n│   ├── docker/            # Dockerfiles, compose\n│   └── github/            # Workflows\n├── docs/\n│   ├── product.md\n│   └── architecture.md\n├── package.json\n├── pnpm-workspace.yaml\n├── nx.json\n└── docker-compose.yml\n```\n\n## 3. Mobile App (apps/mobile)\n\n```\napps/mobile/\n├── app/                  # Expo Router screens\n│   ├── (public)/         # guest browse, search, detail\n│   ├── (auth)/           # login, register, otp\n│   ├── (tabs)/           # home, search, bookings, profile\n│   └── _layout.tsx\n├── components/           # feature components\n├── hooks/                # React Query hooks\n├── lib/                  # api client, storage, reanimated utils\n├── assets/\n├── eas.json\n└── package.json\n```\n\n- **Expo Router** for file-based navigation.\n- **TanStack React Query** for server state; **React Native Reanimated** for animations (bottom sheets, transitions).\n- Secure token storage via Expo SecureStore.\n\n## 4. API Backend (apps/api)\n\n```\napps/api/\n├── src/\n│   ├── modules/\n│   │   ├── auth/         # JWT, OTP, social, lockout\n│   │   ├── user/         # profile, preferences\n│   │   ├── business/     # CRUD, detail\n│   │   ├── category/     # taxonomy\n│   │   ├── service/      # offerings\n│   │   ├── booking/      # slots, confirm, cancel\n│   │   ├── availability/ # slot computation\n│   │   ├── payment/      # Stripe\n│   │   ├── review/       # P1\n│   │   ├── favorite/     # P1\n│   │   └── notification/ # P1\n│   ├── common/           # guards, pipes, decorators\n│   ├── prisma/           # service\n│   ├── redis/            # cache, queue\n│   └── main.ts\n```\n\n- NestJS modular by domain. Each module owns controller, service, dto.\n- Prisma for DB access; PostGIS for geo queries (via `@prisma/client` + raw or extension).\n- Redis for session cache, slot lock, BullMQ.\n\n## 5. Data Layer\n\n- **PostgreSQL** + **PostGIS**: businesses (geom), appointments, users.\n- **Prisma** schema in `libs/prisma/schema.prisma`.\n- **Redis**: cache (search results, slots), locks (double-booking), queues.\n- **Supabase**: Auth fallback (optional), Storage for images.\n\n## 6. Service Boundaries\n\n| Service | Responsibility | Depends On |\n|--------|----------------|-----------|\n| Auth | Identity, tokens, OTP | User, Redis |\n| Business | Profiles, geo search | Category, PostGIS |\n| Booking | Create/cancel, lock | Availability, Payment, User |\n| Availability | Slot generation | Business, Redis |\n| Payment | Stripe charges | Booking, User |\n| Worker | Emails, reminders | Redis, DB |\n\n## 7. Key Flows\n\n- **Booking**: Mobile → API Booking → Availability (compute) → Redis lock → Payment → Confirm → Worker notify.\n- **Search**: Mobile → API Business (PostGIS radius) → Redis cache → Results.\n\n## 8. Infra & CI\n\n- `docker-compose.yml`: postgres, redis, api, worker.\n- GitHub Actions: lint, test (Jest), build (EAS), deploy.\n- EAS Build for mobile releases.\n\n## 9. Testing\n\n- Jest for unit/integration (API, libs).\n- E2E (Detox/P1) for mobile.\n\n## 10. Summary\n\nClean Nx monorepo; mobile (Expo) + NestJS API + Prisma/PostGIS/Redis; separated domains; Docker/GitHub/EAS for delivery.",
  "summary": "Nx monorepo with Expo mobile, NestJS API, Prisma/PostGIS/Redis data layer, and BullMQ workers for a scalable Planity-style booking architecture."
}