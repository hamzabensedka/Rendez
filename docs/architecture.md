{
  "content": "# System Architecture for Planity Clone\n\n## Overview\nThis document outlines the architecture of the Planity Clone mobile application built with Expo, React Native, TypeScript, Expo Router, TanStack React Query, React Native Reanimated, NestJS, Prisma, PostgreSQL, PostGIS, Redis, Nx, pnpm, Docker Compose, GitHub Actions, Supabase, EAS Build, and Jest.\n\n## Technology Stack\n- **Mobile**: Expo (SDK 49), React Native, TypeScript, Expo Router, TanStack React Query, React Native Reanimated\n- **Backend**: NestJS (Node.js), Prisma ORM, PostgreSQL with PostGIS extension, Redis\n- **DevOps**: Nx monorepo, pnpm workspace, Docker Compose for local dev, GitHub Actions CI/CD, Supabase for auth & storage (optional), EAS Build for OTA updates, Jest for testing\n- **Other**: BullMQ for background jobs\n\n## High-Level Components\n1. **Mobile Client** – Expo app handling UI, navigation, state, animations, offline caching.\n2. **API Gateway** – Thin NestJS module that routes requests to domain services.\n3. **Domain Services** – Separate NestJS services for Auth, Business, Booking, Payments, Notifications, Admin, Provider Portal.\n4. **Data Layer** – Prisma models backed by PostgreSQL/PostGIS; Redis for caching and job queues.\n5. **Infrastructure** – Docker Compose defines services (api, db, redis, worker). GitHub Actions builds, tests, and deploys. EAS Build creates iOS/Android binaries. Supabase provides optional auth and file storage.\n\n## Folder Structure (Nx Monorepo)\n```\nplanity-clone/\n├─ apps/\n│   ├─ mobile/                 # Expo React Native app\n│   │   ├─ src/\n│   │   │   ├─ assets/         # Images, fonts, icons\n│   │   │   ├─ components/     # Shared UI components (design system)\n│   │   │   ├─ hooks/          # Custom React hooks\n│   │   │   ├─ lib/            # Utilities, API client, query keys, constants\n│   │   │   ├─ navigation/     # Expo Router route configs (app/)\n│   │   │   ├─ screens/        # Screen components per feature\n│   │   │   ├─ store/          # React Query providers and global state\n│   │   │   ├─ theme/          # Theme tokens, colors, typography\n│   │   │   └─ types/          # Shared TypeScript interfaces (generated from Prisma)\n│   │   ├─ eas.json\n│   │   ├─ app.json\n│   │   ├─ tsconfig.json\n│   │   └─ package.json\n│   └─ api/                    # NestJS backend\n│       ├─ src/\n│       │   ├─ auth/           # Auth service (register, login, reset)\n│       │   ├─ business/       # Business CRUD, search, geospatial queries\n│       │   ├─ booking/        # Booking creation, slot computation\n│       │   ├─ payment/        # Payment gateway integration\n│       │   ├─ notification/   # Notification service (email, push)\n│       │   ├─ provider/       # Provider portal endpoints\n│       │   ├─ admin/          # Admin dashboard endpoints\n│       │   ├─ shared/         # DTOs, validation pipes, guards, interceptors\n│       │   ├─ prisma/         # Prisma schema and client\n│       │   ├─ queues/         # BullMQ job definitions\n│       │   ├─ main.ts\n│       │   └─ tsconfig.json\n│       ├─ test/\n│       │   ├─ unit/\n│       │   └─ e2e/\n│       ├─ dockerfile\n│       ├─ prisma/\n│       │   └─ schema.prisma\n│       └─ package.json\n├─ libs/\n│   ├─ design-system/          # Shared UI library (React Native)\n│   ├─ types/                  # Generated TypeScript models from Prisma (used by mobile & api)\n│   ├─ utils/                  # Cross-platform utilities (date helpers, geo)\n│   └─ eslint-config/          # Shared ESLint rules\n├─ docker-compose.yml\n├─ nx.json\n├─ package.json\n└─ README.md\n```\n\n## Service Boundaries\n- **Auth Service** – Handles user registration, login, password reset, JWT issuance. Uses Supabase Auth (optional) or custom bcrypt hashing.\n- **Business Service** – Manages business profiles, categories, services, pricing, hours. Exposes search endpoints with PostGIS for location‑based queries (radius, bounding box). Caches frequent queries in Redis.\n- **Booking Service** – Computes available time slots based on business hours, existing bookings, and service duration. Creates, updates, cancels bookings. Emits events for notification service.\n- **Payment Service** – Wrapper around a payment gateway (Stripe/PayPal). Creates payment intents, verifies webhooks, records transaction status.\n- **Notification Service** – Sends push notifications (via Expo push service) and emails. Consumes BullMQ jobs triggered by booking events.\n- **Provider Portal Service** – Authenticated endpoints for business owners to manage their services, view bookings, update availability.\n- **Admin Service** – Privileged endpoints for admins to manage users, businesses, system settings.\n- **Shared Layer** – Contains DTOs, validation pipes, auth guards, logging middleware, error handling, and Prisma client initialization.\n\n## Data Model (Prisma Schema Highlights)\n```\nmodel User {\n  id            Int      @id @default(autoincrement())\n  email         String   @unique\n  passwordHash  String\n  role          Role     @default(USER)\n  createdAt     DateTime @default(now())\n  updatedAt     DateTime @updatedAt\n  profiles      Profile? @relation(\"UserProfile\")\n  bookings      Booking[]\n  favorites     Favorite[]\n}\n\nmodel Business {\n  id            Int      @id @default(autoincrement())\n  name          String\n  address       String\n  phone         String\n  location      Point    @db.Point   // PostGIS\n  rating        Float    @default(0)\n  createdAt     DateTime @default(now())\n  updatedAt     DateTime @updatedAt\n  services      Service[]\n  openingHours  OpeningHour[]\n  reviews       Review[]\n}\n\nmodel Service {\n  id            Int      @id @default(autoincrement())\n  businessId    Int\n  business      Business @relation(fields:[businessId], references:[id])\n  name          String\n  durationMin   Int\n  price         Float\n  category      String\n  bookings      Booking[]\n}\n\nmodel Booking {\n  id            Int      @id @default(autoincrement())\n  userId        Int\n  serviceId     Int\n  startTime     DateTime\n  endTime       DateTime\n  status        BookingStatus\n  paymentId     String?\n  user          User     @relation(fields:[userId], references:[id])\n  service       Service  @relation(fields:[serviceId], references:[id])\n}\n\nmodel Favorite {\n  id        Int      @id @default(autoincrement())\n  userId    Int\n  businessId Int?\n  serviceId  Int?\n  user       User     @relation(fields:[userId], references:[id])\n  business   Business @relation(fields:[businessId], references:[id])\n  service    Service  @relation(fields:[serviceId], references:[id])\n}\n```\nAdditional models for `OpeningHour`, `Review`, `Notification`, `PaymentTransaction`, etc.\n\n## API Contracts\n- RESTful JSON endpoints under `/api/{resource}`.\n- Versioned via URL prefix `/api/v1`.\n- Use DTOs for request/response validation (class-validator + class-transformer).\n- Authenticated routes protected by JWT Guard; role‑based guards for provider/admin.\n- Webhook endpoints for payment gateway and Expo push receipts.\n\n## State Management & Caching\n- **Client**: TanStack React Query for server state (queries/mutations). Query keys organized by resource (e.g., `['businesses', lat, lng, radius]`). Automatic refetching on focus, background fetch intervals.\n- **Local UI State**: React Context or Reanimated shared values for animations.\n- **Cache Layer**: Redis caches business list results, geospatial queries, and slot computations (TTL 5‑15 min). Invalidation via Pub/Sub on write events.\n\n## DevOps & CI/CD\n- **Local Development**: `docker compose up` spins up api, db, redis, worker. Nx runs `nx serve mobile` and `nx serve api`.\n- **CI Pipeline** (GitHub Actions):\n  1. Install pnpm dependencies.\n  2. Run lint (ESLint) and type-check (tsc).\n  3. Run unit tests (Jest) for both mobile and api.\n  4. Build Docker images and push to registry (optional).\n  5. Run e2e tests against staging environment.\n  6. On main branch, trigger EAS Build for iOS/Android and publish to Expo store.\n- **CD**: Docker images deployed to Kubernetes or ECS; database migrations via Prisma Migrate; Redis persisted via Elasticache.\n- **Monitoring**: Health checks, logs shipped to a logging service; metrics via Prometheus/Grafana (optional).\n\n## Testing Strategy\n- **Unit Tests**: Jest for services, utilities, React hooks, and custom components.\n- **Integration Tests**: Supertest for NestJS endpoints; React Native Testing Library for screens with mocked React Query.\n- **E2E Tests**: Detox for mobile flows (login, search, booking). Cypress for admin/provider portal (if web).\n- **Test Coverage Goal**: >80% for backend, >70% for frontend.\n\n## Security Considerations\n- Passwords hashed with bcrypt (salt 12).\n- JWT access token (15 min) + refresh token (7 days) stored securely (SecureStore).\n- HTTPS enforced; API behind TLS.\n- Rate limiting via NestJS Throttler.\n- Input validation and sanitization (class-validator, Prisma parameterized queries).\n- CORS restricted to allowed origins.\n- Sensitive data (payment info) never stored; only gateway tokens.\n- Regular dependency audits (npm audit, pnpm audit).\n- Supabase Row Level Security if used for storage.\n\n## Scalability & Performance\n- Horizontal scaling of NestJS API behind load balancer.\n- Read replicas for PostgreSQL for heavy search queries.\n- Redis clustering for cache and job queue.\n- Geospatial indexes (GIST) on `location` column for fast radius searches.\n- Pagination and cursor‑based APIs for large lists.\n- Slot computation performed asynchronously; results cached.\n- Image assets served via CDN (Supabase storage or AWS S3) with responsive sizes.\n- Bundle optimization: Metro bundler with Hermes, tree‑shaking, code splitting via dynamic imports.\n\n## Decision Log\n- **Expo Chosen**: Simplifies OTA updates, device builds, and provides managed push notifications.\n- **NestJS + Prisma**: Provides strong typing, modular architecture, and easy DB migrations.\n- **PostGIS**: Enables location‑based search without external services.\n- **Redis**: Fast cache and reliable background job broker (BullMQ).\n- **Nx Monorepo**: Enables code sharing between mobile and API, consistent tooling.\n- **pnpm**: Efficient workspace management, faster installs.\n- **Docker Compose**: Reproducible local dev environment.\n- **GitHub Actions + EAS Build**: Automated CI/CD pipeline for mobile releases.\n- **Jest**: Unified testing framework across stack.\n\n## Conclusion\nThis architecture separates concerns into clearly bounded services, leverages a modular monorepo for shared code, and uses proven technologies to deliver a scalable, maintainable Planity Clone. The folder structure above can be scaffolded directly with Nx generators, and the outlined contracts and data models provide a concrete starting point for implementation.\n",
  "summary": "This document defines a modular, scalable architecture for the Planity Clone using Expo, React Native, NestJS, Prisma, PostgreSQL with PostGIS, Redis, and Nx monorepo practices."
}