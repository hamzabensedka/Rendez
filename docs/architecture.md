{
  "content": "# Planity Clone Architecture\n\n## 1. Overview\nPlanity Clone is a monorepo‑based full‑stack application that serves a mobile client (Expo/React Native) and several backend services (NestJS) sharing a PostgreSQL/PostGIS database, Redis cache, and BullMQ job queue. The architecture follows Clean Architecture principles: clear separation of concerns, domain‑driven boundaries, and dependency inversion. The monorepo is managed with Nx and pnpm, enabling code sharing (types, UI components, utilities) and consistent tooling.\n\n## 2. Tech Stack\n- **Mobile**: Expo SDK, React Native, TypeScript, Expo Router, TanStack React Query, React Native Reanimated\n- **Web/Admin**: (optional) Next.js or React web app – same shared libraries\n- **Backend**: NestJS (Node.js/TypeScript), Prisma ORM, PostgreSQL + PostGIS extension, Redis, BullMQ\n- **DevOps**: Docker Compose (local), GitHub Actions (CI), EAS Build (mobile), Supabase (optional hosted PG/Redis), Nx (monorepo tooling), Jest (unit/integration), ESLint/Prettier\n\n## 3. High‑Level Architecture\n```\n+-------------------+      +-------------------+      +-------------------+\n|   Mobile App      |<---->|   API Gateway     |<---->|   Business Service|\n| (Expo RN)         |      | (NestJS)          |      | (NestJS)          |\n+-------------------+      +-------------------+      +-------------------+\n        ^                         ^                         ^\n        |                         |                         |\n        |                         |                         |\n        v                         v                         v\n+-------------------+      +-------------------+      +-------------------+\n|   Auth Service    |      |   Booking Service |      |   Provider Portal |\n| (NestJS)          |      | (NestJS)          |      | (NestJS)          |\n+-------------------+      +-------------------+      +-------------------+\n        ^                         ^                         ^\n        |                         |                         |\n        |                         |                         |\n        v                         v                         v\n+-------------------+      +-------------------+      +-------------------+\n|   Admin Dashboard |<---->|   Notification Svc|<---->|   Background Jobs |\n| (NestJS)          |      | (NestJS)          |      | (BullMQ + Workers)|\n+-------------------+      +-------------------+      +-------------------+\n        ^                         ^                         ^\n        |                         |                         |\n        |                         |                         |\n        v                         v                         v\n+-------------------+\n|   Shared Libs     |\n| (types, ui, utils)|\n+-------------------+\n        ^\n        |\n        v\n+-------------------+\n|   Data Store      |\n| (PostgreSQL/PostGIS, Redis)\n+-------------------+\n```\n\n### 3.1 Service Boundaries\n| Service | Responsibility | Technologies |\n|---------|----------------|--------------|\n| **API Gateway** | Entry point, request routing, auth validation, rate limiting, request/response logging. | NestJS (Express adapter), middleware |\n| **Auth Service** | User registration, login, password reset, social OAuth, JWT issuance/validation. | NestJS, Prisma, bcrypt, redis for refresh‑token store |\n| **Business Service** | CRUD for businesses, categories, geospatial search (PostGIS), service listings. | NestJS, Prisma, PostGIS |\n| **Booking Service** | Appointment creation, slot computation, conflict detection, booking lifecycle, payment initiation. | NestJS, Prisma, BullMQ (for async payment confirmation), Redis (optimistic lock) |\n| **Provider Portal** | Business‑owner UI (mobile/web) to manage listings, availability, view bookings. | NestJS (same API), shared frontend libs |\n| **Admin Dashboard** | Super‑admin CRUD over users, businesses, categories, system settings. | NestJS, shared frontend libs |\n| **Notification Service** | Send push (Expo Push), email, SMS via third‑party providers; handles templates. | NestJS, BullMQ jobs, Expo Push SDK, SendGrid/Twilio |\n| **Background Jobs** | Worker processes for BullMQ queues: payment webhook handling, reminder emails, data cleanup, analytics aggregation. | BullMQ, NestJS workers |\n| **Shared Libs** | TypeScript interfaces, DTOs, validation schemas, UI component library (React Native + Web), utility functions, constants, API client (react‑query). | Nx library, pnpm workspace |\n\n## 4. Folder Structure (Nx Monorepo)\n```\nplanity-clone/\n├─ apps/\n│  ├─ mobile/               # Expo RN app\n│  │  ├─ src/\n│  │  │  ├─ assets/\n│  │  │  ├─ components/     # Reusable UI (Reanimated animations)\n│  │  │  ├─ screens/        # Expo Router file‑based routes\n│  │  │  ├─ hooks/\n│  │  │  ├─ services/       # API client (react‑query)\n│  │  │  ├─ store/          # Optional global state (React Query cache)\n│  │  │  ├─ types/          # Re‑export from @planity/shared\n│  │  │  └─ index.tsx\n│  │  ├─ app.json\n│  │  ├─ eas.json\n│  │  └─ tsconfig.json\n│  ├─ web/                  # Optional Next.js admin/web (shares libs)\n│  └─ api/                  # NestJS monolith (gateway + services) – can be split later\n│     ├─ src/\n│     │  ├─ gateway/\n│     │  ├─ auth/\n│     │  ├─ business/\n│     │  ├─ booking/\n│     │  ├─ provider/\n│     │  ├─ admin/\n│     │  ├─ notification/\n│     │  ├─ shared/\n│     │  │  ├─ dto/\n│     │  │  ├─ guards/\n│     │  │  ├─ interceptors/\n│     │  │  ├─ utils/\n│     │  │  └─ types/      # Re‑export from @planity/shared\n│     │  ├─ jobs/         # BullMQ queue definitions & processors\n│     │  ├─ prisma/\n│     │  │  ├─ schema.prisma\n│     │  │  └─ migrations/\n│     │  └─ main.ts\n│     ├─ test/\n│     └─ tsconfig.json\n├─ libs/\n│  ├─ shared/\n│  │  ├─ src/\n│  │  │  ├─ types/          # Domain DTOs, entities, enums\n│  │  │  ├─ ui/\n│  │  │  │  ├─ components/   # RN + Web compatible (via react-native-web)\n│  │  │  │  └─ styles/\n│  │  │  ├─ utils/\n│  │  │  ├─ constants/\n│  │  │  └─ api/\n│  │  │     └─ client.ts   # React‑Query wrapper\n│  │  ├─ tsconfig.json\n│  │  └─ package.json\n│  ├─ mobile-ui/           # RN‑specific UI extensions (Reanimated)\n│  └─ web-ui/              # Web‑specific UI extensions\n├─ prisma/\n│  └─ schema.prisma        # Single source of truth for DB\n├─ docker-compose.yml\n├─ nx.json\n├─ package.json\n├─ tsconfig.base.json\n└─ README.md\n```\n\n### 4.1 Key Notes\n- **Apps** contain platform‑specific entry points; they depend only on libs.\n- **Libs** are published implicitly via Nx; `shared` contains truly universal code (types, UI kit, API client).\n- **API** is a NestJS monolith for simplicity; each folder (`gateway`, `auth`, …) is a NestJS module with its own controllers, services, and DTOs. As the system grows, modules can be extracted to separate Nx apps.\n- **Prisma schema** lives at repo root; all services import the generated client via `shared` or directly.\n- **Docker Compose** defines services: `postgres`, `redis`, `api`, `worker` (BullMQ), plus optional `supabase` stub.\n\n## 5. Data Model (Prisma)\n```prisma\n// Simplified – actual schema includes indexes, timestamps, soft deletes\nmodel User {\n  id            String   @id @default(uuid())\n  email         String   @unique\n  passwordHash  String\n  name          String\n  role          Role     @default(USER)\n  createdAt     DateTime @default(now())\n  updatedAt     DateTime @updatedAt\n  bookings      Booking[]\n  favorites     Favorite[]\n  reviews       Review[]\n}\nmodel Business {\n  id            String   @id @default(uuid())\n  name          String\n  description   String?\n  address       String\n  lat           Float\n  lng           Float\n  location      Point    @db.Point @unique\n  categoryId    String\n  category      Category @relation(fields: [categoryId], references: [id])\n  services      Service[]\n  bookings      Booking[]\n  reviews       Review[]\n  ownerId       String\n  owner         User     @relation(fields: [ownerId], references: [id])\n  createdAt     DateTime @default(now())\n  updatedAt     DateTime @updatedAt\n}\nmodel Category { id String @id @default(uuid()) name String @unique businesses Business[] }\nmodel Service { id String @id @default(uuid()) name String durationInt Int businessId String business Business @relation(fields: [businessId], references: [id]) }\nmodel Booking {\n  id            String   @id @default(uuid())\n  userId        String\n  user          User     @relation(fields: [userId], references: [id])\n  businessId    String\n  business      Business @relation(fields: [businessId], references: [id])\n  serviceId     String\n  service       Service  @relation(fields: [serviceId], references: [id])\n  startTime     DateTime\n  endTime       DateTime\n  status        BookingStatus\n  paymentId     String?\n  createdAt     DateTime @default(now())\n  updatedAt     DateTime @updatedAt\n}\nmodel Favorite { id String @id @default(uuid()) userId String businessId String user User @relation(fields: [userId], references: [id]) business Business @relation(fields: [businessId], references: [id]) }\nmodel Review { id String @id @default(uuid()) userId String businessId String rating Int comment String? createdAt DateTime @default(now()) user User @relation(fields: [userId], references: [id]) business Business @relation(fields: [businessId], references: [id]) }\nenum Role { USER ADMIN BUSINESS_OWNER }\nenum BookingStatus { PENDING CONFIRMED COMPLETED CANCELLED }\n```\n- PostGIS extension enables `location` field for proximity queries.\n- Indexes on `lat/lng`, `categoryId`, `status`, and composite indexes for slot computation.\n\n## 6. API Contracts\nAll modules use **DTOs** (data transfer objects) defined in `libs/shared/src/types`. Controllers validate incoming data with `class-validator`/`class-transformer`. Responses are wrapped in a uniform: `{ success: boolean, data?: T, error?: string }`.\n\n### 6.1 Example Endpoints\n| Method | Path | Description |\n|--------|------|-------------|\n| POST | `/auth/register` | `{email, password, name}` → `{accessToken, refreshToken}` |\n| GET | `/businesses?lat=&lng=&radius=&category=&q=` | Paginated list with distance |\n| GET | `/businesses/:id` | Detail incl. services, reviews |\n| POST | `/bookings` | `{businessId, serviceId, startTime}` → booking + payment intent |\n| POST | `/payments/webhook` | Stripe/Paypal webhook → update booking status |\n| GET | `/users/me/bookings` | User’s upcoming/past bookings |\n| POST | `/notifications/send` | Trigger push/email (internal) |\n\n## 7. State Management & Data Fetching\n- **React Query** (TanStack) handles server state: caching, background refetch, optimistic updates.\n- Query keys are namespaced: `['businesses', lat, lng, radius]`, `['booking', id]`, etc.\n- Mutations use `useMutation` for booking, payment, review submission; they trigger query invalidation.\n- Reanimated is used for gesture‑driven UI (map drag, swipe‑to‑cancel) but does not manage app state.\n\n## 8. Caching & Pub/Sub\n- **Redis**: stores refresh tokens, rate‑limit counters, temporary OTPs, and serves as the BullMQ broker.\n- **In‑memory** (Node process) caching is avoided; all shared state goes through Redis or Postgres.\n- For real‑time updates (e.g., new booking for a provider), we rely on **Expo Push** (notification) and **polling** via React Query (configurable refetchInterval). If WebSocket becomes needed, we can add a NestJS Gateway module later.\n\n## 9. Background Jobs (BullMQ)\nQueues defined in `libs/api/src/jobs`:\n- `payment-confirmation`: listens to payment gateway webhook, updates booking status, sends confirmation.\n- `reminder`: runs hourly, finds bookings within next 30 min, sends push/email reminder.\n- `analytics-aggregation`: nightly roll‑up of booking counts per business/category.\n- `cleanup`: removes expired OTPs, old sessions.\nEach queue has a corresponding NestJS processor with retry logic (3 attempts, exponential backoff). Failed jobs move to a `failed` set for inspection.\n\n## 10. Authentication & Authorization\n- **Auth Service** issues JWT (access token 15 min, refresh token 7 days) signed with RS256; public key exposed via JWKS endpoint.\n- Gateway middleware verifies JWT, attaches `req.user` (id, role).\n- Role‑based guards: `AuthGuard`, `RolesGuard` (USER, BUSINESS_OWNER, ADMIN).\n- Refresh token stored hashed in Redis; rotation on use to mitigate theft.\n\n## 11. Payment Integration\n- Abstracted via `PaymentService` interface; current implementation uses Stripe Checkout Sessions.\n- Booking creation returns a `clientSecret`; frontend confirms payment via Stripe SDK.\n- Webhook endpoint (`/payments/webhook`) verifies signature, updates booking to `CONFIRMED`, triggers `payment-confirmation` job (email, push).\n- Idempotency ensured via `idempotency-key` header and storing processed event IDs in Redis.\n\n## 12. Notifications\n- **Expo Push Notifications** for mobile; **SendGrid** for email; **Twilio** for SMS (optional).\n- Notification Service receives jobs from BullMQ (`notification` queue) and dispatches via appropriate provider.\n- Templates stored as Handlebars strings in DB or static files.\n\n## 13. Provider / Business Owner Portal\n- Shares the same API as the consumer app; UI screens differ (dashboard, calendar, booking list).\n- Availability management: owners define recurring opening hours and special exceptions; stored as `Availability` entity (not shown) linked to Business.\n- Slot computation service (in Booking) reads availability, existing bookings, and returns open windows.\n\n## 14. Admin Dashboard\n- CRUD over Users, Businesses, Categories, System Settings.\n- Role promotion/demotion, booking oversight, fraud monitoring.\n- Built with same shared UI kit; routes under `/admin/*`.\n\n## 15. DevOps & CI/CD\n### 15.1 Local Development\n```bash
docker compose up -d   # postgres, redis
nx run mobile:start    # Expo dev client
nx run api:start:dev   # NestJS watch mode
```
### 15.2 Testing\n- **Unit**: Jest with ts-jest; 80%+ target.\n- **Integration**: SuperTest for API endpoints; uses test Postgres schema.\n- **E2E** (mobile): Detox (optional) or Expo’s built‑in testing.\n- CI runs `nx affected --target=test --base=main~1` on each PR.\n### 15.3 Build\n- Mobile: `eas build --platform all` (managed workflow).\n- API: Dockerfile multi‑stage (Node → dist).\n### 15.4 Deployment\n- **Staging/Prod**: Docker Compose on VM or Kubernetes (helm chart).\n- Env vars managed via Doppler or GitHub Secrets.\n- Database migrations run automatically on startup via Prisma Migrate.\n- Redis persistence via AOF/RDB.\n- Monitoring: Prometheus + Grafana (metrics exposed via NestJS `@nestjs/prometheus`).\n- Logging: Winston + Loki; structured JSON logs.\n\n## 16. Security Considerations\n- HTTPS everywhere (Expo uses TLS; API behind TLS termination).\n- Helmet, CORS, rate limiting (express‑rate‑limit) in Gateway.\n- Input validation & sanitization via class‑validator & Prisma parameterized queries.\n- JWT short‑lived access token; refresh token rotation + replay detection.\n- Password hashing with bcrypt (salt 12).\n- OTP & rate‑limit for auth endpoints.\n- File uploads (if added) stored in S3 with signed URLs; virus scanning via ClamAV.\n- Dependency scanning: `npm audit` and `Dependabot`.\n- CodeQL analysis in GitHub Actions.\n\n## 17. Scalability & Performance\n- **Read scaling**: Postgres read replicas for business listings; queries directed via service layer.\n- **Geo search**: PostGIS `ST_DistanceSphere` with index on `location`.\n- **Cache**: Frequently accessed business details cached in Redis (TTL 5 min).\n- **Horizontal**: API stateless; can run multiple instances behind a load balancer.\n- **Jobs**: BullMQ workers scale horizontally; each worker processes a subset of jobs.\n- **Mobile**: Expo OTA updates for JS bundle; native updates via EAS.\n\n## 18. Summary\nThe architecture separates concerns into focused NestJS services, shares universal types and UI via an Nx monorepo, leverages PostGIS for location‑based search, uses Redis for caching and job queuing, and employs React Query/TanStack for robust client state. CI/CD, Docker, and automated testing ensure reliable delivery, while clean boundaries facilitate future evolution into micro‑services if needed.\n",
  "summary": "A clean‑architecture monorepo using Expo/React Native, NestJS, Prisma/PostGIS, Redis, BullMQ, and Nx to modularly serve mobile, provider, admin, and background workloads with scalable data storage, caching, and secure APIs."
}