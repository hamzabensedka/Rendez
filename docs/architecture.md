```json
{
  "content": "# Planity Clone — System Architecture\n\n## 1. Overview\n\nThis document defines the complete system architecture for the Planity Clone marketplace platform. The architecture prioritizes type safety, developer experience, and operational maintainability through a monorepo structure with clear service boundaries.\n\n## 2. Architectural Principles\n\n| Principle | Application |\n|-----------|-------------|\n| **Separation of Concerns** | API, mobile, shared packages, and infrastructure are independently deployable |\n| **Type Safety End-to-End** | Single source of truth for schemas, API contracts, and database models |\n| **Infrastructure as Code** | Docker Compose for local; Terraform for production (future) |\n| **API-First Design** | OpenAPI specs drive client generation and validation |\n| **Event-Driven Where Appropriate** | Async processing for notifications, search indexing, analytics |\n\n## 3. Tech Stack Summary\n\n| Layer | Technology | Purpose |\n|-------|-----------|---------|\n| Mobile Client | Expo SDK 50+, React Native, TypeScript | Cross-platform iOS/Android app |\n| Navigation | Expo Router v3 | File-based routing with deep links |\n| State & Data | TanStack React Query v5 | Server state, caching, synchronization |\n| Animations | React Native Reanimated v3 | 60fps UI interactions, bottom sheets |\n| Backend API | NestJS v10, TypeScript | HTTP/GraphQL API, business logic |\n| ORM | Prisma v5 | Database access, migrations, type generation |\n| Database | PostgreSQL 15+, PostGIS 3.4 | Relational data, geospatial queries |\n| Cache | Redis 7 | Session store, rate limiting, slot holds, query cache |\n| Monorepo | Nx v18 | Build system, task graph, caching |\n| Package Manager | pnpm v8 | Workspace management, disk deduplication |\n| CI/CD | GitHub Actions | Lint, test, build, deploy pipelines |\n| Auth/Storage | Supabase | OAuth providers, file storage (avatars, business images) |\n| Build & Deploy | EAS Build | OTA updates, native builds, app store submissions |\n| Testing | Jest | Unit, integration, and snapshot tests |\n\n## 4. Monorepo Structure\n\n```\nplanity-clone/\n├── .github/\n│   └── workflows/\n│       ├── ci.yml                    # Lint, test, type-check on PR\n│       ├── api-deploy.yml            # Build and deploy API to staging/prod\n│       └── mobile-build.yml          # EAS Build triggers\n├── apps/\n│   ├── mobile/                       # Expo React Native app\n│   │   ├── app/                      # Expo Router file-based routes\n│   │   │   ├── (auth)/\n│   │   │   │   ├── login.tsx\n│   │   │   │   ├── register.tsx\n │   │   │   │   └── forgot-password.tsx\n│   │   │   ├── (tabs)/\n│   │   │   │   ├── index.tsx         # Home/Discover\n│   │   │   │   ├── search/\n│   │   │   │   │   ├── index.tsx\n│   │   │   │   │   └── [query].tsx\n│   │   │   │   ├── bookings.tsx\n│   │   │   │   └── profile.tsx\n│   │   │   ├── b/\n│   │   │   │   └── [slug]-[businessId].tsx\n│   │   │   ├── booking/\n│   │   │   │   └── [businessId] /\n│   │   │   │       ├── service.tsx\n│   │   │   │       ├── staff.tsx\n│   │   │   │       ├── datetime.tsx\n│   │   │   │       ├── details.tsx\n│   │   │   │       ├── payment.tsx\n│   │   │   │       └── confirm.tsx\n│   │   │   └── _layout.tsx\n│   │   ├── src/\n│   │   │   ├── components/\n│   │   │   │   ├── business/\n│   │   │   │   ├── search/\n│   │   │   │   ├── booking/\n│   │   │   │   ├── map/\n│   │   │   │   └── ui/               # Reusable primitives\n│   │   │   ├── hooks/\n│   │   │   ├── providers/\n│   │   │   ├── lib/\n│   │   │   │   ├── api.ts            # API client setup\n│   │   │   │   ├── query-client.ts\n│   │   │   │   └── storage.ts\n│   │   │   └── types/\n│   │   ├── package.json\n│   │   └── app.json\n│   └── api/                          # NestJS application\n│       ├── src/\n│       │   ├── main.ts\n│       │   ├── app.module.ts\n│       │   ├── modules/\n│       │   │   ├── auth/\n│       │   │   ├── users/\n│       │   │   ├── businesses/\n│       │   │   ├── services/\n│       │   │   ├── bookings/\n│       │   │   ├── search/\n│       │   │   ├── notifications/\n│       │   │   ├── payments/\n│       │   │   └── admin/\n│       │   ├── common/\n│       │   │   ├── decorators/\n│       │   │   ├── filters/\n│       │   │   ├── guards/\n│       │   │   ├── interceptors/\n│       │   │   └── pipes/\n│       │   └── config/\n│       ├── test/\n│       └── Dockerfile\n├── packages/\n│   ├── types/                        # Shared TypeScript definitions\n│   │   ├── src/\n│   │   │   ├── api/                  # API request/response DTOs\n│   │   │   ├── domain/               # Business entity types\n│   │   │   └── index.ts\n│   │   └── package.json\n│   ├── ui/                           # Shared React Native components (future)\n│   ├── config/                       # Shared ESLint, TS, Tailwind configs\n│   └── ts-config/                    # Base TypeScript configurations\n├── prisma/\n│   ├── schema/\n│   │   ├── base.prisma               # Enums, common models\n│   │   ├── user.prisma\n│   │   ├── business.prisma\n│   │   ├── service.prisma\n│   │   ├── booking.prisma\n│   │   └── location.prisma           # PostGIS extensions\n│   ├── migrations/\n│   └── seed.ts\n├── docker/\n│   ├── docker-compose.yml\n│   ├── docker-compose.prod.yml\n│   └── init-scripts/\n├── docs/\n│   ├── product.md\n│   └── architecture.md\n├── nx.json\n├── pnpm-workspace.yaml\n├── package.json\n└── tsconfig.base.json\n```\n\n## 5. Service Boundaries & Module Design\n\n### 5.1 API Layer (NestJS)\n\n| Module | Responsibility | Key Dependencies |\n|--------|---------------|----------------|\n| `AuthModule` | JWT issuance, refresh rotation, OAuth integration, password reset | Redis (sessions), Prisma, Supabase Auth |\n| `UsersModule` | Profile management, role transitions, preferences | Prisma |\n| `BusinessesModule` | CRUD, verification, hours, amenities, media | Prisma, Supabase Storage, SearchModule |\n| `ServicesModule` | Category taxonomy, service definitions, pricing | Prisma |\n| `BookingsModule` | Slot availability, holds, reservations, lifecycle | Redis (locks), Prisma, NotificationsModule |\n| `SearchModule` | Full-text search, geospatial queries, aggregations | PostgreSQL (PostGIS), Redis (cache) |\n| `NotificationsModule` | Push, SMS, email orchestration | Redis (queues), external providers |\n| `PaymentsModule` | Payment intent, capture, refunds | Stripe/similar, Prisma |\n| `AdminModule` | Moderation, analytics, platform config | Prisma |\n\n### 5.2 Module Communication Pattern\n\n```\n┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐\n│   Controllers   │────▶│    Services     │────▶│  Repositories   │\n│   (HTTP/WS)     │     │  (Business Logic)│     │   (Prisma/Redis) │\n└─────────────────┘     └─────────────────┘     └─────────────────┘\n                               │\n                               ▼\n                        ┌─────────────────┐\n                        │  Event Bus      │\n                        │  (Redis Pub/Sub)│\n                        └─────────────────┘\n```\n\n**Rule:** Cross-module communication via injected services (synchronous) or events (asynchronous). Direct repository access from other modules is prohibited.\n\n## 6. Database Architecture\n\n### 6.1 PostgreSQL + PostGIS Schema Strategy\n\nPrisma schema is split across domain files for maintainability, merged at generation time.\n\n```prisma\n// base.prisma — core enums and configuration\ngenerator client {\n  provider = \"prisma-client-js\"\n}\n\ndatasource db {\n  provider = \"postgresql\"\n  url      = env(\"DATABASE_URL\")\n  extensions = [postgis]\n}\n\nenum UserRole {\n  CONSUMER\n  PROVIDER\n  ADMIN\n}\n\nenum BookingStatus {\n  PENDING\n  CONFIRMED\n  COMPLETED\n  CANCELLED\n  NO_SHOW\n}\n```\n\n```prisma\n// location.prisma — PostGIS integration\nmodel Location {\n  id        String   @id @default(cuid())\n  latitude  Float\n  longitude Float\n  // PostGIS column managed via raw migration\n  geom      Unsupported(\"geometry(Point, 4326)\")?\n  \n  business  Business?\n  \n  @@index([geom], name: \"idx_location_geom\", type: Gist)\n}\n```\n\n### 6.2 Key Data Patterns\n\n| Pattern | Implementation |\n|---------|---------------|\n| **Soft Deletes** | `deletedAt DateTime?` on all entities; Prisma middleware filters |\n| **Timestamps** | `createdAt`, `updatedAt` on all entities |\n| **Audit Logging** | Separate `AuditLog` table; async writes via events |\n| **JSON Flexibility** | `metadata Json?` for extensible business/service attributes |\n| **Full-Text Search** | PostgreSQL `tsvector` with GIN index; synchronized via trigger |\n\n### 6.3 Redis Data Structures\n\n| Purpose | Key Pattern | Data Structure | TTL |\n|---------|-----------|---------------|-----|\n| Refresh Tokens | `refresh:{userId}:{tokenId}` | String | 7 days |\n| Rate Limit Buckets | `ratelimit:{ip}:{action}` | String (counter) | 15 min |\n| Slot Holds | `hold:{businessId}:{slotTime}` | Hash (userId, expiresAt) | 10 min |\n| Session Cache | `session:{sessionId}` | Hash | 15 min |\n| Query Cache | `cache:{hash}` | String (JSON) | 5 min |\n| Geohash Buckets | `geo:{geohash}` | Sorted Set (lat,lng,businessId) | 1 hour |\n\n## 7. API Design\n\n### 7.1 RESTful Endpoints with Versioning\n\nBase path: `/api/v1`\n\n| Resource | Endpoints | Notes |\n|----------|-----------|-------|\n| Auth | `POST /auth/register`, `POST /auth/login`, `POST /auth/refresh`, `POST /auth/logout`, `POST /auth/forgot-password`, `POST /auth/reset-password` | OAuth via `POST /auth/oauth/{provider}` |\n| Users | `GET /users/me`, `PATCH /users/me`, `DELETE /users/me` | Role upgrade via `POST /users/become-provider` |\n| Businesses | `GET /businesses`, `GET /businesses/:id`, `GET /businesses/:id/availability`, `POST /businesses` (provider), `PATCH /businesses/:id` (owner) | Search via `GET /businesses?lat=&lng=&radius=&q=` |\n| Services | `GET /businesses/:id/services`, `POST /services` (provider) | Nested under business for discovery |\n| Bookings | `POST /bookings`, `GET /bookings`, `GET /bookings/:id`, `PATCH /bookings/:id/reschedule`, `DELETE /bookings/:id` | Atomic slot reservation |\n| Search | `GET /search/suggest?q=`, `GET /search/categories`, `GET /search/trending` | Autocomplete endpoint |\n\n### 7.2 Slot Availability Algorithm\n\n```\nINPUT: businessId, dateRange, staffId? (optional)\nOUTPUT: available slots\n\n1. FETCH business operating hours for dateRange\n2. FETCH existing bookings for dateRange, staffId\n3. FETCH blocked times (staff breaks, business closures)\n4. GENERATE candidate slots from operating hours + service duration\n5. FILTER: remove slots where booking exists\n6. FILTER: remove slots where blocked time overlaps\n7. FILTER: remove slots in the past\n8. RETURN remaining slots with 10-min hold status\n```\n\nSlot hold implementation: Redis `HSET` with `NX` (not exists) flag. Background job releases expired holds.\n\n## 8. Mobile Architecture\n\n### 8.1 Navigation Structure (Expo Router)\n\n```\napp/\n├── (auth)/           # Auth stack — no bottom tab\n│   ├── _layout.tsx   # Stack navigator\n│   ├── login.tsx\n│   ├── register.tsx\n│   └── forgot-password.tsx\n├── (tabs)/           # Main app with bottom tabs\n│   ├── _layout.tsx   # Tab navigator\n│   ├── index.tsx     # Home/Discover (redirects to search)\n│   ├── search/       # Search tab with nested stack\n│   │   ├── _layout.tsx\n│   │   ├── index.tsx\n│   │   └── [query].tsx\n│   ├── bookings.tsx  # Appointments list\n│   └── profile.tsx   # User profile\n├── b/                # Business detail (modal stack)\n│   └── [slug]-[businessId].tsx\n├── booking/          # Booking flow (linear wizard)\n│   └── [businessId]/\n│       ├── _layout.tsx   # Progress header\n│       ├── service.tsx\n│       ├── staff.tsx\n│       ├── datetime.tsx\n│       ├── details.tsx\n│       ├── payment.tsx\n│       └── confirm.tsx\n└── _layout.tsx       # Root with providers, auth gate\n```\n\n### 8.2 State Management Strategy\n\n| State Type | Solution | Rationale |\n|-----------|----------|-----------|\n| Server State | TanStack Query | Caching, stale-while-revalidate, background refetch, optimistic updates |\n| Local UI State | React `useState`/`useReducer` | Simple, no over-engineering |\n| Cross-Component UI | React Context (lightweight) | Theme, auth status, bottom sheet refs |\n| Persistent Local | AsyncStorage + Zustand | Guest data, search history, preferences |\n| Form State | React Hook Form | Performance, validation, reduced re-renders |\n\n### 8.3 Query Organization (React Query)\n\n```typescript\n// hooks/queries/businesses.ts\nexport const businessKeys = {\n  all: ['businesses'] as const,\n  lists: () => [...businessKeys.all, 'list'] as const,\n  list: (filters: BusinessFilters) => [...businessKeys.lists(), filters] as const,\n  details: () => [...businessKeys.all, 'detail'] as const,\n  detail: (id: string) => [...businessKeys.details(), id] as const,\n  availability: (id: string, date: string) => \n    [...businessKeys.detail(id), 'availability', date] as const,\n};\n\nexport function useBusinesses(filters: BusinessFilters) {\n  return useQuery({\n    queryKey: businessKeys.list(filters),\n    queryFn: () => api.search.businesses(filters),\n    staleTime: 5 * 60 * 1000, // 5 min\n  });\n}\n\nexport function useBusinessDetail(id: string) {\n  return useQuery({\n    queryKey: businessKeys.detail(id),\n    queryFn: () => api.businesses.getById(id),\n    staleTime: 10 * 60 * 1000,\n  });\n}\n```\n\n### 8.4 Animation Strategy (Reanimated)\n\n| Interaction | Technique |\n|-------------|-----------|\n| Bottom Sheet (map preview, filters) | `react-native-reanimated` + `react-native-gesture-handler` |\n| List scroll | Shared element transitions, `useAnimatedScrollHandler` |\n| Booking progress | `withTiming` on width/translateX |\n| Map marker press | `withSpring` scale + opacity |\n| Skeleton loading | `useAnimatedStyle` with pulse opacity |\n\n## 9. Authentication & Security\n\n### 9.1 JWT Flow\n\n```\nClient                          API                            Redis\n  │    POST /auth/login         │\n  │ ──────────────────────────▶ │\n  │                             │  1. Validate credentials\n  │                             │  2. Generate accessToken (15min)\n  │                             │  3. Generate refreshToken + tokenId\n  │                             │  4. Store: SET refresh:{userId}:{tokenId} {hash} EX 7d\n  │  ◀───────────────────────── │\n  │  { accessToken, refreshToken }\n  │\n  │  [subsequent requests]\n  │  Authorization: Bearer {accessToken}\n  │\n  │  [token expires]\n  │    POST /auth/refresh\n  │    { refreshToken }\n  │ ──────────────────────────▶\n  │                             │  1. Verify refreshToken signature\n  │                             │  2. Check Redis: EXISTS refresh:{userId}:{tokenId}\n  │                             │  3. Rotate: delete old, create new\n  │                             │  4. Return new pair\n```\n\n### 9.2 Guest Flow Architecture\n\n```\nGuest Session\n├── Local state: viewed businesses, search history\n├── Booking attempt: capture email, phone, name\n├── POST /bookings/guest creates temporary account\n├── Background: POST /auth/register with auto-generated password\n├── Email sent: \"Complete your account\" with magic link\n├── Account merge: guest data → permanent account\n```\n\n### 9.3 Rate Limiting\n\n| Endpoint | Limit | Window |\n|----------|-------|--------|\n| `POST /auth/login` | 5 attempts | 15 minutes |\n| `POST /auth/register` | 3 attempts | 15 minutes |\n| `POST /auth/forgot-password` | 3 attempts | 1 hour |\n| General API | 100 requests | 1 minute |\n\nImplementation: Redis sliding window counter with `INCR` + `EXPIRE`.\n\n## 10. Search & Discovery Architecture\n\n### 10.1 Search Pipeline\n\n```\nUser Query → Normalize → Tokenize → PostgreSQL Full-Text\n                              ↓\n                     [Optional: Redis Cache Hit]\n                              ↓\n                    PostgreSQL Query Builder\n                    - tsquery against name, description\n                    - PostGIS distance filter\n                    - Availability subquery\n                    - Aggregated rating, price\n                              ↓\n                    Cursor Pagination (keyset)\n                              ↓\n                    Response with nextCursor\n```\n\n### 10.2 Geospatial Strategy\n\n| Zoom Level | Query Strategy |\n|-----------|----------------|\n| > 12 (street) | Exact point markers, no clustering |\n| 9-12 | Grid-based clustering with `ST_SnapToGrid` |\n| < 9 | City/region aggregation, heatmap or count markers |\n\nMap tile API: `GET /search/map-tiles?z={zoom}&x={x}&y={y}&filters=` returns MVT (Mapbox Vector Tile) for native rendering.\n\n## 11. Booking System\n\n### 11.1 Slot Hold Distributed Lock\n\n```typescript\n// Pseudocode for atomic hold\nasync function holdSlot(businessId: string, slotTime: Date, userId: string): Result {\n  const key = `hold:${businessId}:${slotTime.toISOString()}`;\n  const holdData = { userId, expiresAt: Date.now() + 10 * 60 * 1000 };\n  \n  // Redis HSETNX — only sets if key doesn't exist\n  const acquired = await redis.hsetnx(key, 'data', JSON.stringify(holdData));\n  \n  if (!acquired) {\n    return { success: false, reason: 'SLOT_HELD' };\n  }\n  \n  await redis.expire(key, 10 * 60); // 10 min TTL\n  return { success: true, holdId: key };\n}\n```\n\n### 11.2 Double-Booking Prevention\n\nDatabase-level: `UNIQUE` constraint on `(businessId, staffId, startTime)` with `WHERE status IN ('CONFIRMED', 'PENDING')`.\n\nApplication-level: Pessimistic lock via `SELECT FOR UPDATE` during payment confirmation.\n\n## 12. DevOps & Infrastructure\n\n### 12.1 Docker Compose (Local Development)\n\n```yaml\n# docker/docker-compose.yml\nversion: '3.8'\nservices:\n  postgres:\n    image: postgis/postgis:15-3.4\n    environment:\n      POSTGRES_DB: planity\n      POSTGRES_USER: dev\n      POSTGRES_PASSWORD: dev\n    volumes:\n      - postgres_data:/var/lib/postgresql/data\n    ports:\n      - \"5432:5432\"\n\n  redis:\n    image: redis:7-alpine\n    ports:\n      - \"6379:6379\"\n\n  api:\n    build:\n      context: ../\n      dockerfile: apps/api/Dockerfile\n    depends_on:\n      - postgres\n      - redis\n    environment:\n      - DATABASE_URL=postgresql://dev:dev@postgres:5432/planity\n      - REDIS_URL=redis://redis:6379\n    ports:\n      - \"3000:3000\"\n    volumes:\n      - ../:/app\n      - /app/node_modules\n```\n\n### 12.2 GitHub Actions Pipeline\n\n```yaml\n# .github/workflows/ci.yml\nname: CI\non: [pull_request]\njobs:\n  lint-test-build:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - uses: pnpm/action-setup@v2\n      - uses: actions/setup-node@v4\n        with:\n          node-version: '20'\n          cache: 'pnpm'\n      - run: pnpm install --frozen-lockfile\n      - run: pnpm nx affected -t lint test build --parallel=3\n```\n\n### 12.3 EAS Build Configuration\n\n```json\n// apps/mobile/eas.json\n{\n  \"cli\": {\n    \"version\": \">= 7.0.0\"\n  },\n  \"build\": {\n    \"development\": {\n      \"developmentClient\": true,\n      \"distribution\": \"internal\"\n    },\n    \"preview\": {\n      \"distribution\": \"internal\",\n      \"android\": {\n        \"buildType\": \"apk\"\n      }\n    },\n    \"production\": {\n      \"autoIncrement\": true\n    }\n  },\n  \"submit\": {\n    \"production\": {}\n  }\n}\n```\n\n## 13. Testing Strategy\n\n| Layer | Tool | Coverage Target |\n|-------|------|---------------|\n| API Unit | Jest + @nestjs/testing | 80% |\n| API Integration | Jest + testcontainers (PostgreSQL, Redis) | 70% |\n| Mobile Unit | Jest + @testing-library/react-native | 70% |\n| Mobile E2E | Maestro (future) | Critical flows |\n| API Contract | Schemathesis / Dredd | All endpoints |\n\n## 14. Performance Targets\n\n| Metric | Target | Measurement |\n|--------|--------|-------------|\n| App cold start | < 2s | Sentry / EAS profiling |\n| Time to First Content | < 1.5s | React Native Performance API |\n| API p99 latency | < 200ms | NestJS interceptors + APM |\n| Search result display | < 500ms (cached), < 2s (cold) | React Query metrics |\n| Image loading | Progressive, blur placeholder | Expo Image |\n| Bundle size | < 15MB (iOS), < 12MB (Android) | EAS build artifacts |\n\n## 15. Future Considerations\n\n| Concern | Current | Future |\n|---------|---------|--------|\n| Search | PostgreSQL full-text + PostGIS | Meilisearch/Elasticsearch for scale |\n| Real-time | Polling (React Query refetch) | WebSocket for booking updates |\n| Payments | Stripe integration | Multi-provider support |\n| Analytics | Application logs | Segment/Amplitude integration |\n| Push Notifications | Expo push | OneSignal for segmentation |\n\n## 16. Decision Log\n\n| Date | Decision | Rationale |\n|------|----------|-----------|\n| Initial | Expo over bare React Native | Faster iteration, OTA updates, unified build |\n| Initial | Expo Router over React Navigation | File-based routing, deep link parity, Expo ecosystem |\n| Initial | Nx monorepo vs. separate repos | Shared types, atomic changes, single CI pipeline |\n| Initial | PostgreSQL over NoSQL | Relational data with geospatial; ACID for bookings |\n| Initial | Prisma over TypeORM/Sequelize | Type safety, migration system, active development |\n| Initial | Redis over in-memory cache | Distributed slot holds, shared across API instances |\n\n---\n\n*Document Version: 1.0*\n*Last Updated: Architecture design phase*",
  "summary": "Complete system architecture for Planity Clone using a TypeScript monorepo with Expo mobile app, NestJS API, PostgreSQL/PostGIS database, and Redis caching, structured for scalability and maintainability."
}
```