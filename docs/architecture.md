{
  "content": "# Architecture Document\n\n## Overview\nThis document describes the complete system architecture for the Planity Clone, a mobile app for discovering, booking, and managing appointments with local businesses. The solution uses a monorepo managed by Nx, with pnpm as the package manager, and follows clean architecture principles: clear separation of concerns, domain‑driven service boundaries, and technology‑agnostic core logic.\n\n## Tech Stack\n- **Mobile**: Expo (SDK 49), React Native, TypeScript, Expo Router, TanStack React Query, React Native Reanimated\n- **Backend**: NestJS (Node.js), Prisma ORM, PostgreSQL + PostGIS extension, Redis, BullMQ (background jobs)\n- **DevOps**: Docker Compose (local), GitHub Actions (CI/CD), EAS Build (Expo), Supabase (optional auth/storage fallback), Jest (testing)\n- **Monorepo**: Nx, pnpm workspace\n\n## High‑Level Architecture\n```\n+-------------------+      +-------------------+      +-------------------+\n|   Mobile App      |<---->|   API Gateway     |<---->|   Worker Services |\n| (Expo RN)         |      | (NestJS REST/GraphQL) |      | (NestJS + BullMQ) |\n+-------------------+      +-------------------+      +-------------------+\n        ^                         ^                         ^\n        |                         |                         |\n        |                         |                         |\n+-------------------+      +-------------------+      +-------------------+\n|   Auth Service    |      |   Business Service|      |   Notification   |\n| (NestJS)          |      | (NestJS)          |      |   Service        |\n+-------------------+      +-------------------+      +-------------------+\n        \\_____________________|_____________________/\n                               |\n                               v\n                        +-------------------+\n                        |   Data Store      |\n                        | (PostgreSQL + PostGIS, Redis) |\n                        +-------------------+\n```\n\n### Service Boundaries\n1. **Mobile Client (apps/mobile)**\n   - UI layer built with Expo Router (file‑based routing).\n   - State management via TanStack React Query (server‑state) and React Native Reanimated for gestures/animations.\n   - Shared Types & Design System live in `libs/ui` and `libs/types`.\n   - Authentication handled via JWT stored in SecureStore; refresh token flow.\n   - Guest browse mode: limited data fetched without auth; auth gate for mutating actions.\n\n2. **API Gateway (apps/api)**\n   - NestJS monolith exposing REST endpoints (could be swapped for GraphQL).\n   - Modules correspond to bounded contexts: Auth, Business, Booking, User, Review, Payment, Notification, Admin, ProviderPortal.\n   - Each module contains:\n     - Controllers (HTTP layer)\n     - Services (application logic)\n     - Repositories (Prisma wrappers)\n     - DTOs (validation with class-validator)\n     - Guards (JWT, roles)\n   - Uses Prisma Client for DB access; PostGIS for geo‑queries.\n   - Redis used for rate‑limiting, caching of frequent queries (e.g., business list), and as BullMQ broker.\n\n3. **Worker Services (apps/worker)**\n   - Separate NestJS application that shares the same modules but only exposes BullMQ processors.\n   - Responsibilities:\n     - Sending push notifications (via Expo Push Notifications service).\n     - Computing availability slots (cron‑like jobs).\n     - Processing payments (webhook handling from Stripe).\n     - Sending email/SMS via third‑party providers.\n   - Communicates with the API via internal HTTP calls or directly via Prisma (shared DB).\n\n4. **Data Store**\n   - **PostgreSQL** with **PostGIS** for geographic queries (nearby businesses, map bounds).\n   - **Redis** for caching, session store (optional), and BullMQ job queue.\n\n5. **External Services**\n   - **Stripe** for payment processing.\n   - **Expo Push Notifications** for mobile push.\n   - **Supabase** (optional) as fallback auth/storage if needed.\n   - **Email/SMS** providers (e.g., SendGrid, Twilio).\n\n## Folder Structure (Nx Monorepo)\n```\nplanity-clone/\n├─ apps/\n│  ├─ mobile/                 # Expo React Native app\n│  │   ├─ src/\n│  │   │   ├─ app/            # Expo Router routes (file‑based)\n│  │   │   │   ├─ (tabs)/     # Tab navigator\n│  │   │   │   │   ├─ home.tsx\n│  │   │   │   │   ├─ search.tsx\n│  │   │   │   │   ├─ map.tsx\n│  │   │   │   │   ├─ favorites.tsx\n│  │   │   │   │   └─ profile.tsx\n│  │   │   │   ├─ auth/\n│  │   │   │   │   ├─ login.tsx\n│  │   │   │   │   ├─ register.tsx\n│  │   │   │   │   └─ reset-password.tsx\n│  │   │   │   ├─ business/\n│  │   │   │   │   ├─ [businessId]/\n│  │   │   │   │   │   ├─ detail.tsx\n│  │   │   │   │   │   ├─ reviews.tsx\n│  │   │   │   │   │   └─ book.tsx\n│  │   │   │   │   └─ list.tsx\n│  │   │   │   ├─ booking/\n│  │   │   │   │   ├─ upcoming.tsx\n│  │   │   │   │   ├─ history.tsx\n│  │   │   │   │   └─ manage/[bookingId].tsx\n│  │   │   │   ├─ provider/\n│  │   │   │   │   └─ dashboard.tsx\n│  │   │   │   └─ admin/\n│  │   │   │       └─ dashboard.tsx\n│  │   │   ├─ components/      # Shared UI (Reanimated gestures, buttons, etc.)\n│  │   │   ├─ hooks/           # Custom React Query hooks, auth hooks\n│  │   │   ├─ lib/             # Expo config, SecureStore wrapper, analytics\n│  │   │   ├─ types/           # Re‑exported from @planity/types\n│  │   │   └─ theme/           # Design system tokens (colors, spacing)\n│  │   ├─ assets/\n│  │   ├─ expo.config.js\n│  │   └─ package.json\n│  ├─ api/                    # NestJS REST API\n│  │   ├─ src/\n│  │   │   ├─ app.module.ts\n│  │   │   ├─ main.ts\n│  │   │   ├─ auth/\n│  │   │   │   ├─ auth.controller.ts\n│  │   │   │   ├─ auth.service.ts\n│  │   │   │   ├─ dto/\n│  │   │   │   ├─ guards/\n│  │   │   │   └─ strategies/\n│  │   │   ├─ business/\n│  │   │   │   ├─ business.controller.ts\n│  │   │   │   ├─ business.service.ts\n│  │   │   │   ├─ dto/\n│  │   │   │   ├─ repositories/\n│  │   │   │   └─ entities/\n│  │   │   ├─ booking/\n│  │   │   │   ├─ booking.controller.ts\n│  │   │   │   ├─ booking.service.ts\n│  │   │   │   ├─ dto/\n│  │   │   │   └─ entities/\n│  │   │   ├─ user/\n│  │   │   ├─ review/\n│  │   │   ├─ payment/\n│  │   │   ├─ notification/\n│  │   │   ├─ provider-portal/\n│  │   │   ├─ admin/\n│  │   │   ├─ common/\n│  │   │   │   ├─ interceptors/\n│  │   │   │   ├─ pipes/\n│  │   │   │   └─ utils/\n│  │   │   ├─ prisma/\n│  │   │   │   └─ schema.prisma\n│  │   │   └─ config/\n│  │   │       ├─ database.config.ts\n│  │   │       ├─ redis.config.ts\n│  │   │       └─ stripe.config.ts\n│  │   ├─ test/\n│  │   └─ package.json\n│  └─ worker/                 # NestJS BullMQ workers\n│      ├─ src/\n│      │   ├─ app.module.ts\n│      │   ├─ main.ts\n│      │   ├─ processors/\n│      │   │   ├─ notification.processor.ts\n│      │   │   ├─ availability.processor.ts\n│      │   │   └─ payment.webhook.processor.ts\n│      │   ├─ modules/ (re‑use same feature modules as API, but only import services)\n│      │   └─ config/\n│      └─ package.json\n├─ libs/\n│  ├─ types/                  # Shared TypeScript interfaces (DTOs, entities)\n│  │   └─ src/\n│  │       ├─ auth.ts\n│  │       ├─ business.ts\n│  │       ├─ booking.ts\n│  │       ├─ user.ts\n│  │       ├─ review.ts\n│  │       ├─ payment.ts\n│  │       └─ index.ts\n│  ├─ ui/                     # Design system components (React Native)\n│  │   └─ src/\n│  │       ├─ components/\n│  │       ├─ tokens.ts\n│  │       └─ index.ts\n│  ├─ utils/                  # Helper functions (geo, date, validation)\n│  │   └─ src/\n│  └─ eslint/                 # Shared ESLint config\n├─ tools/\n│   └─ generators/            # Nx schematic generators for feature modules\n├─ docker-compose.yml\n├─ nx.json\n├─ package.json (root pnpm workspace)\n└─ README.md\n```\n\n## Data Model (Prisma Schema Highlights)\n```prisma\nmodel User {\n  id            String   @id @default(uuid())\n  email         String   @unique\n  passwordHash  String\n  name          String\n  role          Role     @default(CUSTOMER)\n  createdAt     DateTime @default(now())\n  updatedAt     DateTime @updatedAt\n  bookings      Booking[]\n  reviews       Review[]\n  favorites     Favorite[]\n}\nmodel Business {\n  id            String   @id @default(uuid())\n  ownerId       String\n  name          String\n  description   String?\n  address       String\n  lat           Float\n  lng           Float\n  location      Point    @db.Point\n  category      String\n  phone         String?\n  website       String?\n  rating        Float    @default(0)\n  createdAt     DateTime @default(now())\n  updatedAt     DateTime @updatedAt\n  services      Service[]\n  workingHours  WorkingHour[]\n  bookings      Booking[]\n  reviews       Review[]\n}\nmodel Service {\n  id            String   @id @default(uuid())\n  businessId    String\n  name          String\n  durationMin   Int\n  price         Float\n  business      Business @relation(fields: [businessId], references: [id])\n}\nmodel WorkingHour {\n  id            String   @id @default(uuid())\n  businessId    String\n  dayOfWeek     Int      // 0‑6\n  openTime      String   // HH:MM\n  closeTime     String\n  business      Business @relation(fields: [businessId], references: [id])\n}\nmodel Booking {\n  id            String   @id @default(uuid())\n  userId        String\n  serviceId     String\n  startDateTime DateTime\n  endDateTime   DateTime\n  status        BookingStatus @default(PENDING)\n  paymentId     String?\n  user          User     @relation(fields: [userId], references: [id])\n  service       Service  @relation(fields: [serviceId], references: [id])\n}\nmodel Review {\n  id            String   @id @default(uuid())\n  businessId    String\n  userId        String\n  rating        Int\n  comment       String?\n  createdAt     DateTime @default(now())\n  business      Business @relation(fields: [businessId], references: [id])\n  user          User     @relation(fields: [userId], references: [id])\n}\nmodel Favorite {\n  id            String   @id @default(uuid())\n  userId        String\n  businessId    String\n  user          User     @relation(fields: [userId], references: [id])\n  business      Business @relation(fields: [businessId], references: [id])\n  @@unique([userId, businessId])\n}\nenum Role { CUSTOMER PROVIDER ADMIN }\nenum BookingStatus { PENDING CONFIRMED COMPLETED CANCELED }\n```\n\n## API Design Principles\n- **RESTful** resource‑oriented URLs (`/businesses`, `/businesses/:id`, `/bookings`, etc.).\n- **Versioning** via URL prefix (`/api/v1`).\n- **DTOs** for input validation; **entities** never exposed directly.\n- **Error handling** via custom exception filter returning consistent JSON shape.\n- **Authentication**: JWT access token (15 min) + refresh token (7 days) stored in HttpOnly cookie (web) / SecureStore (mobile).\n- **Authorization**: Guards for roles (`CUSTOMER`, `PROVIDER`, `ADMIN`).\n- **Geo queries**: Use PostGIS `ST_DWithin` for nearby businesses; exposed via `/businesses/near?lat=&lng=&radius=`.\n- **Pagination**: Cursor‑based (`limit`, `cursor`) for lists.\n- **Rate limiting**: Redis‑based limiter per IP.\n- **Caching**: Frequently read‑only data (categories, top‑rated businesses) cached in Redis with TTL 5‑15 min.\n\n## State Management & Data Flow (Mobile)\n1. **React Query** handles server state:\n   - `useQuery` for fetching lists, details, availability.\n   - `useMutation` for creating/updating/deleting (login, booking, review).\n   - Query invalidation on mutations to keep UI fresh.\n2. **Auth Context** provides `user`, `login`, `logout`, `refreshToken` functions.\n3. **Reanimated 2** used for:\n   - Animated tab transitions.\n   - Gesture‑based swipe to delete in favorites/bookings.\n   - Map marker animations (using `react-native-maps` + Reanimated).\n4. **Shared Types** imported from `@planity/types` ensure compile‑time consistency between mobile and API.\n5. **Offline‑first considerations**: Query results cached; optimistic updates for favorite toggles.\n\n## Background Jobs (BullMQ)\n- **Notification Processor**: Sends push notifications via Expo Push API; batches tokens; handles failures.\n- **Availability Processor**: Runs nightly and on business hour updates; computes future slots based on `WorkingHour` and existing bookings; stores slots in a denormalized `Availability` table (or computes on‑the‑fly with caching).\n- **Payment Webhook Processor**: Receives Stripe events, confirms payments, updates booking status, triggers confirmation notifications.\n- **Retry policy**: Exponential backoff, max 5 attempts; dead‑letter queue for manual inspection.\n\n## DevOps & CI/CD\n- **Local Development**: `docker compose up` starts PostgreSQL (with PostGIS), Redis, and optionally a mock Stripe CLI.\n- **Testing**: Jest unit tests for services, controllers, and React components; end‑to‑end tests using Detox for critical flows (login → booking).\n- **GitHub Actions**:\n    - Lint (ESLint, Prettier).\n    - Type check (`tsc --noEmit`).\n    - Run backend tests.\n    - Run mobile unit + Detox tests on Expo dev client.\n    - Build Docker images for API and Worker.\n    - On push to `main`: trigger EAS Build for iOS/Android preview; push Docker images to registry.\n- **Production**: Kubernetes or ECS (not detailed) runs API and Worker replicas; PostgreSQL managed (RDS/Aurora); Redis Elasticache; Expo updates via OTA; App Store/Play Store releases via EAS Build.\n\n## Security Considerations\n- Passwords hashed with bcrypt (salt 12).\n- JWT signed with RS256; private key stored in secret manager.\n- Sensitive data (payment info) never stored; only Stripe payment IDs.\n- Input validation and sanitization via class‑validator and Prisma parameterized queries.\n- CORS restricted to allowed origins (expo dev, web).\n- Rate limiting and brute‑force protection on auth endpoints.\n- Audit logs for admin actions (optional extension).\n\n## Scalability & Performance\n- **Read scaling**: Read replicas for PostgreSQL; Redis cache for hot data.\n- **Write scaling**: Sharding not required at MVP; can partition by `businessId` if needed.\n- **Worker scaling**: Multiple worker instances consume from same BullMQ queues; horizontal scaling.\n- **Geo queries**: PostGIS GIST index on `location` ensures fast nearest‑neighbor searches.\n- **Mobile**: Expo OTA allows pushing JS updates without store review; bundle splitting via Metro.\n\n## Monitoring & Observability\n- **Logging**: Structured JSON logs (winston) forwarded to Loki or CloudWatch.\n- **Metrics**: Prometheus endpoints (`/metrics`) on API and Worker; Grafana dashboards for request latency, queue depth, DB load.\n- **Error tracking**: Sentry integrated for both backend and mobile.\n- **Health checks**: `/health` endpoint returning DB, Redis, and queue status.\n\n## Summary\nThis architecture cleanly separates concerns into mobile client, API gateway, and worker services, leveraging a monorepo with Nx, TypeScript, and proven libraries to deliver a scalable, maintainable Planity Clone.\n",
  "summary": "This document outlines a scalable, clean‑architecture system for the Planity Clone using Expo, React Native, NestJS, Prisma, PostGIS, Redis, BullMQ, and Nx monorepo tooling."
}