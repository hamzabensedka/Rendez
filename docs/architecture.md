{
  "content": "# Planity Clone System Architecture\n\n## Overview\nThe Planity Clone is a mobile-first platform that connects users with local service businesses for discovery, booking, and management of appointments. The architecture follows a clean, modular approach using an Nx monorepo to share code between the Expo mobile app and the NestJS backend, ensuring consistency, maintainability, and scalability.\n\n## Tech Stack\n- **Mobile**: Expo, React Native, TypeScript, Expo Router, TanStack React Query, React Native Reanimated\n- **Backend**: NestJS, Prisma ORM, PostgreSQL with PostGIS extension, Redis, BullMQ (background jobs)\n- **Tooling**: Nx monorepo, pnpm workspace, Docker Compose, GitHub Actions, EAS Build, Jest\n- **Optional**: Supabase for auth/storage (can be replaced by custom NestJS auth)\n- **Design System**: Shared UI components and types across mobile and web portals\n\n## High-Level Architecture\nThe system is divided into three main layers:\n1. **Presentation Layer** – Expo mobile app (user-facing) and two web portals (Provider Owner Portal, Admin Dashboard) built with React/NestJS served as static assets or separate Next.js apps (out of scope but can be added).\n2. **Application Layer** – NestJS API providing REST/GraphQL endpoints, handling business logic, validation, authentication, and orchestrating domain services.\n3. **Infrastructure Layer** – PostgreSQL (primary data store) with PostGIS for geo‑queries, Redis for caching and job queue, BullMQ for background workers, Docker containers for each service, and CI/CD pipelines for automated testing, building, and deployment.\n\nCommunication:\n- Mobile app ↔ NestJS API via HTTPS (JSON payloads). React Query manages caching, background updates, and optimistic UI.\n- Web portals (if built with NestJS) serve server‑side rendered pages or act as micro‑frontends consuming the same API.\n- Background jobs (e.g., notification sending, slot recomputation) are triggered by API endpoints and processed by BullMQ workers.\n\n## Mobile Application (Expo/React Native)\n### Folder Structure\n```\nmobile/\n├── app/                     # Expo Router v2 (file‑based routing)\n│   ├── (tabs)/              # Tab navigator\n│   │   ├── index.tsx        # Home tab (guest browse)\n│   │   ├── search.tsx       # Search & map view\n│   │   ├── bookings.tsx     # Appointment management\n│   │   ├── favorites.tsx    # Favorites list\n│   │   └── profile.tsx      # User profile\n│   ├── auth/                # Auth flow (login, register, social)\n│   │   ├── login.tsx\n│   │   └── register.tsx\n│   ├── business/            # Business detail & services\n│   │   ├── [id]/            # Dynamic route for business ID\n│   │   │   ├── index.tsx\n│   │   │   ├── services.tsx\n│   │   │   └── reviews.tsx\n│   │   └── list.tsx         # Search results list\n│   ├── provider/            # Provider Owner Portal (web‑like screens inside app, optional)\n│   └── admin/               # Admin Dashboard screens (if embedded)\n├── components/              # Shared UI components (design system)\n│   ├── ui/                  # Primitive buttons, inputs, cards\n│   ├── layout/              # Headers, footers, modals\n│   └── business/            # Business‑specific components (map, slot picker)\n├── hooks/                   # Custom React hooks (e.g., useAuth, useQueryWrapper)\n├── lib/                     # Utility functions, constants, API client\n│   ├── api.ts               # Axios/fetch wrapper with interceptors\n│   ├── queryKeys.ts         # React Query key factories\n│   └── geo.ts               # Haversine / PostGIS helpers\n├── types/                   # Shared TypeScript interfaces (generated from backend schemas)\n│   ├── generated/           # Prisma‑generated DTOs via nx plugin\n│   └── domain.ts            # Domain models used in UI\n├── assets/                  # Images, icons, fonts\n├── styles/                  # Global styles, theme (Tailwind/NativeWind or styled‑components)\n├── tests/                   # Unit & integration tests (Jest + React Native Testing Library)\n└── app.json / expo.config.js\n```\n### Key Libraries\n- **Expo Router** – file‑based navigation, supports web and native.\n- **TanStack React Query** – data fetching, caching, background refetch.\n- **React Native Reanimated** – smooth gestures, animated maps, slot picker.\n- **React Native Maps** (or Mapbox) – map‑based search.\n- **React Hook Form** – form validation with Yup.\n- **Zod** – runtime schema validation (shared with backend via Nx).\n- **MMKV** or **AsyncStorage** – lightweight local storage for tokens.\n\n### State Management & Data Fetching\nReact Query is the primary state manager for server state. UI state (e.g., modal visibility, tab index) lives in React's useState/useContext or a lightweight Zustand store if needed. Auth context provides current user and token.\n\n### Navigation\nExpo Router provides:\n- Stack navigation inside each tab.\n- Deep linking support for sharing business URLs.\n- Automatic web fallback for provider/admin portals.\n\n### Animations\nReanimated 2 drives:\n- Animated tab transitions.\n- Gesture‑based slot dragging for rescheduling.\n- Interactive map markers with spring animations.\n\n### Shared Types & Design System\nA library `ui` (exposed via Nx) contains:\n- Styled components (NativeWind/Tailwind).\n- Theme tokens (colors, spacing).\n- Reusable primitives (Button, Input, Card, Avatar).\nAll components are typed with TypeScript and exported from `mobile/components/ui`.\n\nAn Nx library `types-shared` holds generated DTOs from Prisma (via `nx prisma:generate`) ensuring the mobile app and backend share the exact same TypeScript interfaces.\n\n## Backend API (NestJS)\n### Folder Structure\n```\nbackend/\n├── src/\n│   ├── main.ts                     # Bootstrap\n│   ├── app.module.ts               # Root module\n│   ├── config/                     # Environment validation (dotenv)\n│   ├── common/                     # Guards, interceptors, pipes, exceptions\n│   │   ├── auth/\n│   │   │   ├── jwt-auth.guard.ts\n│   │   │   ├── roles.guard.ts\n│   │   │   └── jwt.strategy.ts\n│   │   ├── validation/\n│   │   │   └── zod-validation.pipe.ts\n│   │   └── logging/\n│   │       └── logger.service.ts\n│   ├── auth/                       # Auth module (register, login, social)\n│   │   ├── auth.controller.ts\n│   │   ├── auth.service.ts\n│   │   └── dto/\n│   │       ├── register.dto.ts\n│   │       └── login.dto.ts\n│   ├── users/                      # User profile CRUD\n│   │   ├── users.controller.ts\n│   │   ├── users.service.ts\n│   │   └── dto/\n│   ├── businesses/                 # Business listing & search\n│   │   ├── businesses.controller.ts\n│   │   ├── businesses.service.ts\n│   │   ├── dto/\n│   │   └── repositories/           # Prisma repository pattern\n│   ├── services/                   # Services offered by businesses\n│   │   ├── services.controller.ts\n│   │   ├── services.service.ts\n│   │   └── dto/\n│   ├── bookings/                   # Booking flow & management\n│   │   ├── bookings.controller.ts\n│   │   ├── bookings.service.ts\n│   │   ├── dto/\n│   │   └── scheduling/             # Slot computation logic\n│   ├── payments/                   # Payment gateway integration (Stripe/PayPal)\n│   │   ├── payments.controller.ts\n│   │   ├── payments.service.ts\n│   │   └── dto/\n│   ├── reviews/                    # Reviews & ratings\n│   │   ├── reviews.controller.ts\n│   │   ├── reviews.service.ts\n│   │   └── dto/\n│   ├── notifications/              # Notification preferences & templates\n│   │   ├── notifications.controller.ts\n│   │   ├── notifications.service.ts\n│   │   └── dto/\n│   ├── provider/                   # Owner portal API (protected by role guard)\n│   │   ├── provider.controller.ts\n│   │   ├── provider.service.ts\n│   │   └── dto/\n│   ├── admin/                      # Admin dashboard API\n│   │   ├── admin.controller.ts\n│   │   ├── admin.service.ts\n│   │   └── dto/\n│   └── jobs/                       # BullMQ workers & queues\n│       ├── queues/\n│       │   ├── notification.queue.ts\n│       │   └── slot-recompute.queue.ts\n│       ├── workers/\n│       │   ├── notification.worker.ts\n│       │   └── slot-recompute.worker.ts\n│       └── job.module.ts\n├── prisma/\n│   ├── schema.prisma               # DB schema with PostGIS extensions\n│   └── seed.ts                     # Seed data for dev\n├── test/                           # Jest unit & e2e tests\n├── Dockerfile\n└── tsconfig.json\n```\n### Service Boundaries\n| Domain | Responsibility | Key Tables |\n|--------|----------------|------------|\n| **Auth** | User registration, login, JWT issuance, social OAuth | `User` |\n| **Users** | Profile CRUD, preferences | `UserProfile` |\n| **Businesses** | CRUD, geo‑search, categorization | `Business`, `Category`, `BusinessCategory` |\n| **Services** | Service items, pricing, duration | `Service` |\n| **Bookings** | Appointment creation, rescheduling, cancellation, status | `Booking`, `TimeSlot` |\n| **Payments** | Payment intent, webhook handling, transaction logs | `Payment`, `Invoice` |\n| **Reviews** | Ratings, comments, moderation | `Review` |\n| **Notifications** | Template storage, user preferences, dispatch logs | `Notification`, `UserNotificationPreference` |\n| **Provider** | Owner‑specific business management, schedule, staff | `Provider`, `Staff`, `WorkingHours` |\n| **Admin** | Global oversight, user/business moderation, analytics | `AuditLog`, `Settings` |\n| **Jobs** | Background processing (email/SMS, slot recomputation) | N/A (queues in Redis) |\n\n### Database\n- **PostgreSQL** with **PostGIS** column `location` on `Business` for radius queries.\n- Prisma ORM generates TypeScript client; migrations managed via `prisma migrate`.\n- Indexes: spatial index on `location`, composite indexes on `(businessId, startTime)` for slot lookup.\n\n### Caching & Background Jobs\n- **Redis** stores:\n  - Access token blacklist (logout).\n  - Cached business list pages (short TTL).\n  - BullMQ job queue.\n- **BullMQ** workers:\n  - `notification.queue` → sends email/SMS via external provider (e.g., SendGrid, Twilio).\n  - `slot-recompute.queue` → recalculates available slots when schedule changes or a booking is made/removed.\n\n### Authentication & Authorization\n- JWT (access token 15 min, refresh token 7 days) stored in SecureStore (expo) and HTTP‑only cookie for web.\n- Role‑based guards: `USER`, `PROVIDER`, `ADMIN`.\n- Custom Zod validation pipe ensures DTOs conform to Prisma models.\n\n### Provider Portal & Admin Dashboard\n- Implemented as separate NestJS modules serving server‑side rendered pages (using `@nestjs/platform-express` with `express-react-views` or as a Next.js app consuming the same API). For simplicity, they share the same API and reuse DTOs.\n\n## Shared Monorepo (Nx)\n```\nworkspace/\n├── apps/\n│   ├── mobile/          # Expo app\n│   └── backend/         # NestJS API\n├── libs/\n│   ├── ui/              # Design system primitives (React Native + Web)\n│   ├── types-shared/    # Generated DTOs from Prisma\n│   ├── geo-utils/       # Haversine, PostGIS helpers (shared)\n│   ├── validation/      # Zod schemas reused across frontend & backend\n│   └── eslint-config/   # Shared lint rules\n└── tools/\n    ├── generators/      # Custom Nx generators (e.g., lib-schema)\n│   └── scripts/         # Dev utilities\n```\n- **pnpm** workspace hoists dependencies, ensuring consistent versions.\n- Nx task pipeline: `lint → test → build` for each affected project.\n- Code generation: `nx g @nestjs/schematics:resource business` automatically creates DTOs, controller, service, and updates `types-shared` via a custom generator that runs `prisma generate`.\n\n## DevOps & CI/CD\n### Docker Compose (local)\n```yaml\nversion: \"3.8\"\nservices:\n  postgres:\n    image: postgis/postgis:15\n    environment:\n      POSTGRES_USER: dev\n      POSTGRES_PASSWORD: dev\n      POSTGRES_DB: planity\n    volumes:\n      - pgdata:/var/lib/postgresql/data\n    ports: [\"5432:5432\"]\n  redis:\n    image: redis:7\n    ports: [\"6379:6379\"]\n  api:\n    build: ./backend\n    env_file: .env\n    depends_on: [postgres, redis]\n    ports: [\"3000:3000\"]\n  # mobile runs via expo start, not containerized locally\nvolumes:\n  pgdata:\n```\n### GitHub Actions\n- **Workflow**: `ci.yml`\n  - On push/pull request to `main`:\n    - Setup pnpm, Node.js.\n    - Run `nx run-many --target=lint --all`.\n    - Run `nx run-many --target=test --all`.\n    - Build Docker images for API.\n    - Push images to GitHub Packages (or Docker Hub).\n- **Workflow**: `cd.yml`\n  - On release tag:\n    - Deploy API to a staging Kubernetes cluster (or VM) via Docker Compose/Helm.\n    - Trigger EAS build for mobile (see below).\n    - Run smoke tests against deployed endpoints.\n\n### EAS Build (Expo)\n- Configured in `eas.json` with profiles:\n  - `development`: `expo-dev-client`.\n  - `preview`: internal distribution.\n  - `production`: app‑store/play‑store.\n- EAS Update used for OTA JS updates (optional).\n- Secrets (API keys, Firebase credentials) stored in EAS Secrets and injected at build time.\n\n### Supabase (optional)\n- Can replace custom Auth & Storage:\n  - Auth via Supabase JWT (validated via JWKS).\n  - Storage for business images.\n- If used, the `auth` module becomes a thin proxy.\n\n## Data Flow Examples\n1. **Guest Browse & Search**\n   - Mobile app sends GET `/businesses?lat=…&lon=…&radius=…&category=…` to API.\n   - API uses Prisma + PostGIS `ST_DWithin` to fetch nearby businesses.\n   - Result cached in Redis for 30 s; React Query refetches on focus.\n\n2. **User Registration**\n   - Submit form → `POST /auth/register` with Zod‑validated DTO.\n   - Service hashes password, creates `User` record, returns access & refresh tokens.\n   - Tokens stored in SecureStore; Axios interceptor attaches access token to requests.\n\n3. **Booking Flow**\n   - User selects service → API returns available slots via `GET /bookings/slots?businessId=&serviceId=&date=` (computed from `WorkingHours` minus existing `Booking`).\n   - User picks slot → `POST /bookings` creates `Booking` record, publishes job to `slot-recompute.queue`.\n   - Worker recalculates affected slots and updates cache.\n   - Payment intent created via Stripe; webhook confirms payment → updates `Booking.status` to `CONFIRMED`.\n   - Notification job enqueued → worker sends email/SMS and push notification via Expo push service.\n\n4. **Provider Portal – Schedule Update**\n   - Provider edits `WorkingHours` → `PUT /provider/working-hours`.\n   - API validates, updates record, enqueues slot recompute for affected date ranges.\n   - Mobile app receives real‑time update via React Query refetch interval or websocket (optional).\n\n## Conclusion\nThis architecture delivers a scalable, maintainable foundation for the Planity Clone. By leveraging an Nx monorepo, we keep types and UI primitives in sync across mobile and web. The NestJS/Prisma/PostGIS backend provides robust business logic, geo‑search, and transactional integrity, while Expo, React Query, and Reanimated deliver a polished, responsive mobile experience. CI/CD pipelines with Docker, GitHub Actions, and EAS ensure rapid, reliable releases.\n",
  "summary": "A clean, modular architecture using an Nx monorepo with Expo/React Native mobile, NestJS/Prisma/PostgreSQL+PostGIS backend, Redis/BullMQ for caching and jobs, and CI/CD via Docker, GitHub Actions, and EAS Build."
}