{
  "content": "# Architecture Document\n\n## Overview\nThis document outlines the system architecture for the Planity Clone, a mobile-first appointment booking platform. The solution leverages a monorepo managed by Nx with pnpm, separating concerns into mobile (Expo/React Native), backend (NestJS), shared libraries, and infrastructure (Docker, CI/CD). Key technologies include TypeScript, Expo Router, TanStack React Query, React Native Reanimated, Prisma with PostgreSQL/PostGIS, Redis, BullMQ, Supabase (auth/storage), EAS Build, and GitHub Actions.\n\n## High-Level Architecture\n- **Mobile App** (Expo) consumes backend REST/GraphQL APIs via React Query, handles UI/UX with Reanimated animations, and stores minimal state locally (AsyncStorage).\n- **Backend API** (NestJS) provides domain‑driven services, exposes REST endpoints (or GraphQL if needed), enforces business logic, validates data via DTOs, and interacts with the database through Prisma.\n- **Database**: PostgreSQL with PostGIS extension for geospatial queries (location‑based search). Prisma generates type‑safe client.\n- **Cache/Job Queue**: Redis stores session tokens, caches frequent queries (e.g., business listings), and backs BullMQ for background jobs (notifications, slot computation).\n- **Infrastructure**: Docker Compose orchestrates local dev services (API, DB, Redis). GitHub Actions runs lint, test, build, and pushes Docker images. EAS Build creates iOS/Android binaries. Supabase provides optional auth/storage fallback.\n- **Shared Libraries** (Nx libs) contain reusable UI components, TypeScript types, utility functions, and design tokens, ensuring consistency across mobile and backend.\n\n## Domain Boundaries\n| Domain        | Responsibility                                                                 | Mobile (Feature)                     | Backend (NestJS Module)          |\n|---------------|-------------------------------------------------------------------------------|--------------------------------------|----------------------------------|\n| Auth          | User registration, login, social auth, JWT handling                           | `auth/` screens, `useAuth` hook      | `AuthModule` (JWT strategy)      |\n| Business      | CRUD, search, geolocation filtering, categories, hours                        | `business/` screens, map view        | `BusinessModule` (Prisma repo)   |\n| Booking       | Slot computation, appointment creation, validation, payment initiation        | `booking/` flow screens              | `BookingModule` (service)        |\n| Payment       | Integration with payment gateway (Stripe/PayPal), webhook handling            | `payment/` screen                    | `PaymentModule` (gateway client) |\n| Notification  | In‑app & push notifications, preference management                            | `notification/` center               | `NotificationModule` (BullMQ)    |\n| Review        | Ratings, comments, owner responses                                            | `review/` screen                     | `ReviewModule`                   |\n| Favorite      | User‑specific bookmarking                                                     | `favorite/` tab                      | `FavoriteModule`                 |\n| Profile       | User data CRUD, avatar upload                                                 | `profile/` screen                    | `UserModule`                     |\n| Admin         | Dashboard, user/business moderation, analytics                                | `admin/` screens (web‑like)          | `AdminModule` (RBAC)             |\n| Provider      | Business owner portal: manage services, availability, bookings                | `provider/` screens                  | `ProviderModule`                 |\n| Background    | Slot recomputation, email/SMS reminders, cleanup jobs                         | —                                    | `JobsModule` (BullMQ processors) |\n\n## Folder Structure (Nx Monorepo)\n```\nplanity-clone/\n├─ apps/\n│  ├─ mobile/                 # Expo React Native app\n│  │   ├─ assets/\n│  │   │   ├─ images/\n│  │   │   └─ icons/\n│  │   ├─ src/\n│  │   │   ├─ components/      # Reusable UI (shared via libs/ui)\n│  │   │   ├─ screens/         # Route‑based screens (Expo Router)\n│  │   │   ├─ hooks/           # Custom React hooks (query, auth, etc.)\n│  │   │   ├─ services/        # API clients (axios/react-query wrappers)\n│  │   │   ├─ types/           # App‑specific TS types (re-export from libs/types)\n│  │   │   ├─ utils/           # Helper functions\n│  │   │   ├─ constants/\n│  │   │   └─ App.tsx          # Root (providers, routing)\n│  │   └─ expo-router/\n│  │       └─ (tabs)/          # Tab navigator (Home, Search, Map, Profile, etc.)\n│  └─ api/                    # NestJS backend\n│      ├─ src/\n│      │   ├─ modules/\n│      │   │   ├─ auth/\n│      │   │   ├─ business/\n│      │   │   ├─ booking/\n│      │   │   ├─ payment/\n│      │   │   ├─ notification/\n│      │   │   ├─ review/\n│      │   │   ├─ favorite/\n│      │   │   ├─ user/\n│      │   │   ├─ admin/\n│      │   │   ├─ provider/\n│      │   │   └─ jobs/          # BullMQ processors\n│      │   ├─ libs/\n│      │   │   ├─ guards/      # Auth, RBAC\n│      │   │   ├─ interceptors/\n│      │   │   ├─ pipes/\n│      │   │   └─ utils/\n│      │   ├─ prisma/\n│      │   │   ├─ schema.prisma\n│      │   │   └─ migrations/\n│      │   ├─ main.ts\n│      │   └─ app.module.ts\n│      └─ test/\n│          ├─ unit/\n│          └─ e2e/\n├─ libs/\n│  ├─ ui/                     # Shared React Native components (buttons, cards, modals)\n│  ├─ types/                  # Domain‑level TS interfaces (User, Business, Booking, etc.)\n│  ├─ utils/                  # Cross‑platform helpers (date, geo, validation)\n│  ├─ design-tokens/          # Colors, spacing, typography (used by ui)\n│  └─ config/                 # Expo, NestJS, EAS, Docker configs\n├─ docker/\n│  ├─ compose.yml             # Dev: api, db, redis\n│  └─ Dockerfile.api\n├─ .github/\n│   └─ workflows/\n│       ├─ ci.yml             # lint, test, build\n│       └─ cd.yml             # Docker push, EAS build trigger\n├─ prisma/\n│   └─ schema.prisma          # Shared with api/src/prisma\n├─ README.md\n└─ nx.json\n```\n\n## Service Boundaries & Communication\n- **Mobile ↔ API**: REST (JSON) over HTTPS; Axios instance wrapped with React Query for caching, background refetch, and optimistic updates.\n- **API ↔ Database**: Prisma Client (generated from schema). Transactions used for booking creation (slot lock → payment → appointment).\n- **API ↔ Redis**: \n  - Session store (JWT refresh tokens).\n  - Cache layer (`@nestjs/cache-manager`) for expensive queries (search results, business lists).\n  - BullMQ queue backend for background jobs.\n- **Background Jobs**: BullMQ workers defined in `apps/api/src/modules/jobs`. Examples:\n  - `slot-computation`: runs nightly / on availability change to generate future time slots.\n  - `notification-sender`: processes email/SMS/push jobs.\n  - `cleanup-expired-slots`: removes stale provisional bookings.\n- **Real‑time (optional)**: NestJS Gateway with WebSocket (via `ws` adapter) for live booking status updates; fallback to polling via React Query.\n- **External Services**:\n  - Payment gateway (Stripe) – webhook endpoint in `PaymentModule`.\n  - Push notifications – Expo Push API (backend sends to Expo using push token stored in User entity).\n  - Supabase – optional auth fallback or file storage for avatars.\n\n## Data Model (Prisma Schema Highlights)\n```prisma\nmodel User {\n  id            String   @id @default(uuid())\n  email         String   @unique\n  password      String\n  role          Role     @default(USER)\n  profile       Profile? @relation(fields: [profileId], references: [id])\n  profileId     String?  @unique\n  businesses    Business[] @relation(\"ownerBusinesses\")\n  bookings      Booking[] @relation(\"customerBookings\")\n  favorites     Favorite[]\n  reviews       Review[]\n  notifications Notification[]\n  createdAt     DateTime @default(now())\n  updatedAt     DateTime @updatedAt\n}\nmodel Business {\n  id            String   @id @default(uuid())\n  ownerId       String\n  owner         User     @relation(\"ownerBusinesses\", fields: [ownerId], references: [id])\n  name          String\n  description   String?\n  address       String\n  lat           Float\n  lng           Float\n  location      Point    @db.Postgis\n  categoryId    String\n  category      Category @relation(fields: [categoryId], references: [id])\n  hours         BusinessHours[]\n  services      Service[]\n  bookings      Booking[] @relation(\"businessBookings\")\n  reviews       Review[]\n  createdAt     DateTime @default(now())\n}\nmodel Booking {\n  id            String   @id @default(uuid())\n  customerId    String\n  customer      User     @relation(\"customerBookings\", fields: [customerId], references: [id])\n  businessId    String\n  business      User     @relation(\"businessBookings\", fields: [businessId], references: [id])\n  serviceId     String\n  service       Service\n  startTime     DateTime\n  endTime       DateTime\n  status        BookingStatus\n  paymentId     String?\n  createdAt     DateTime @default(now())\n}\n// ... other models: Service, Category, BusinessHours, Favorite, Review, Notification, etc.\n```\nGeospatial index on `location` enables radius searches via Prisma’s `@db.Postgis` and raw SQL when needed.\n\n## Caching Strategy\n- **Listings**: Cache business search results for 5 min keyed by `(query, lat, lng, radius, category)`.\n- **User Profile**: Cache per‑user data for 2 min; invalidated on update via Redis pub/sub.\n- **Session**: Store JWT refresh tokens with TTL matching refresh token life (7 days).\n\n## Security Considerations\n- **Authentication**: JWT access token (15 min) + refresh token (HTTP‑only cookie or AsyncStorage with encryption).\n- **Authorization**: NestJS guards (`RolesGuard`, `OwnershipGuard`) and RBAC roles (`ADMIN`, `PROVIDER`, `USER`).\n- **Data Validation**: DTOs with class‑validator & class‑transformer; Prisma enforces DB constraints.\n- **Input Sanitization**: Use Prisma parameterized queries; escape HTML in review text client‑side.\n- **Rate Limiting**: `@nestjs/throttler` on auth and public endpoints.\n- **Secrets**: Managed via Docker secrets / GitHub Actions environment variables; never committed.\n\n## Development Workflow\n1. **Local Dev**: `docker compose up -d` starts API, PostgreSQL, Redis. Run `nx serve mobile` and `nx serve api`.\n2. **Testing**: Unit tests with Jest (`nx test <lib>`). E2E tests for mobile using Detox (optional) and API using SuperTest.\n3. **CI**: GitHub Actions runs lint (`nx lint`), test (`nx test`), build (`nx build`), and pushes Docker images to registry.\n4. **CD**: On tag push, EAS Build creates iOS/Android binaries; Docker images deployed to staging/production via Kubernetes or ECS (out of scope).\n5. **Database Migrations**: `nx migrate` runs Prisma Migrate; migrations stored under `apps/api/prisma/migrations`.\n\n## Deployment Architecture (Production)\n- **API**: Containerized NestJS app behind ALB/Nginx, autoscaling group.\n- **Database**: Managed PostgreSQL (Amazon RDS / Supabase) with PostGIS extension.\n- **Cache/Job Queue**: Elasticache Redis (or Redis Labs) serving both cache and BullMQ.\n- **Storage**: S3‑compatible bucket for user uploads (avatars, business photos) – integrated via Supabase storage or direct AWS SDK.\n- **Mobile**: Distributed via App Store / Play Store using EAS Build.\n- **Monitoring**: CloudWatch logs, Prometheus + Grafana for metrics; Sentry for error tracking.\n\n## Cross‑Cutting Concerns\n- **Design System**: Shared `libs/ui` and `libs/design-tokens` ensure consistent colors, spacing, typography, and component behavior across mobile screens.\n- **Type Safety**: Shared `libs/types` exported to both mobile and backend; Prisma generates types that are re‑exported where needed.\n- **State Management**: React Query handles server state; React Context or Zustand for UI‑only state (theme, auth status).\n- **Internationalization**: i18next wrapper in `libs/utils`; JSON locale files under `apps/mobile/src/locales`.\n- **Accessibility**: Follow WCAG 2.1; use React Native accessibility props; test with axe‑core.\n\n## Summary\nThe architecture separates concerns into a modular Nx monorepo with a React Native mobile app (Expo, Router, Reanimated, Query) and a NestJS backend (Prisma, PostgreSQL/PostGIS, Redis, BullMQ), supported by shared UI/types libraries, Docker‑based dev environment, GitHub Actions CI/CD, and EAS Build for distribution, delivering a scalable, maintainable foundation for the Planity Clone.\n",
  "summary": "A modular Nx monorepo separates an Expo/React Native mobile app from a NestJS backend, using Prisma/PostGIS/PostgreSQL, Redis/BullMQ, shared UI/types, Docker, GitHub Actions, and EAS Build for a scalable, maintainable Planity Clone."
}