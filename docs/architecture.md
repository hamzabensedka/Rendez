{
  "content": "# Planity Clone System Architecture\n\n## 1. Overview\nPlanity Clone is a monorepo‑based full‑stack application consisting of a cross‑platform mobile client (Expo/React Native) and a set of backend services (NestJS) that share a PostgreSQL/PostGIS database, Redis caching, and background job processing (BullMQ). The architecture follows Clean Architecture principles: clear separation of concerns, dependency inversion, and domain‑driven boundaries.\n\n## 2. Tech Stack Summary\n- **Mobile**: Expo SDK, React Native, TypeScript, Expo Router, TanStack React Query, React Native Reanimated, Expo‑Managed Build (EAS)\n- **Web/Admin Portals**: Next.js (optional) or plain React with same design system – not required for MVP but scaffolded\n- **Backend**: NestJS (Node.js), TypeORM‑like Prisma ORM, PostgreSQL + PostGIS extension, Redis, BullMQ\n- **DevOps**: Nx monorepo, pnpm workspaces, Docker Compose (local), GitHub Actions (CI/CD), EAS Build (mobile), Supabase (optional hosted DB for preview)\n- **Testing**: Jest (unit/integration), React Native Testing Library, Supertest (API), E2E with Detox (optional)\n\n## 3. High‑Level Architecture\n```\n+-------------------+        +---------------------+        +-------------------+\n|   Mobile App      | <--->  |   API Gateway (Nest) | <--->  |   Auth Service    |\n| (Expo/React)      |        |   (REST + GraphQL)  |        |   (NestJS)        |\n+-------------------+        +---------------------+        +-------------------+\n        ^                           ^                           ^\n        |                           |                           |\n        |                           |                           |\n        v                           v                           v\n+-------------------+        +---------------------+        +-------------------+\n|  Business Service | <--->  |  Booking Service    | <--->  |  Notification Svc |\n| (NestJS)          |        | (NestJS)            |        | (NestJS + BullMQ) |\n+-------------------+        +---------------------+        +-------------------+\n        ^                           ^                           ^\n        |                           |                           |\n        |                           |                           |\n        v                           v                           v\n+-------------------+        +---------------------+        +-------------------+\n|   Payment Svc     | <--->  |  Admin/Portal Svc   | <--->  |   Analytics Svc   |\n| (NestJS)          |        | (NestJS)            |        | (NestJS)          |\n+-------------------+        +---------------------+        +-------------------+\n        \\                           |                           /\n         \\                          |                         /\n          \\                         |                        /\n           \\                       |                       /\n            \\                      |                      /\n             \\                     |                     /\n              \\                    |                    /\n               \\                   |                   /\n                \\                  |                  /\n                 \\                 |                 /\n                  \\                |                /\n                   \\               |               /\n                    \\              |              /\n                     \\             |             /\n                      \\            |            /\n                       \\           |           /\n                        \\          |          /\n                         \\         |         /\n                          \\        |        /\n                           \\       |       /\n                            \\      |      /\n                             \\     |     /\n                              \\    |    /\n                               \\   |   /\n                                \\  |  /\n                                 \\ | /\n                                  \\|/\n                             +-----------------+\n                             |  Data Store     |\n                             | (PostgreSQL/PostGIS, Redis) |\n                             +-----------------+\n```\n\n### 3.1 Service Boundaries\n| Service | Responsibility | Key Tables / Collections | Communication |
|---------|----------------|--------------------------|---------------|
| **Auth** | User registration, login, password reset, social OAuth, JWT issuance | `users`, `user_sessions`, `oauth_providers` | REST/GraphQL (public) |
| **Business** | CRUD of business profiles, categories, location (PostGIS), hours, services | `businesses`, `business_categories`, `business_hours`, `services`, `business_locations` | REST/GraphQL |
| **Booking** | Appointment creation, rescheduling, cancellation, slot computation, conflict detection | `appointments`, `appointment_slots`, `bookings`, `booking_transactions` | REST/GraphQL, internal events via BullMQ |
| **Notification** | Send push/email/SMS via providers (Firebase, Twilio, SendGrid) – jobs queued by Booking/Payment services | `notifications`, `notification_templates` | BullMQ workers, REST callbacks |
| **Payment** | Integration with payment gateway (Stripe/PayPal), webhook handling, refunds | `payments`, `payment_refunds` | REST/GraphQL, webhook endpoints |
| **Admin/Portal** | Business owner dashboard (listings, schedule, staff), admin dashboard (users, content, settings) | Same core tables + `staff`, `admin_roles`, `content_blocks` | REST/GraphQL (protected) |
| **Analytics** (optional) | Aggregated metrics, reporting, export | `events`, `aggregates` (materialized views) | Internal jobs, read‑replica queries |
| **Shared** | Common DTOs, validation schemas, utility functions, design system tokens, TypeScript enums | N/A (library) | Imported by all services\n\n### 3.2 Data Flow Highlights\n- **Guest Browse**: Mobile → Business Service (read‑only) → PostgreSQL (with PostGIS for geo‑queries) → cached list in Redis (TTL 5 min).\n- **Auth Flow**: Mobile → Auth Service (login) → JWT → stored in SecureStore; subsequent requests include Authorization header.\n- **Booking**: Mobile → Booking Service (validate slot via cached availability) → Prisma transaction creates appointment + updates slot table → publishes `booking.created` job → Notification Service sends confirmation.\n- **Availability Computation**: Background job (cron) runs nightly to pre‑compute slots for next 7 days per business; also invalidated on each booking change via BullMQ.\n- **Map Search**: Mobile passes bounding box → Business Service uses PostGIS `ST_Intersects` → returns GeoJSON.\n- **Webhooks**: Payment gateway → Payment Service → updates `payments` table → triggers `payment.completed` job → Notification Service.\n\n## 4. Folder Structure (Nx Monorepo)\n```\nplanity-clone/\n├─ apps/\n│  ├─ mobile/               # Expo React Native app\n│  │   ├─ src/\n│  │   │   ├─ assets/\n│  │   │   ├─ components/\n│  │   │   ├─ screens/\n│  │   │   ├─ navigation/\n│  │   │   ├─ hooks/\n│  │   │   ├─ store/\n│  │   │   ├─ utils/\n│  │   │   ├─ theme/          # Design system tokens (styled‑components or nativewind)\n│  │   │   ├─ types/          # Shared UI types (re-exported from libs/shared/types)\n│  │   │   ├─ services/       # React Query API clients (generated from OpenAPI)\n│  │   │   ├─ config/         # Expo config, env vars\n│  │   │   └─ App.tsx\n│  │   └─ expo/\n│  │      ├─ dev-client.ts\n│  │      └─ production.ts\n│  ├─ web/                  # Optional Next.js admin portal (same libs)\n│  └─ api/                  # NestJS monolith (micro‑service style via Nx libs)\n│      ├─ src/\n│      │   ├─ main.ts\n│      │   ├─ app.module.ts\n│      │   ├─ auth/\n│      │   │   ├─ controller/\n│      │   │   ├─ service/\n│      │   │   ├─ dto/\n│      │   │   ├─ guards/\n│      │   │   └─ strategies/\n│      │   ├─ business/\n│      │   │   ├─ controller/\n│      │   │   ├─ service/\n│      │   │   ├─ dto/\n│      │   │   ├─ repositories/ (Prisma)\n│      │   │   └─ entities/\n│      │   ├─ booking/\n│      │   │   ├─ controller/\n│      │   │   ├─ service/\n│      │   │   ├─ dto/\n│      │   │   ├─ repositories/\n│      │   │   ├─ sagas/ (optional for complex flows)\n│      │   │   └─ entities/\n│      │   ├─ notification/\n│      │   │   ├─ controller/\n│      │   │   ├─ service/\n│      │   │   ├─ dto/\n│      │   │   ├─ queues/ (BullMQ)\n│      │   │   └─ entities/\n│      │   ├─ payment/\n│      │   │   ├─ controller/\n│      │   │   ├─ service/\n│      │   │   ├─ dto/\n│      │   │   ├─ webhooks/\n│      │   │   └─ entities/\n│      │   ├─ admin/\n│      │   │   ├─ controller/\n│      │   │   ├─ service/\n│      │   │   ├─ dto/\n│      │   │   └─ entities/\n│      │   ├─ shared/\n│      │   │   ├─ dto/\n│      │   │   ├─ guards/\n│      │   │   ├─ interceptors/\n│      │   │   ├─ pipes/\n│      │   │   ├─ utils/\n│      │   │   └─ constants/\n│      │   └─ prisma/\n│      │       ├─ schema.prisma\n│      │       └─ migrations/\n│      └─ test/\n│          ├─ unit/\n│          └─ integration/\n├─ libs/\n│  ├─ ui/                   # Shared React Native components (design system)\n│  │   ├─ src/\n│  │   │   ├─ components/\n│  │   │   ├─ tokens.ts\n│  │   │   └─ index.ts\n│  ├─ types/                # TypeScript interfaces/enums used across mobile & api\n│  │   └─ src/\n│  │       ├─ auth.ts\n│  │       ├─ business.ts\n│  │       ├─ booking.ts\n│  │       ├─ payment.ts\n│  │       └─ common.ts\n│  ├─ utils/                # Logging, error handling, date helpers, geo utils\n│  │   └─ src/\n│  ├─ config/               # Nx workspace config, jest, eslint, prettier\n│  │   └─ workspace.json\n│  └─ openapi/              # Generated OpenAPI spec (used by mobile React Query)\n│      └─ schema.yaml\n├─ docker/\n│  ├─ compose.yml           # Local dev: api, db, redis, mailhog\n│  └─ Dockerfile.api\n├─ .github/\n│  └─ workflows/\n│      ├─ ci.yml            # Lint, test, build\n│      └─ cd.yml            # Publish to EAS, deploy API to render/fly.io\n├─ tools/\n│  └─ scripts/\n│      ├─ db:migrate.ts\n│      └─ seed.ts\n├─ jest.config.js\n├─ tsconfig.base.json\n├─ nx.json\n└─ package.json (pnpm workspace root)\n```\n\n### 4.1 Key Conventions\n- **Domain‑Driven**: Each NestJS feature (`business`, `booking`, …) lives in its own folder with controller, service, dto, repositories, entities.\n- **Shared Library**: `libs/types` contains plain TypeScript interfaces that are consumed by both mobile (via generated OpenAPI) and backend.\n- **Design System**: `libs/ui` exports styled components (using `nativewind` or `styled-components`) and token definitions; mobile imports from here.\n- **State Management**: Mobile uses React Query for server state; local UI state handled with React Context or Zustand (optional).\n- **Code Generation**: `nx run api:generate-openapi` creates `libs/openapi/schema.yaml`; mobile runs `npx openapi-typescript` to produce typed query hooks.\n- **Testing**: Unit tests with Jest; integration tests with Supertest (API) and React Native Testing Library (mobile).\n\n## 5. Infrastructure & DevOps\n- **Local Development**: Docker Compose spins up PostgreSQL (with PostGIS extension), Redis, Mailhog (for email preview), and the NestJS API. Expo dev client runs locally.\n- **CI**: GitHub Actions runs on PR:\n  1. Install pnpm workspace\n  2. Lint (eslint)\n  3. Type check (tsc)\n  4. Run unit tests (jest)\n  5. Build Docker image for API\n  6. Publish Expo build to EAS (preview)\n- **CD**: On merge to `main`:\n  1. Deploy API to a managed Node.js platform (Render, Fly.io, or AWS ECS Fargate) using Docker.\n  2. Run Prisma migrations via release hook.\n  3. Trigger EAS build for production (iOS/Android) and distribute to internal testers or stores.\n  4. Clear Redis cache via API endpoint.\n- **Observability**: Structured logging (pino) → forwarded to Loki/Grafana; metrics via Prometheus (NestJS `@nestjs/metrics`); tracing via OpenTelemetry.\n- **Secrets**: Managed via GitHub Secrets (dev) and platform‑specific secret stores (Render env, EAS secrets).\n\n## 6. Data Layer Details\n- **PostgreSQL + PostGIS**: Stores geolocation (`POINT`) for businesses; enables radius queries (`ST_DWithin`).\n- **Prisma Schema**: Defines relations with cascade rules; uses `@unique` constraints for email, slug.\n- **Redis**: \n  - Cache layer for business list/search results (key: `browse:list:{filters}`).\n  - Rate limiting tokens (token bucket).\n  - BullMQ job queue storage (optional separate Redis instance).\n- **Backup Strategy**: Daily logical dump (`pg_dump`) to S3; point‑in‑time recovery enabled via WAL archiving.\n\n## 7. Security Considerations\n- **Authentication**: JWT (access token 15 min, refresh token 7 days) stored in SecureStore; refresh token rotated.\n- **Authorization**: NestJS guards (`RolesGuard`, `OwnershipGuard`) enforce RBAC.\n- **Input Validation**: DTOs with class‑validator & class‑transformer; sanitization via Prisma parameterized queries.\n- **CORS**: Restrict to known domains (expo dev, production web).\n- **HTTPS**: Enforced via TLS termination at platform level.\n- **Data Privacy**: GDPR‑ready – endpoints for data export/deletion; personal data encrypted at rest (AWS RDS encryption or platform equivalent).\n\n## 8. Scalability & Performance\n- **Read Replicas**: PostgreSQL read replica for heavy browse/search traffic.\n- **Caching Strategy**: Redis LRU eviction; cache invalidation via BullMQ after write operations.\n- **Horizontal Scaling**: NestJS services are stateless; can run multiple instances behind a load balancer.\n- **Background Jobs**: BullMQ with multiple workers; concurrency tuned per job type (e.g., notifications high concurrency, slot computation low).\n- **GraphQL Batching**: Use DataLoader pattern inside services to avoid N+1 queries.\n\n## 9. Future Extensions\n- **Web Admin Portal**: Scaffolded under `apps/web` using same `libs/ui` and `libs/types`.\n- **Real‑time Updates**: Replace polling with WebSocket (NestJS `@nestjs/websockets`) for live slot changes.\n- **AI Recommendations**: Add a recommendation service leveraging collaborative filtering stored in a separate Redis graph.\n- **Multi‑tenancy**: If expanding to multiple cities, add tenant_id column and enforce via middleware.\n\n## 10. Summary\nThe architecture separates concerns into clearly bounded services (Auth, Business, Booking, Notification, Payment, Admin/Portal, Analytics) backed by a relational database with PostGIS for geo‑queries, Redis for caching and job queuing, and a monorepo managed by Nx/pnpm. The mobile client leverages Expo, React Router, TanStack Query, and Reanimated for a smooth UI, while the backend uses NestJS with Prisma ORM for type‑safe data access. CI/CD pipelines via GitHub Actions, EAS Build, and Docker Compose ensure reliable, automated delivery. This design supports scalability, maintainability, and rapid iteration for the Planity Clone feature set.\n",
  "summary": "A clean‑architecture monorepo with Expo/React Native mobile, NestJS microservices, PostgreSQL+PostGIS, Redis, and Nx/pnpm tooling, delivering scalable, maintainable Planity Clone functionality."
}