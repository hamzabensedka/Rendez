{
  "content": "# Planity Clone System Architecture\n\n## 1. Overview\nPlanity Clone is a mobile-first appointment booking platform connecting users with local service businesses. The system follows a clean, layered architecture with clear separation of concerns: presentation (Expo/React Native), application/services (NestJS), data (Prisma/PostgreSQL/PostGIS), caching (Redis), and background processing (BullMQ). The monorepo is managed with Nx and pnpm, enabling code sharing between mobile, backend, and shared libraries.\n\n## 2. Technology Stack\n- **Mobile**: Expo (SDK 50), React Native, TypeScript, Expo Router, TanStack React Query, React Native Reanimated\n- **Backend**: NestJS (Node.js), TypeORM via Prisma, PostgreSQL + PostGIS extension, Redis, BullMQ\n- **DevOps**: Docker Compose (local), GitHub Actions (CI/CD), EAS Build (Expo), Supabase (auth & storage optional), pnpm workspace, Nx\n- **Testing**: Jest (unit/integration), React Native Testing Library\n- **Design System**: Shared UI component library (built with React Native Reanimated & Tailwind‑like styling)\n\n## 3. High‑Level Architecture\n```\n+-------------------+      +-------------------+      +-------------------+\n|   Mobile App      | <--> |   API Gateway     | <--> |   Backend Services|\n| (Expo/React)      |      | (NestJS Controllers)|    | (NestJS Modules)  |\n+-------------------+      +-------------------+      +-------------------+\n        ^                         ^                         |\n        |                         |                         v\n        |                 +-------------------+      +-------------------+\n        |                 |   Auth Service    |      |   Payment Service |\n        |                 | (Supabase/JWT)    |      | (Stripe/PayPal)   |\n        |                 +-------------------+      +-------------------+\n        |                         ^                         |\n        |                         |                         v\n        |                 +-------------------+      +-------------------+\n        |                 |   Redis Cache     |      |   BullMQ Workers  |\n        |                 +-------------------+      +-------------------+\n        |                         |                         |\n        |                         v                         v\n        |                 +-------------------+      +-------------------+\n        |                 |   PostgreSQL      |<---->|   PostGIS Ext.    |\n        |                 +-------------------+      +-------------------+\n        +--------------------------------------------------------+\n```\n\n### 3.1 Service Boundaries\n| Service | Responsibility | Key Technologies |\n|---------|----------------|------------------|\n| **Auth** | User registration, login, JWT issuance, social login (optional) | Supabase Auth / custom NestJS guard |\n| **Business** | CRUD for businesses, categories, services, location (PostGIS) | NestJS + Prisma |\n| **Booking** | Appointment creation, slot computation, validation, timezone handling | NestJS + BullMQ (slot computation jobs) |\n| **Payment** | Payment intent creation, webhook handling, refunds | Stripe SDK (or PayPal) |\n| **Reviews** | Ratings, comments, business responses | NestJS + Prisma |\n| **Notifications** | Push & email notifications, preference management | Expo Push + BullMQ worker |\n| **Admin** | Dashboard APIs for users, businesses, appointments, reviews | NestJS (role‑based guard) |\n| **Provider Portal** | Business‑owner specific APIs (manage profile, appointments, reviews) | NestJS (role‑based guard) |\n| **Shared** | TypeScript interfaces, DTOs, utility functions, design system tokens | Nx library (`@planity/shared`) |\n\n## 4. Monorepo Structure (Nx + pnpm)\n```\nplanity-clone/\n├─ apps/\n│  ├─ mobile/            # Expo React Native app\n│  │   ├─ src/\n│  │   │   ├─ assets/\n│  │   │   ├─ components/          # Reusable UI (design system)\n│  │   │   ├─ screens/             # Expo Router file‑based routes\n│  │   │   ├─ hooks/\n│  │   │   ├─ navigation/\n│  │   │   ├─ services/            # API clients (React Query)\n│  │   │   ├─ store/               # Optional Zustand/Jotai\n│  │   │   ├─ types/               # Re‑export from @planity/shared\n│  │   │   └─ __tests__/\n│  │   ├─ app.json\n│  │   ├─ eas.json\n│  │   └─ tailwind.config.js (if using TailwindRN)\n│  └─ api/               # NestJS backend\n│      ├─ src/\n│      │   ├─ auth/\n│      │   ├─ business/\n│      │   ├─ booking/\n│      │   ├─ payment/\n│      │   ├─ reviews/\n│      │   ├─ notifications/\n│      │   ├─ admin/\n│      │   ├─ provider/\n│      │   ├─ common/          # guards, interceptors, pipes, utils\n│      │   ├─ app.module.ts\n│      │   └─ main.ts\n│      ├─ test/\n│      ├─ prisma/\n│      │   ├─ schema.prisma\n│      │   └─ migrations/\n│      ├─ dockerfile\n│      └─ nest-cli.json\n├─ libs/\n│  ├─ shared/            # TypeScript interfaces, enums, constants\n│  ├─ ui/                # Design system components (Reanimated, Tailwind)\n│  ├─ utils/             # Logging, date helpers, geo utilities\n│  └─ test-utils/\n├─ prisma/               # Shared Prisma schema (referenced by api)\n├─ docker-compose.yml    # Local dev: api, db, redis, mailhog\n├─ nx.json\n├─ package.json          # pnpm workspace root\n└─ .github/\n    └─ workflows/\n        ├─ ci.yml        # lint, test, build\n        └─ cd.yml        # EAS build + Docker push\n```\n\n## 5. Mobile App Details\n- **Routing**: Expo Router (file‑based) → `src/screens/(tabs)/...` for tab navigation, `src/screens/auth/...` for auth flows.\n- **State Management**: React Query for server state (caching, background refetch). Local UI state via React hooks or Zustand.\n- **Animations**: React Native Reanimated 2 for smooth transitions, gesture‑based filters, map interactions.\n- **Design System**: Shared `ui` library provides themed components (Button, Input, Card, MapMarker) with dark mode support.\n- **Offline**: Query persistence via `react-query/persistQueryClient` (AsyncStorage).\n- **Push Notifications**: Expo Push Token service → stored in backend via `/notifications/register`.\n- **Testing**: Jest + React Native Testing Library for unit/component tests; Detox for e2e (optional).\n\n## 6. Backend (NestJS) Details\n- **Module per Bounded Context**: Each service (auth, business, booking, etc.) is an NestJS module with its own controllers, services, DTOs, and guards.\n- **Validation**: Use class‑validator + class‑transformer via ValidationPipe.\n- **Authentication**: JWT strategy; access token short‑lived, refresh token stored in HttpOnly cookie (or Supabase).\n- **Authorization**: Custom `@Roles()` guard (USER, PROVIDER, ADMIN).\n- **Database**: Prisma ORM with PostgreSQL provider; PostGIS extension enabled via `prisma` provider (`extension postgis;`).\n- **Geo Queries**: Use Prisma’s raw queries or `@prisma/client` with `ST_DistanceSphere` for nearby business search.\n- **Caching**: Redis layer (via `ioredis`) for:\n  - Frequently accessed business listings\n  - Session store (optional)\n  - Rate limiting\n- **Background Jobs**: BullMQ queues:\n  - `slot-computation`: pre‑computes availability for a business/service range.\n  - `notification-send`: sends push/email via Expo or SMTP.\n  - `payment-reconcile`: handles webhook retries.\n  Workers are separate NestJS processes (`nest start --worker`).\n- **API Versioning**: URL versioning (`/api/v1/`).\n- **Documentation**: Swagger/OpenAPI generated via `@nestjs/swagger`.\n- **Testing**: Jest unit tests for services/controllers; integration tests using SuperTest against a test DB.\n\n## 7. Data Model (Prisma Snippet)\n```prisma\ndatasource db {\n  provider = \"postgresql\"\n  url      = env(\"DATABASE_URL\")\n}\ngenerator client {\n  provider = \"prisma-client-js\"\n}\n\nmodel Business {\n  id            String   @id @default(uuid())\n  name          String\n  address       String\n  location      Json     @db.Jsonb  // PostGIS point stored as {lat, lng}\n  categories    Category[] @relation(\"BusinessCategory\")\n  services      Service[]\n  workingHours  WorkingHour[]\n  reviews       Review[]\n  createdAt     DateTime @default(now())\n  updatedAt     DateTime @updatedAt\n}\n\nmodel Service {\n  id          String   @id @default(uuid())\n  name        String\n  durationInt Int      // minutes\n  price       Float\n  business    Business @relation(fields: [businessId], references: [id])\n  businessId  String\n  providers   Provider[]\n}\n\nmodel Appointment {\n  id            String   @id @default(uuid())\n  user          User     @relation(fields: [userId], references: [id])\n  service       Service  @relation(fields: [serviceId], references: [id])\n  provider      Provider @relation(fields: [providerId], references: [id])\n  startTime     DateTime\n  endTime       DateTime\n  status        AppointmentStatus\n  paymentId     String?\n  createdAt     DateTime @default(now())\n  updatedAt     DateTime @updatedAt\n}\n\n// ... User, Provider, Review, Category, WorkingHour, Notification, etc.\n```\n\n## 8. DevOps & CI/CD\n- **Local Development**: `docker compose up` spins up `api`, `postgres`, `redis`, `mailhog` (for email preview).\n- **Testing**: GitHub Actions runs `nx run-many --target=test --all` on every PR.\n- **Build**:\n  - Mobile: EAS Build (`eas build --platform ios/android`) triggered on `main` tag.\n  - API: Docker image built and pushed to GitHub Packages/Docker Hub.\n- **Deployment**: (Out of scope) – Kubernetes or AWS ECS with rolling updates; environment variables managed via Doppler or GitHub Secrets.\n- **Monitoring**: Sentry (frontend & backend), Loki/Prometheus for logs/metrics (optional).\n\n## 9. Shared Types & Design System\n- **`@planity/shared`**: Contains TypeScript interfaces (`UserDto`, `BusinessDto`, `AppointmentDto`, etc.) used by both mobile (via generated client) and backend, ensuring contract consistency.\n- **`@planity/ui`**: Theme‑agnostic components built with React Native Reanimated 2 and Tailwind‑like styling (via `tailwind-rn`). Exposes tokens (colors, spacing, radius) for dark/light mode.\n\n## 10. Testing Strategy\n| Layer | Tool | Scope |\n|-------|------|-------|\n| Unit | Jest | Pure functions, utilities, service methods |\n| Component | Jest + React Native Testing Library | UI components in isolation |\n| Integration | SuperTest (API) + React Native Testing Library (screen navigation) | End‑to‑end API contracts, navigation flows |\n| E2E (optional) | Detox | Critical user journeys (login → book → cancel) |\n| Load | k6 / Artillery | Simulate peak booking slots |\n\n## 11. Security Considerations\n- HTTPS everywhere (Expo managed, NestJS behind TLS termination).\n- JWT stored securely; refresh token rotation.\n- Input validation & sanitization (Prisma prevents SQL injection).\n- Rate limiting on auth endpoints via Redis.\n- File uploads (if any) scanned and stored via Supabase Storage with signed URLs.\n- Environment secrets never committed; managed via GitHub Secrets & Doppler.\n\n## 12. Summary\nThe architecture separates concerns into clearly bounded modules, leverages a monorepo for code sharing, uses modern React Native tooling for a performant UI, and relies on NestJS/Prisma/PostGIS for a scalable, geo‑aware backend, all orchestrated via Docker, GitHub Actions, and EAS Build for reliable CI/CD.\n\n---\n*Generated by Jordan, Staff Software Engineer – Architecture Focus.*\n",
  "summary": "Planity Clone adopts a clean, layered monorepo architecture with Expo/React Native mobile, NestJS/Prisma/PostGIS backend, Redis caching, BullMQ background jobs, and unified CI/CD via Docker, GitHub Actions, and EAS Build."
}