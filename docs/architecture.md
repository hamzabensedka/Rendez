{
  "content": "# Planity Clone Architecture\\n\\n## 1. Overview\\nThis document describes the system architecture for the Planity Clone mobile app and its supporting backend services. The solution follows a clean‑architecture approach with clear separation of concerns, leveraging a monorepo managed by Nx, pnpm workspaces, and Docker Compose for local development.\\n\\n## 2. Technology Stack\\n- **Mobile**: Expo (SDK 49), React Native, TypeScript, Expo Router, TanStack React Query, React Native Reanimated\\n- **Backend**: NestJS (Node.js), Prisma ORM, PostgreSQL + PostGIS extension, Redis, BullMQ (job queue)\\n- **DevOps**: Nx monorepo, pnpm, Docker Compose, GitHub Actions, EAS Build (Expo), Supabase (optional auth/storage fallback)\\n- **Testing**: Jest (unit/integration), React Native Testing Library\\n\\n## 3. High‑Level Architecture\\n```\\n+-------------------+        +---------------------+        +-------------------+\\n|   Mobile App      | <----> |   API Gateway (Nest) | <----> |   PostgreSQL/PostGIS|\\n| (Expo RN)        |        |   - Auth Service    |        |   - Redis Cache   |\\n|                   |        |   - Business Svc    |        |   - BullMQ Workers|\\n|                   |        |   - Booking Svc     |        +-------------------+\\n|                   |        |   - Payment Svc     |\\n|                   |        |   - Admin Svc       |\\n|                   |        |   - Provider Portal |\\n+-------------------+        +---------------------+\\n```\\nThe mobile app communicates exclusively via REST/GraphQL (NestJS) over HTTPS. All business logic resides in the backend; the mobile layer focuses on presentation, navigation, and client‑state caching.\\n\\n## 4. Monorepo Structure (Nx)\\n```\\nplanity-clone/\\n├── apps/\\n│   ├── mobile/                 # Expo React Native app\\n│   │   ├── src/\\n│   │   │   ├── assets/\\n│   │   │   ├── components/     # Shared UI components (design system)\\n│   │   │   ├── hooks/\\n│   │   │   ├── navigation/     # Expo Router config\\n│   │   │   ├── screens/\\n│   │   │   │   ├── auth/\\n│   │   │   │   ├── browse/\\n│   │   │   │   ├── business/\\n│   │   │   │   ├── booking/\\n│   │   │   │   ├── profile/\\n│   │   │   │   ├── favorites/\\n│   │   │   │   └── map/\\n│   │   │   ├── store/            # React Query providers & query keys\\n│   │   │   ├── utils/\\n│   │   │   └── types/\\n│   │   ├── app.json\\n│   │   ├── eas.json\\n│   │   └── tsconfig.json\\n│   └── api/                    # NestJS backend\\n│       ├── src/\\n│       │   ├── auth/\\n│       │   ├── business/\\n│       │   ├── booking/\\n│       │   ├── payment/\\n│       │   ├── admin/\\n│       │   ├── provider/\\n│       │   ├── common/         # guards, interceptors, dto, validation\\n│       │   ├── jobs/           # BullMQ processors\\n│       │   ├── prisma/         # Prisma client & migrations\\n│       │   ├── redis/          # Redis wrapper\\n│       │   ├── app.module.ts\\n│       │   └── main.ts\\n│       └── test/\\n├── libs/                       # Shared Nx libraries\\n│   ├── ui/                     # Design system (React Native primitives)\\n│   ├── types/                  # Shared TypeScript interfaces (DTOs, entities)\\n│   ├── utils/                  # Logging, date helpers, geo utils\\n│   └── test-helpers/\\n├── prisma/                     # Prisma schema & migrations (shared)\\n├── docker-compose.yml\\n├── nx.json\\n└── package.json\\n```\\n\\n## 5. Service Boundaries (Backend)\\n| Service | Responsibility | Key Tables / Collections |\\n|---------|----------------|--------------------------|\\n| Auth    | User registration, login, password reset, social OAuth, JWT issuance | `users`, `sessions`\\n| Business| CRUD for business listings, categories, location (PostGIS), operating hours | `businesses`, `categories`, `business_hours`\\n| Booking | Appointment creation, rescheduling, cancellation, slot computation | `appointments`, `services`, `time_slots`\\n| Payment | Integration with payment gateway (Stripe/PayPal), webhook handling | `payments`, `payment_intents`\\n| Provider| Business owner portal: manage listings, services, staff, schedule | same as Business + `staff`, `service_definitions`\\n| Admin   | Global settings, user moderation, business verification, analytics | `settings`, `audit_logs`\\n| Jobs    | Background workers: notification sending, slot recomputation, cleanup | N/A (uses Redis BullMQ queues)\\n\\n## 6. Data Layer\\n- **Primary DB**: PostgreSQL with PostGIS for geospatial queries (nearby businesses, distance filtering).\\n| Entity | Key Fields |\\n|--------|------------|\\n| User   | id, email, passwordHash, role, createdAt, updatedAt\\n| Business| id, name, description, location (Point), categoryId, avgRating\\n| Service| id, businessId, name, duration, price\\n| Appointment| id, userId, serviceId, startTime, endTime, status, paymentId\\n| Review | id, businessId, userId, rating, comment, createdAt\\n|\\n- **Cache**: Redis stores frequently accessed read‑only data (business listings, popular categories, computed slot maps) with TTL 5‑15 min.\\n- **Job Queue**: BullMQ backed by Redis; processors live in the `jobs` folder and are triggered by API endpoints (e.g., after booking, trigger slot recomputation & notification).\\n\\n## 7. Mobile App Architecture\\n- **State Management**: TanStack React Query handles server state (queries/mutations) with automatic caching, background refetch, and optimistic updates.\\n- **Navigation**: Expo Router provides file‑based routing (tabs, stack, modal).\\n- **Animations**: React Native Reanimated 2 for smooth transitions (map markers, modal presentations).\\n- **Design System**: Shared `ui` library provides themed components (Button, Input, Card) using React Native Paper or custom styled components, ensuring consistency.\\n- **Type Safety**: Shared `types` library defines DTOs matching backend contracts; generated via `openapi-typescript` from NestJS Swagger docs (or manually kept in sync).\\n- **Offline**: Query results are cached; optimistic UI updates improve perceived performance.\\n\\n## 8. API Contracts\\nBackend exposes REST endpoints (NestJS controllers) with versioning (`/api/v1/`). Example:\\n```\\nGET   /api/v1/businesses?lat=&lng=&radius=&category=&search=\\nPOST  /api/v1/auth/register\\nPOST  /api/v1/bookings\\nPUT   /api/v1/bookings/:id/cancel\\n```\\nAll DTOs are validated via class‑validator and class‑transformer. OpenAPI spec is generated (`@nestjs/swagger`) and consumed by the mobile app for type generation.\\n\\n## 9. Security Considerations\\n- Authentication: JWT (access token 15 min, refresh token 7 days) stored securely in Expo’s `SecureStore`.\\n- Authorization: NestJS guards (`RolesGuard`, `OwnershipGuard`) enforce RBAC (user, provider, admin).\\n- Data Protection: TLS everywhere; sensitive fields (password, payment info) never logged.\\n- Rate Limiting: NestJS `ThrottlerGuard` per‑route.\\n- Input Validation: DTOs + Prisma schema constraints.\\n- Geo‑fencing: PostGIS `ST_DWithin` for distance queries; indexes on geography column.\\n\\n## 10. DevOps & CI/CD\\n- **Local Development**: Docker Compose spins up PostgreSQL (with PostGIS), Redis, and the NestJS API. Expo dev client runs locally.\\n- **CI**: GitHub Actions workflow runs on push/PR:\\n  1. Install pnpm workspace\\n  2. Lint (ESLint)\\n  3. Type‑check (tsc)\\n  4. Run Jest unit tests (mobile & api)\\n  5. Build Docker images (api) and push to registry\\n  6. Run integration tests against ephemeral DB (using Testcontainers)\\n- **CD**:\\n  - API: Docker image deployed to a managed Kubernetes service (or VM) via GitHub Actions\\n  - Mobile: EAS Build creates iOS/Android binaries; release channels managed through Expo; automatic upload to TestFlight/Google Play internal track.\\n- **Env Vars**: Managed via GitHub Secrets; `.env.example` provided for local dev.\\n\\n## 11. Testing Strategy\\n- **Unit**: Jest with mocking of external services (Prisma, Redis, payment gateway).\\n- **Integration**: API tests using SuperTest against a real Postgres/Redis container; mobile UI tests with React Native Testing Library + Jest.\\n- **E2E** (optional): Detox for critical flows (login → booking → confirmation).\\n- **Coverage Target**: ≥80% for backend, ≥70% for mobile.\\n\\n## 12. Monitoring & Observability\\n- Backend: NestJS built‑in logger (pino) forwarded to ELK or Loki; metrics via Prometheus endpoint (`@nestjs/microservices`).\\n- Mobile: Expo’s `expo-dev-client` logs; Sentry for error tracking; Firebase Performance for network & UI metrics.\\n- Jobs: BullMQ dashboard (optional) for queue depth & processing times.\\n\\n## 13. Summary of Decisions\\n- **Monorepo with Nx** enables shared types, UI components, and streamlined versioning.\\n- **Expo** simplifies mobile builds and OTA updates while providing access to native modules via the Expo SDK.\\n- **TanStack React Query** gives robust server‑state handling, reducing boilerplate.\\n- **NestJS** provides modular, testable backend with built‑in validation and microservices readiness.\\n- **Prisma + PostGIS** offers type‑safe ORM and powerful geo queries essential for location‑based search.\\n- **Redis** serves both caching and reliable job queue (BullMQ).\\n- **Docker Compose** ensures reproducible local environments matching production.\\n- **GitHub Actions + EAS Build** automate testing, building, and distribution.\\n\\n---\\n*This architecture satisfies all functional requirements (auth, browse, search, map, booking, payments, notifications, provider portal, admin dashboard, background jobs) while maintaining scalability, maintainability, and clear separation of concerns.*\\n",
  "summary": "A clean‑architecture monorepo plan for Planity Clone using Expo React Native, NestJS, Prisma/PostGIS, Redis, BullMQ, Nx, pnpm, Docker, GitHub Actions, and EAS Build, with defined mobile and backend service boundaries, data models, and CI/CD strategy."
}