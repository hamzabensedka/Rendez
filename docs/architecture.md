{
  "content": "# Planity Clone System Architecture\n\n## 1. Overview\nThe Planity Clone is a monorepo built with Expo (React Native) and a NestJS backend. The architecture follows clean architecture principles with clear separation of concerns: presentation, domain, application, and infrastructure layers. The monorepo is managed by Nx and pnpm, enabling code sharing between mobile, server, and shared libraries.\n\n## 2. High‑Level Components\n- **Mobile Client** (Expo, React Native, TypeScript, Expo Router, TanStack React Query, React Native Reanimated)\n- **API Server** (NestJS, Prisma ORM, PostgreSQL + PostGIS, Redis, BullMQ)\n- **Shared Libraries** (TypeScript types, design system, utilities)\n- **Infrastructure** (Docker Compose for local dev, GitHub Actions CI/CD, EAS Build for mobile, Supabase for auth/storage fallback, PostgreSQL managed, Redis, Nginx reverse proxy optional)\n\n## 3. Folder Structure (Nx Monorepo)\n```\nplanity-clone/\n├─ apps/\n│  ├─ mobile/            # Expo React Native app\n│  │   ├─ src/\n│  │   │   ├─ assets/\n│  │   │   ├─ components/\n│  │   │   ├─ screens/\n│  │   │   ├─ navigation/\n│  │   │   ├─ hooks/\n│  │   │   ├─ services/   # API clients (React Query)\n│  │   │   ├─ store/      # Optional state (e.g., Zustand)\n│  │   │   ├─ themes/\n│  │   │   └─ utils/\n│  │   ├─ app.json\n│  │   ├─ eas.json\n│  │   └─ tsconfig.json\n│  └─ api/               # NestJS server\n│      ├─ src/\n│      │   ├─ modules/\n│      │   │   ├─ auth/\n│      │   │   ├─ business/\n│      │   │   ├─ booking/\n│      │   │   ├─ appointment/\n│      │   │   ├─ favorite/\n│      │   │   ├─ profile/\n│      │   │   ├─ notification/\n│      │   │   ├─ provider/\n│      │   │   └─ admin/\n│      │   ├─ shared/\n│      │   │   ├─ dto/\n│      │   │   ├─ guards/\n│      │   │   ├─ interceptors/\n│      │   │   └─ utils/\n│      │   ├─ prisma/\n│      │   │   ├─ schema.prisma\n│      │   │   └─ migrations/\n│      │   ├─ main.ts\n│      │   └─ tsconfig.json\n│      └─ Dockerfile\n├─ libs/\n│  ├─ types/             # Shared TypeScript interfaces (used by mobile & api)\n│  ├─ design-system/     # React Native components (styled with Reanimated)\n│  ├─ utils/             # Helper functions (date, geo, validation)\n│  └─ config/            # Nx workspace config, eslint, prettier\n├─ docker-compose.yml\n├─ .github/\n│   └─ workflows/\n│       ├─ ci.yml        # lint, test, build\n│       └─ cd.yml        # deploy to EAS, Docker registry\n├─ nx.json\n├─ package.json          # pnpm workspace root\n└─ README.md\n```\n\n## 4. Service Boundaries (Bounded Contexts)\n\n| Context | Responsibility | Owner (Mobile/API) | Key Entities |\n|---------|----------------|--------------------|--------------|\n| **Auth** | User registration, login, email verification, social auth, JWT issuance | API (NestJS AuthModule) + Mobile (expo-auth-session) | User, RefreshToken |\n| **Business** | CRUD of business info, location (PostGIS), categories, operating hours | API (BusinessModule) | Business, Category, BusinessHours |\n| **Service** | Services offered by a business (haircut, massage, etc.) | API (BusinessModule) | Service, Price |\n| **Booking** | Appointment creation, validation, conflict detection | API (BookingModule) | Booking, BookingSlot |\n| **Appointment** | User‑side view, reschedule, cancel, reminders | API (AppointmentModule) + Mobile (React Query) | Appointment, Notification |\n| **Favorite** | User‑favorite businesses | API (FavoriteModule) | Favorite |\n| **Profile** | User profile management, password change | API (ProfileModule) | UserProfile |\n| **Notification** | Real‑time push & in‑app notifications (email, SMS, push) | API (NotificationModule) + BullMQ workers | NotificationTemplate, SentNotification |\n| **Provider Portal** | Business owner dashboard: manage business, services, schedule, view bookings | API (ProviderModule) + separate web (could be Next.js but out of scope) | ProviderSession |\n| **Admin Dashboard** | System‑wide admin: users, businesses, analytics | API (AdminModule) | AdminUser, AuditLog |\n| **Availability & Slot Computation** | Background job that recalculates free slots based on business hours & existing bookings | API (AvailabilityWorker) (BullMQ) | SlotCache |\n| **Payments** | Integration with payment gateway (Stripe/PayPal) | API (PaymentModule) | Transaction |\n| **Reviews & Ratings** | CRUD of reviews, responses | API (ReviewModule) | Review, ReviewResponse |\n\nEach context is implemented as a NestJS module with its own DTOs, services, controllers, and repositories (Prisma). Mobile consumes them via a generated API client (or manual fetch) wrapped in React Query hooks.\n\n## 5. Data Model (Prisma Schema Highlights)\n\n```prisma\nmodel User {\n  id            String   @id @default(uuid())\n  email         String   @unique\n  passwordHash  String\n  name          String?\n  phone         String?\n  role          Role     @default(USER)\n  createdAt     DateTime @default(now())\n  updatedAt     DateTime @updatedAt\n  refreshTokens RefreshToken[]\n  favorites     Favorite[]\n  bookings      Booking[]\n  appointments  Appointment[]\n  reviews       Review[]\n}\n\nmodel Business {\n  id            String   @id @default(uuid())\n  name          String\n  description   String?\n  address       String\n  latitude      Float\n  longitude     Float\n  location      Point?   @db.Point\n  categoryId    String\n  category      Category @relation(fields: [categoryId], references: [id])\n  ownerId       String\n  owner         User     @relation(fields: [ownerId], references: [id])\n  hours         BusinessHours[]\n  services      Service[]\n  bookings      Booking[]\n  reviews       Review[]\n}\n\nmodel Category {\n  id   String   @id @default(uuid())\n  name String   @unique\n  businesses Business[]\n}\n\nmodel Service {\n  id          String   @id @default(uuid())\n  name        String\n  durationMin Int\n  price       Float\n  businessId  String\n  business    Business @relation(fields: [businessId], references: [id])\n}\n\nmodel Booking {\n  id            String   @id @default(uuid())\n  userId        String\n  businessId    String\n  serviceId     String\n  slotStart     DateTime\n  slotEnd       DateTime\n  status        BookingStatus\n  user          User     @relation(fields: [userId], references: [id])\n  business      Business @relation(fields: [businessId], references: [id])\n  service       Service  @relation(fields: [serviceId], references: [id])\n  payment       Payment?\n}\n\nmodel Appointment {\n  id            String   @id @default(uuid())\n  bookingId     String   @unique\n  booking       Booking  @relation(fields: [bookingId], references: [id])\n  userId        String\n  user          User     @relation(fields: [userId], references: [id])\n  startsAt      DateTime\n  endsAt        DateTime\n  status        AppointmentStatus\n  reminderSent  Boolean  @default(false)\n}\n\nmodel Favorite {\n  id        String   @id @default(uuid())\n  userId    String\n  businessId String\n  user      User     @relation(fields: [userId], references: [id])\n  business  Business @relation(fields: [businessId], references: [id])\n  @@unique([userId, businessId])\n}\n\nmodel Review {\n  id        String   @id @default(uuid())\n  businessId String\n  userId     String\n  rating     Int\n  comment    String?\n  createdAt  DateTime @default(now())\n  business   Business @relation(fields: [businessId], references: [id])\n  user       User     @relation(fields: [userId], references: [id])\n  responses  ReviewResponse[]\n}\n\nmodel ReviewResponse {\n  id        String   @id @default(uuid())\n  reviewId  String\n  reply     String\n  createdAt DateTime @default(now())\n  review    Review   @relation(fields: [reviewId], references: [id])\n}\n\nmodel Notification {\n  id        String   @id @default(uuid())\n  userId    String\n  type      NotificationType\n  payload   Json\n  sentAt    DateTime?\n  read      Boolean  @default(false)\n  user      User     @relation(fields: [userId], references: [id])\n}\n\nmodel Payment {\n  id            String   @id @default(uuid())\n  bookingId     String   @unique\n  amount        Float\n  currency      String   @default(\"USD\")\n  status        PaymentStatus\n  provider      String   // e.g., stripe_charge_id\n  booking       Booking  @relation(fields: [bookingId], references: [id])\n}\n\nenum Role { USER, PROVIDER, ADMIN }\nenum BookingStatus { PENDING, CONFIRMED, COMPLETED, CANCELLED }\nenum AppointmentStatus { SCHEDULED, IN_PROGRESS, COMPLETED, NO_SHOW }\nenum NotificationType { BOOKING_CONFIRMED, APPOINTMENT_REMINDER, REVIEW_RESPONSE, PROMO }\nenum PaymentStatus { PENDING, SUCCEEDED, FAILED, REFUNDED }\n```\n\nGeospatial queries use PostGIS `Point` type; we can fetch businesses within a radius using Prisma's raw query or `@distance`.\n\n## 6. API Contracts (RESTful)\n\n- Base URL: `/api`\n- Versioning: `/api/v1/`\n- Auth: Bearer JWT in `Authorization` header.\n- Standard response envelope: `{ success: boolean, data?: any, error?: { message: string, code?: string } }`\n- Validation via NestJS `class-validator` and DTOs.\n- Pagination: `page` & `limit` query params.\n- Filtering: query params per resource (e.g., `?category=hair&lat=40.7&lng=-73.9&distance=5000`).\n\nKey endpoints (examples):\n- `POST /v1/auth/register`\n- `POST /v1/auth/login`\n- `GET /v1/businesses?lat=&lng=&distance=&category=`\n- `GET /v1/businesses/:id`\n- `POST /v1/bookings`\n- `GET /v1/appointments/me`\n- `POST /v1/favorites`\n- `GET /v1/notifications/me`\n- `POST /v1/payments/webhook` (Stripe)\n\nWebSocket gateway (`/ws/notifications`) for real‑time push using NestJS `Gateway` backed by Redis PubSub.\n\n## 7. Real‑Time & Background Jobs\n\n- **WebSocket**: NestJS `@WebSocketGateway` with `redis-adapter` (via `@nestjs/platform-socket.io` + `socket.io-redis`). Clients connect via Expo `socket.io-client`.\n- **BullMQ**: Queues defined in `libs/queues` (or directly in API):\n  - `email-queue` – send verification, booking confirmation.\n  - `push-queue` – push notifications via Expo push service.\n  - `slot-recalc` – runs every minute or on business hour change, computes free slots and caches in Redis.\n  - `payment-retry` – handle failed payment attempts.\n- Workers are separate NestJS processes (`npm run start:worker queue-name`) or integrated via `@nestjs/bullmq`.\n\n## 8. Shared Libraries\n\n- **libs/types**: Contains all TypeScript interfaces (User, Business, Booking, etc.) generated from Prisma schema via `prisma-typescript` or manually kept in sync; used by both mobile and API for type safety.\n- **libs/design-system**: Reusable React Native components (Button, Card, Input, MapView) built with React Native Reanimated for smooth animations; styled with a theme context.\n- **libs/utils**: Date helpers, haversine distance, validation, logging.\n- **libs/config**: Nx workspace configuration, Jest preset, ESLint, Prettier.\n\n## 9. Development Workflow\n\n- **Local Dev**: `docker compose up` spins up PostgreSQL (with PostGIS extension), Redis, and optionally Maildev for email testing.\n- **Nx Commands**:\n  - `nx run mobile:dev` – Expo dev server.\n  - `nx run api:start` – NestJS in watch mode.\n  - `nx run api:test` – Jest unit/integration tests.\n  - `nx run mobile:test` – Jest for React Native (using `@testing-library/react-native`).\n- **Code Formatting**: `nx format`.\n- **Linting**: `nx lint`.\n\n## 10. CI/CD (GitHub Actions)\n\n- **CI Workflow** (`.github/workflows/ci.yml`):\n  - Install pnpm dependencies.\n  - Run lint, type-check, unit tests for mobile and api.\n  - Build Docker images for API.\n  - Run EAS build preview for mobile (optional).\n- **CD Workflow** (`.github/workflows/cd.yml`):\n  - On push to `main`:\n    - Deploy API Docker image to a container registry (e.g., GitHub Packages) and then to a VPS/Kubernetes.\n    - Run database migrations (`npx prisma migrate deploy`).\n    - Trigger EAS build for production and submit to App Store/Play Store.\n    - Notify via Slack/email.\n\n## 11. Deployment & Infrastructure\n\n- **Docker Compose** (dev):\n  ```yaml\n  services:\n    postgres:\n      image: postgis/postgis:15\n      environment: POSTGRES_USER=planity, POSTGRES_PASSWORD=secret, POSTGRES_DB=planity\n      ports: [\"5432:5432\"]\n      volumes: [pg_data:/var/lib/postgresql/data]\n    redis:\n      image: redis:7\n      ports: [\"6379:6379\"]\n    maildev:\n      image: maildev/maildev\n      ports: [\"1080:1080\"]\n  ```\n- **Production**: Managed PostgreSQL (e.g., Supabase or AWS RDS) with PostGIS extension, Elasticache Redis, NestJS deployed as Docker containers behind an NGINX or ALB, Expo OTA updates via EAS, optional CDN for assets.\n- **Secrets**: Stored in GitHub Secrets, injected into CI/CD and runtime via environment variables (`DATABASE_URL`, `REDIS_URL`, `JWT_SECRET`, `STRIPE_KEY`, `EXPO_PUSH_TOKEN`).\n\n## 12. Testing Strategy\n\n- **Unit Tests**: Jest for services, controllers, utility functions.\n- **Integration Tests**: Supertest for API endpoints; React Native Testing Library for mobile components.\n- **E2E Tests** (optional): Detox for mobile flows; Cypress for admin portal.\n- **Test Coverage**: Aim >80% for critical paths (auth, booking, payment).\n\n## 13. Security Considerations\n\n- Passwords hashed with bcrypt (Salt 12).\n- JWT access token short-lived (15 min), refresh token stored HTTP‑only cookie or secure storage.\n- Role‑based access control (RBAC) via NestJS guards (`RolesGuard`).\n- Input validation and sanitization.",
  "summary": "The architecture uses an Nx monorepo with Expo React Native mobile app and NestJS backend, leveraging Prisma, PostGIS, Redis, BullMQ, and shared libraries to implement clean, modular services for authentication, business discovery, booking, payments, notifications, and admin/provider portals."
}