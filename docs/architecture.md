{
  "content": "# Planity Clone Architecture\n\n## Overview\nThis document outlines the system architecture for the Planity Clone, a mobile-first appointment booking platform. The solution follows clean architecture principles, separates concerns into distinct layers and services, and leverages a monorepo managed by Nx for scalable development.\n\n## Tech Stack\n- **Mobile**: Expo, React Native, TypeScript, Expo Router, TanStack React Query, React Native Reanimated\n- **Backend**: NestJS, Prisma ORM, PostgreSQL (with PostGIS extension), Redis, BullMQ (for background jobs)\n- **DevOps**: Docker Compose, GitHub Actions, EAS Build, Supabase (auth & storage), Jest (testing)\n- **Monorepo Tooling**: Nx, pnpm\n\n## High-Level Architecture\nThe system is divided into three main domains:\n1. **Client Application** – Expo mobile app handling UI/UX, navigation, state, and animations.\n2. **API Gateway & Services** – NestJS microservices (modular monolith) exposing REST/GraphQL endpoints.\n3. **Data & Infrastructure** – PostgreSQL/PostGIS for relational & geospatial data, Redis for caching & pub/sub, BullMQ job queue, Supabase for auth & file storage.\n\nCommunication flows: Mobile ↔ API Gateway (via HTTP/WebSocket) ↔ Services ↔ DB/Redis/Supabase. Background jobs are triggered by services and processed by workers.\n\n## Service Boundaries (NestJS Modules)\nEach module is a self‑contained service with its own DTOs, entities, controllers, and services. They can be extracted to separate NestJS applications later if needed.\n\n- **Auth Service** (Supabase‑backended): user registration, login, email verification, social login, token issuance.\n- **User Service**: profile CRUD, preferences, favorites, payment methods.\n- **Business Service**: business CRUD, categories, location (PostGIS), operating hours, media uploads via Supabase Storage.\n- **Booking Service**: appointment creation, rescheduling, cancellation, slot computation, conflict detection.\n- **Provider Portal Service**: business‑owner dashboard for managing listings, schedules, bookings, and reviews.\n- **Admin Service**: admin dashboard for user/business moderation, analytics, reports.\n- **Notification Service**: templates, sending via email/push (integrated with Supabase Functions or external providers), preference management.\n- **Search Service**: full‑text + geo search (using PostgreSQL trigram & PostGIS) powering map‑based and filter‑based discovery.\n- **Payment Service**: wrapper around payment gateway (Stripe/PayPal), webhook handling, transaction records.\n- **Review Service**: ratings, comments, business responses, moderation.\n- **Job Workers**: BullMQ processors for async tasks (e.g., sending notifications, recalculating availability, cleaning expired tokens).\n\n## Folder Structure (Nx Monorepo)\n```\nplanity-clone/\n├── apps/\n│   ├── mobile/                 # Expo React Native app\n│   │   ├── src/\n│   │   │   ├── assets/\n│   │   │   ├── components/     # Shared UI components (design system)\n│   │   │   ├── navigation/     # Expo Router route definitions\n│   │   │   ├── hooks/\n│   │   │   ├── screens/        # Feature screens (auth, browse, booking, profile, etc.)\n│   │   │   ├── store/          # React Query query keys & mutators\n│   │   │   ├── animations/     # Reanimated shared values & worklets\n│   │   │   ├── utils/\n│   │   │   ├── types/          # Shared TS types (imported from libs/types)\n│   │   │   └── app.tsx\n│   │   ├── expo.config.js\n│   │   └── eas.json\n│   └── api/                    # NestJS backend (monolith)\n│       ├── src/\n│       │   ├── auth/\n│       │   ├── user/\n│       │   ├── business/\n│       │   ├── booking/\n│       │   ├── provider/\n│       │   ├── admin/\n│       │   ├── notification/\n│       │   ├── search/\n│       │   ├── payment/\n│       │   ├── review/\n│       │   ├── jobs/           # BullMQ processors\n│       │   ├── common/         # Guards, interceptors, DTO bases, utils\n│       │   ├── config/\n│       │   ├── main.ts\n│       │   └── app.module.ts\n│       └── test/\n├── libs/\n│   ├── types/                  # Shared TS interfaces (DTOs, entities, enums)\n│   ├── design-system/          # UI kit (tokens, primitives, themes) used by mobile\n│   ├── utils/                  # Cross‑platform helpers (date, geo, validation)\n│   └── test-utils/\n├── infrastructure/\n│   ├── docker-compose.yml      # Services: postgres, redis, supabase (studio), api, worker\n│   ├── prisma/\n│   │   ├── schema.prisma       # Defines all models with PostGIS extensions\n│   │   └── migrations/\n│   └── supabase/\n│       ├── functions/          # Edge functions for auth triggers, storage hooks\n│       └── storage/\n├── .nx/\n├── .github/\n│   └── workflows/\n│       ├── ci.yml              # Lint, test, build (Nx affected)\n│       ├── cd-mobile.yml       # EAS build & submit to stores\n│       └── cd-api.yml          # Docker image build & push\n├── jest.config.js\n├── tsconfig.base.json\n├── nx.json\n└── package.json\n```\n\n## Data Model Highlights (Prisma)\n- **User**: id, email, role (USER|PROVIDER|ADMIN), profile JSON, createdAt.\n- **Business**: id, name, description, location (Point), category JSON, coverImageUrl, ratingAvg.\n- **Service**: id, businessId, name, duration, price.\n- **Schedule**: id, businessId, weekday, startTime, endTime, slotLength.\n- **Booking**: id, userId, businessId, serviceId, startTime, endTime, status, paymentId.\n- **Review**: id, businessId, userId, rating, comment, response, createdAt.\n- **Favorite**: id, userId, businessId.\n- **Payment**: id, userId, gateway, gatewayId, amount, status.\n\nPostGIS enables distance‑based queries for \"nearby businesses\" and map bounds filtering.\n\n## State Management & Server State\n- **React Query** handles all API interactions: query keys mirror service endpoints (e.g., ['businesses', lat, lng, radius]), automatic caching, background refetch, optimistic updates for booking/favorite actions.\n- Auth state stored in SecureStore (Expo) and synchronized with Supabase auth token.\n- UI state (modal visibility, form inputs) managed with React useState/useReducer or Zustand if needed.\n\n## Navigation & Routing\n- **Expo Router** provides file‑based routing: `src/screens/(tabs)/index.tsx` for bottom tab layout (Home, Map, Bookings, Profile).\n- Deep linking supported via Expo Linking for email verification and provider invites.\n\n## Animations & Gestures\n- **React Native Reanimated 2** used for smooth transitions: map marker drag, swipe‑to‑cancel booking, modal bottom sheets, shared‑element transitions between list and detail screens.\n- Worklets defined in `src/animations/` and shared via `useAnimatedProps`/`useSharedValue`.\n\n## Authentication & Authorization\n- Supabase handles email/password, OAuth, email verification, JWT issuance.\n- NestJS Auth Guard validates JWT, attaches user info to request.\n- Role‑based access control (RBAC) implemented via custom `@Roles()` decorator and guard.\n- Provider and Admin portals protected by role checks.\n\n## Background Jobs (BullMQ)\n- Redis stores job queues.\n- Queues: `notifications`, `availability-recalc`, `cleanup-tokens`.\n- Workers reside in `apps/api/src/jobs/`; each processor is a separate class handling retries, logging, and dead‑letter queues.\n- Triggered from services after relevant mutations (e.g., after booking creation → add notification job).\n\n## API Communication\n- RESTful endpoints versioned under `/api/v1/`.\n- OpenAPI (Swagger) generated via NestJS Swagger module for documentation and client type generation (optional).\n- GraphQL could be added later; current choice favors simplicity and compatibility with React Query.\n\n## DevOps & CI/CD\n- **Docker Compose** defines local dev environment: postgres (with PostGIS), redis, supabase (studio + API), api, worker.\n- **GitHub Actions** workflows:\n  - `ci.yml`: runs `nx affected --target=lint --target=test --target=build` on PRs.\n  - `cd-mobile.yml`: on merge to main, runs EAS build for iOS & Android, uploads to stores.\n  - `cd-api.yml`: builds Docker image, pushes to registry, triggers rolling update on staging/prod.\n- **EAS Build** manages Expo OTA updates and store submissions.\n- **Supabase** provides auth, storage, and optional edge functions for custom logic (e.g., image resizing).\n- **Jest** configured for unit and integration tests; tests co‑located with feature files (`*.spec.ts`).\n\n## Monitoring & Observability\n- API logs structured JSON; forwarded to a log aggregation service (e.g., Loki) via Docker logging driver.\n- Redis queue metrics exposed via BullMQ dashboard.\n- Health check endpoints (`/health`) for each service.\n- Error tracking via Sentinel or Sentry (optional).\n\n## Security Considerations\n- HTTPS enforced in production; API behind a reverse proxy (NGINX/TLS).\n- Rate limiting on auth endpoints via NestJS Throttler.\n- Input validation using class‑validator & DTOs.\n- SQL injection prevented by Prisma ORM.\n- Sensitive data (payment tokens) never stored; only gateway references.\n- JWTs short‑lived (15 min) with refresh token rotation via Supabase.\n- File uploads validated for type and size; stored in Supabase Storage with private buckets and signed URLs.\n\n## Scalability & Future Extensibility\n- Modular NestJS services can be split into separate microservices if load increases.\n- Read replicas for PostgreSQL to scale geo‑search queries.\n- Redis clustering for higher throughput.\n- Feature flags (via Unleash or custom) to roll out new capabilities.\n- Potential migration to GraphQL or tRPC for tighter client‑server typing.\n\n---\n*This architecture provides a solid foundation for building a maintainable, scalable Planity Clone while adhering to the chosen technology stack.*\n",
  "summary": "The architecture defines a monorepo with an Expo/React Native mobile app, a NestJS/Prisma/PostGIS/Redis backend split into bounded‑context services, and CI/CD pipelines using Docker, GitHub Actions, EAS Build, and Supabase for auth and storage."
}